# Explicit single-crate workspace root: keeps the engine self-contained and
# excludes the `runner/` submodule, which is its own workspace in the
# rs-quickjs-testing repository.
[workspace]
exclude = ["runner"]

[package]
name = "rs-quickjs"
version = "0.2.44"
edition = "2024"
license = "MIT"
repository = "https://github.com/surveria/rs-quickjs"
description = "A small JavaScript engine prototype written in safe Rust, using QuickJS as a reference."
readme = "README.md"

[lib]
name = "rs_quickjs"
path = "src/lib.rs"

[[bin]]
name = "rsqjs"
path = "src/bin/rsqjs.rs"

# The engine is a standalone, dependency-light crate. The test/benchmark runner
# and its heavier dependencies live in the separate `rs-quickjs-testing`
# repository, consumed here as the `runner/` git submodule.
[dependencies]
anyhow = "1"
parking_lot = "0.12"
serde_json = "1"
thiserror = "2"

[lints.rust]
unsafe_code = "deny"

[lints.clippy]
nursery = { level = "deny", priority = -1 }
pedantic = { level = "deny", priority = -1 }
uninlined_format_args = "deny"
unwrap_used = "deny"
expect_used = "deny"
panic = "deny"
todo = "deny"
unimplemented = "deny"
dbg_macro = "deny"
mem_forget = "deny"
redundant_pub_crate = "deny"
too_many_lines = "deny"
cast_possible_truncation = "deny"
cast_sign_loss = "deny"
