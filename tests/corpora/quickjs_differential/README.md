# QuickJS Differential Corpus

This corpus is for tests that compare `rs-quickjs` behavior with the original QuickJS command line runtime.

Set `RSQJS_QUICKJS=/path/to/qjs` to enable reference execution. When the variable is absent, rows are reported as skipped with a concrete reason.
