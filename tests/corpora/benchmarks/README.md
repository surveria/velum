# Benchmark Corpus

This corpus contains small benchmark scripts used by `rsqjs-test-runner`.

Benchmark cases are executed sequentially. Local engine measurements always run. QuickJS measurements run only when `RSQJS_QUICKJS=/path/to/qjs` is configured.
