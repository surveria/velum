# Project Development Plan

This document is the canonical product roadmap and execution plan for growing
`rs-quickjs` into a safe-Rust, embeddable JavaScript engine. It describes what
we are building, the rough order of work, and the protocol every branch should
follow.

The plan is intentionally operational. Each repository, embedding API,
compatibility, built-in, async, testing, resource-control, observability,
runtime-architecture, performance, or memory task should update this document
in the same branch that implements the task. Future work should resume from
repository state instead of relying on conversation history.

This is a whole-project development plan, not an optimization backlog. Read it
as the delivery order for the engine: keep the validation base reliable, keep
the Rust library API useful for embedders, expand language compatibility, add
practical built-ins, design modules and async jobs, expand resource control,
add production observability, and evolve runtime internals behind stable public
interfaces. Performance and memory budgets are acceptance criteria for all of
that work, with dedicated checkpoint tasks only when measurements show debt.

The focused [Architecture Stabilization And Development
Strategy](architecture-stabilization-plan.md) defines the architecture
prerequisites, development gates, and ordered stabilization program used by
cross-cutting runtime work. This document continues to decide whole-product
priority; the focused plan decides whether an affected product task first needs
object-model, semantic-operation, completion, ownership, execution-frame,
heap, or optimization-boundary work.

## How To Read This Plan

This document answers three different questions:

1. What are we building?
   A safe-Rust, embeddable JavaScript engine with many isolated VMs, typed host
   extensions, async integration, resource controls, observability, and
   QuickJS/Test262 validation.

2. What do we do next?
   Follow the default delivery order and the current delivery queue. Pick one
   small branch that moves product capability, compatibility, embedding support,
   reliability, resource control, observability, or measured performance
   forward, then update the task board with the evidence from that branch.

3. How do we execute the branch?
   Follow the branch protocol: refresh requirements, mark one task in progress,
   implement narrowly, validate, record compatibility and performance evidence,
   open a PR, wait for CI, squash-merge, update `main`, and remove the worktree.

4. How do runtime optimization ideas fit into the roadmap?
   Treat them as implementation methods. A branch starts from a product,
   compatibility, embedding, resource-control, observability, memory, or
   measured-performance need, then chooses atoms, slots, shapes, dense arrays,
   bytecode, caches, or heap work only when that method is the right tool for
   the selected need.

5. Which architecture prerequisites must be completed first?
   Follow the gates and stable task ids in the architecture stabilization plan.
   Update that plan with PR, merge, validation, compatibility, and measurement
   evidence as each item is completed.

Runtime architecture notes are not the project goal by themselves. Atoms,
slots, shapes, dense arrays, bytecode, inline caches, and GC are foundations
that we pull forward when they unblock compatibility, embedding API behavior,
resource accounting, observability, or measured performance and memory debt.

## Roadmap Layers

Read this plan in three layers:

1. Product delivery.
   The engine must become a safe Rust library for many isolated VMs, host
   extensions, async integration, resource controls, diagnostics,
   observability, Test262-visible compatibility, and practical built-ins.
   This layer decides what users and embedders can rely on.

2. Runtime foundations.
   Atoms, slot-based bindings, shapes, dense arrays, bytecode, inline caches,
   indexed heaps, and GC are the internal methods used to deliver the product
   layer without losing the QuickJS-like footprint and latency budget.

3. Evidence loop.
   Test reports, QuickJS differential results, benchmark ratios, memory
   reports, and direct embedding tests decide when a runtime foundation task
   should preempt ordinary compatibility or API work.

When these layers conflict, prefer a narrow branch that preserves product
direction and records the measured trade-off. Do not let the task board become
a queue of standalone runtime experiments.

## Plan Scope

This document intentionally covers the whole project, not only speed work:

- product surface: library API, VM isolation, host functions, async callbacks,
  resource limits, teardown, and observability
- compatibility: parser, runtime semantics, object model, functions, errors,
  built-ins, Test262 progress, and QuickJS differential behavior
- infrastructure: CI, one-command validation, corpus management, reports, and
  benchmark reliability
- runtime foundations: atoms, slots, shapes, arrays, bytecode, inline caches,
  indexed heaps, and collection strategy when they support the product path
- performance and memory: continuous budgets plus explicit checkpoint tasks
  when a report shows measured debt

When a task is mainly an internal architecture or performance task, it must
still explain which product capability, compatibility cluster, resource-control
need, observability feature, or measured budget exception it protects.

## Product Direction

`rs-quickjs` is a library-first JavaScript engine for Rust applications that
need many isolated virtual machines in one process. The CLI, test runner, and
scripts are support surfaces for validation and benchmarking.

The engine should keep the parts that make QuickJS attractive for embedded
Linux devices:

- small startup footprint
- predictable memory use
- fast interpreter performance without a JIT dependency
- explicit resource limits
- deterministic teardown
- behavior checked against QuickJS and Test262

It should also provide Rust-specific control that native QuickJS bindings
cannot provide as cleanly:

- safe Rust implementation by default
- isolated VMs with no mutable process-global JavaScript state
- typed host extensions
- asynchronous host callbacks through embedder-owned executors
- structured execution events, profiling hooks, and resource accounting
- memory and CPU limits that are visible at the library API boundary

## Targets

- Make the Rust library API the primary product surface before treating the CLI
  as more than a validation tool.
- Support many independent VM instances in one Rust process without mutable
  process-global JavaScript state.
- Provide typed host extensions and an async-host-callback path that can be
  driven by an embedder-owned executor.
- Keep implemented, comparable benchmark cases at or below `1.00x` QuickJS latency.
- Keep implemented, comparable memory measurements at or below `1.00x` QuickJS
  memory use when the report has a reliable reference measurement.
- Add project-specific engine tests for behavior that matters to the engine,
  even when Test262 does not cover it.
- Add Test262 coverage for ECMAScript semantics that are implemented locally.
- Add QuickJS differential coverage when QuickJS provides comparable behavior.
- Add benchmark coverage for hot paths and embedding-facing features.
- Keep the public API stable enough that bytecode internals can evolve without
  forcing ordinary embedder code to change.
- Keep the implementation safe by default. `unsafe` remains forbidden unless a
  separate design review proves that a measured bottleneck cannot be solved in
  safe Rust.

## Default Delivery Order

Use this order for ordinary branch selection when the latest report does not
show a stronger reason to do something else:

1. Preserve repository and measurement trust.
   CI, one-command validation, Test262 setup, QuickJS setup, compact reports,
   and benchmark reporting must keep working before feature work expands.

2. Keep the library surface usable for embedders.
   Direct Rust API coverage should stay ahead of CLI-only behavior for isolated
   VMs, resource limits, typed host functions, output separation, teardown, host
   errors, and reusable compiled scripts.

3. Expand compatibility by visible Test262 clusters.
   Parser, runtime semantics, object semantics, functions, errors, and built-ins
   should land in narrow branches that improve measured corpus progress.

4. Add practical built-ins by usage and report evidence.
   Prioritize `Object`, `Array`, `String`, `Number`, `Math`, `Boolean`,
   `Function`, standard errors, JSON, Date, RegExp, Map, and Set by
   missing-binding counts, feature-area failures, and embedding needs.

5. Improve diagnostics before errors become API debt.
   Syntax, runtime, host callback, and resource-limit errors should become
   stable enough for embedders to log, classify, and act on.

6. Keep host extension support close to real embedding use.
   Improve value conversion, VM-local callback ownership, examples, cancellation
   points, and direct library benchmarks before adding advanced async behavior.

7. Add modules, jobs, promises, and async host callbacks.
   These features should build on the synchronous embedding surface and keep the
   embedder in control of I/O policy and the outer executor.

8. Expand resource control and observability.
   Heap, stack, atom, job, module, host callback, cancellation, event, profiling,
   and teardown data should become first-class library behavior.

9. Change runtime data structures when they unblock product work or measured
   debt.
   Atoms, slots, shapes, dense arrays, indexed heaps, bytecode, inline caches,
   and GC are architecture work in service of compatibility, embedding,
   observability, resource control, performance, and memory footprint.

10. Run performance and memory checkpoint tasks when evidence calls for them.
    Checkpoints can preempt the order only when measurements show a regression
    or budget exception that blocks the next feature tranche, embedding API
    promise, or device-footprint target.

## Current Delivery Queue

This is the rough product order for the next branches. It is intentionally not
an optimization queue. Runtime architecture work appears here only when it
protects compatibility, embedding behavior, resource accounting, observability,
or measured performance and memory budgets.

Choose branches from this queue before reading the task board as a priority
list. The board records history and known work; the queue below decides the
default direction.

1. Keep report triage current.
   Before choosing each branch, summarize the newest Test262, QuickJS
   differential, benchmark, and memory signals. The latest report is the input
   to the next task, not a side artifact.

2. Keep compatibility moving in visible clusters.
   Prioritize syntax, functions, lexical environments, `this`, exceptions,
   equality, prototype behavior, iteration, and coercion when they unlock
   visible Test262 areas, practical built-ins, or embedding API behavior.

3. Continue object, function, and error semantics.
   The current product need is still the object/function property model:
   descriptors, own-property queries, prototype behavior, function metadata,
   standard errors, and operations needed by `Object`, `Array`, `Function`,
   errors, JSON, Date, RegExp, Map, and Set. These branches should improve
   Test262-visible behavior and add QuickJS differential coverage where
   reference behavior exists.

4. Grow arrays and practical built-ins.
   Add high-value Array methods, function callability semantics, standard error
   objects, and remaining practical built-ins in narrow clusters. Pull dense
   array storage or faster call paths forward only when correctness, resource
   accounting, or measured hot paths justify it.

5. Tighten the embedding API and documentation.
   Keep examples, crate docs, direct API tests, typed host functions,
   multi-VM isolation, resource failures, teardown, compiled-script reuse, and
   output behavior aligned with the actual public API.

6. Improve diagnostics and error classification.
   Stabilize syntax, runtime, host callback, and resource-limit errors before
   many more API surfaces depend on ad-hoc messages.

7. Design modules, jobs, promises, and async callbacks.
   The VM should own JavaScript jobs. Embedders should own I/O policy, module
   loading policy, cancellation, job draining, and the outer executor.

8. Expand resource control and observability.
   Make heap, stack, atom, job, module, host callback, wall-clock cancellation,
   structured events, profiling, and teardown data visible at the library API.

9. Profile runtime hot paths when architecture work is being selected.
   Recent benchmark reports show low compile times on many heavy rows while
   compiled evaluation remains much larger. Treat arrays, property lookup,
   prototype traversal, built-in calls, descriptor paths, and binding lookup as
   measured runtime debt, but capture profiles as evidence for a concrete
   product or compatibility branch rather than as the roadmap by itself.

10. Pull runtime data-model foundations forward when they are needed.
    Compiler-assigned slots, property-key atoms, shapes, dense arrays, indexed
    heaps, bytecode, inline caches, and GC are foundation work for the product
    queue above, not isolated speed experiments. The likely order is complete
    atomization, compiler-assigned slots and upvalues, shape-based object
    layouts, dense array storage, bytecode dispatch, inline caches, and compact
    VM-owned heaps.

11. Add bytecode and heap collection after enough semantics exist.
    Bytecode should stay behind the `CompiledScript` API. Heap accounting and a
    safe collection strategy should preserve host callbacks, queued jobs,
    deterministic teardown, hard limits, and many isolated VMs.

12. Run performance and memory checkpoints continuously.
    Checkpoint branches are allowed when reports show a budget exception, but
    they should name the affected product path and leave compatibility coverage
    intact.

## Product Capability Backlog

Use this backlog to translate the product direction into branch-sized work.
Architecture methods are listed only as likely implementation tools.

| Product area | Next capabilities | Likely architecture support | Required evidence |
| --- | --- | --- | --- |
| Embedding API | Many isolated VMs, direct API examples, resource-limit failures, teardown reports, typed host functions, host callback errors, compiled-script reuse. | Stable `Engine`/`Vm`/`CompiledScript` API, VM-local registries, immutable shared metadata, explicit resource counters. | Direct library tests, embedding examples, QuickJS differential checks only where behavior is comparable, benchmark rows for API hot paths. |
| Core compatibility | Syntax, lexical scopes, functions, `this`, equality, exceptions, prototype behavior, iteration, coercion, and standard error behavior. | Slot-based bindings, better diagnostics, shape/prototype correctness, bytecode-ready operands where runtime lookup becomes blocking debt. | Test262 progress by feature area, project fixtures, QuickJS differential cases, compact failure summaries. |
| Objects and functions | Descriptors, own-property queries, prototype traversal, callability, function metadata, constructors, and method `this` binding. | Atomized property keys, shapes, descriptor-aware shape transitions, prototype guards, per-site caches. | Engine object/function fixtures, QuickJS differential object cases, benchmark rows for property/prototype/call paths. |
| Arrays and built-ins | High-value `Array`, `Object`, `String`, `Number`, `Math`, `Boolean`, `Function`, errors, JSON, Date, RegExp, Map, and Set coverage. | Packed/holey/sparse storage, intrinsic metadata, direct native-call paths, bytecode/native loops, atomized built-in names. | Test262 built-in clusters, QuickJS differential cases, sequential benchmarks for hot built-ins. |
| Diagnostics | Stable syntax, runtime, host, and resource-limit error classes with source and context where available. | Error enums, contextual `Result` chains, structured report fields, atom/string heap boundaries for diagnostic text. | Direct error tests, report snapshots, embedding-facing API tests. |
| Modules and async | Module loading policy, job queue, promises, async functions, and async Rust host callbacks through embedder-owned executors. | VM-owned job queue, immutable module records, callback handles, cancellation/resource accounting. | Direct API tests, event/job reports, future differential coverage where comparable. |
| Resource control | Heap, stack, atom, job, module, host callback, runtime-step, source-size, and wall-clock limits. | Indexed VM heaps, atom budgets, stack/job accounting, safe collection strategy, explicit cancellation points. | Limit tests, teardown/resource reports, memory benchmark rows. |
| Observability | Structured events, profiling hooks, resource snapshots, callback/module/job accounting, and feature gates for constrained devices. | Stable event model, VM-local counters, profiling spans, compact report fields. | Direct observability tests, tracked reports, benchmark metadata. |
| Runtime foundations | Atoms, slots, shapes, dense arrays, bytecode, inline caches, heap strings, indexed heaps, GC, and parallel VM execution. | These are the methods themselves; select them only when they support one of the product areas above or a measured budget exception. | Latest report signal, baseline measurement, targeted regression tests, benchmark or memory delta when applicable. |

## Workstreams

### Repository And Validation

Repository work keeps the project reliable enough to measure progress. CI,
`scripts/test-all.sh`, Test262, the QuickJS reference setup, benchmark
orchestration, report generation, and documentation links are product
infrastructure, not side work.

Every branch should preserve one-command validation and compact reports. A
feature that cannot be measured, compared, or reproduced should be treated as
incomplete.

### Compatibility

Compatibility work grows the supported JavaScript language and built-in
surface. It should be driven by focused engine fixtures, QuickJS differential
cases, and Test262 progress reports.

Near-term compatibility should prioritize features that unlock many Test262
cases without forcing premature architectural commitments: parser and lexer
gaps, object and array semantics, basic built-ins, function semantics, and
standard error behavior.

### Embedding API

Embedding work defines the product surface. The public model should evolve
around `Engine`, `Vm`, `Context`, `CompiledScript`, and host function
registration.

Every embedding-facing feature must describe VM isolation, resource ownership,
teardown behavior, host callback behavior, queued jobs, and error propagation.
CLI-only proof is not enough for these features.

### Testing And Reporting

Testing work keeps one command as the default validation path while preserving
detailed reports:

- project-specific engine fixtures under `tests/`
- active Test262 subset for CI gates
- full Test262 progress reports
- QuickJS differential corpus
- benchmark corpus with rs-quickjs and QuickJS columns
- tracked Markdown reports under `reports/test-runs/`

Reports should stay compact: summaries and failure classifications are more
important than listing every passing or intentionally skipped case.

### Modules, Jobs, And Async

Modules, promises, the JavaScript job queue, async functions, and async Rust
host callbacks are product features for embedders. The VM owns JavaScript jobs,
while embedding applications own I/O policy, module loading policy, executor
choice, cancellation, and job-draining policy.

This work should happen after the synchronous embedding surface and enough core
runtime semantics exist to make async behavior meaningful.

### Runtime Architecture

Runtime architecture work changes the internal model behind the public API.
These tasks should be introduced when they unlock compatibility, resource
control, observability, or measured performance and memory debt. They are not a
separate product direction.

Recent benchmark reviews point to runtime execution rather than parsing as the
main measured debt. Many heavy rows now spend only a few microseconds in
compile, while compiled evaluation stays in the hundreds of microseconds. The
hot areas are arrays, descriptor-heavy objects, prototype traversal, built-in
constructor/prototype calls, property lookup, `in`, `for...in`, and binding
lookup.

The major implementation directions are:

- `CompiledScript` before bytecode
- atom ids for identifiers and property keys
- slot-based locals and upvalues
- shape-based object layouts
- inline property caches after shapes exist
- dense array fast paths
- VM-owned indexed heaps instead of scattered small allocations
- explicit heap accounting and a safe collection strategy

Current architecture status:

| Direction | Status | Next step |
| --- | --- | --- |
| `CompiledScript` | Runtime bytecode foundation complete. The public API stores bytecode-owned execution metadata: `BytecodeProgram`, top-level `var` hoist plan, `BytecodeFunction` bodies, bytecode-owned function signatures/default blocks, AST-free binding metadata, and usage counters. `Context::eval` compiles through `CompiledScript`, user functions execute bytecode bodies, and the old AST interpreter modules and `EvalAst*` bytecode instruction family are gone. The parser AST remains a front-end input to `binding_layout` and the dedicated compiler module, not a runtime fallback path. `binding_layout` now only builds `binding_metadata::BindingLayout`; runtime and bytecode layers consume `binding_metadata` instead of the front-end analyzer. `CompiledScript::compile` is the only bridge into the lexer/parser/binding-layout/compiler pipeline, the `bytecode` module is an AST-free executable metadata layer, and guard coverage rejects parser AST imports, front-end pipeline imports, and AST-owned execution/storage markers outside front-end/compiler layers. Shared operator, declaration, and binding metadata now lives outside the AST module so VM paths do not depend on front-end AST definitions. | Add compiler-assigned direct operands, quickening, modules, async jobs, host/async integration, and stronger resource accounting behind the bytecode-first API. A later direct parser-to-frontend-IR rewrite can remove the parser AST itself, but it must be treated as a front-end redesign rather than AST fallback cleanup. Break external APIs, runner code, or tests whenever that produces a cleaner architecture. |
| Frontend IR / parser AST removal | Not started. Runtime execution is already bytecode-only, but the parser still builds a compile-time AST that `binding_layout` and `compiler` traverse. `binding_layout` immediately lowers scope facts into AST-free `binding_metadata`, and this AST is not retained by `CompiledScript`, function objects, bytecode, runtime layers, or binding metadata. | Introduce a compact frontend IR that is closer to bytecode operands: explicit statement/expression ids, function ids, binding declarations, property/call site ids, literal pools, and source spans for diagnostics. Migrate `binding_layout` and `compiler` to consume that IR, then make the parser emit the IR directly and delete `src/ast` only after existing Test262 and embedding coverage stays stable. |
| Slot-based bindings | Partial. `BindingScope` stores cells in a `Vec` behind an atom-to-slot map, the AST separates parser-interned name text from per-occurrence `StaticBinding` operands, compiled scripts build a scope-aware binding layout with `GlobalSlot`, `LocalSlot`, and `UpvalueSlot` metadata, compiled function parameter scopes can be populated at compiled `LocalSlot` offsets, compiled lexical declarations, lexical `for...in` bindings, catch parameter scopes, and function-local hoisted `var` declarations can best-effort populate compiled `LocalSlot` offsets, user globals are separated from lazy builtin globals, top-level declarations can best-effort populate compiled `GlobalSlot` offsets, compiled global layout now assigns hoisted `var` slots before lexical globals to match the runtime hoist phase, first global cache misses can build guarded locations directly from `GlobalSlot`, runtime local frames can carry a compiled `ScopeId` after slot-compatible insertion, first local cache misses can build guarded locations directly from `ScopeId + LocalSlot`, function objects can carry compact `UpvalueSlot -> BindingCell` frames, first upvalue cache misses can read directly from the active upvalue frame, compiled function AST nodes carry `StaticFunctionId` metadata, compiled layouts preserve function parent metadata, transitive upvalues are lifted through intermediate closures, simple and nested compiled closures with complete lifted upvalue frames can skip legacy captured scope snapshots, exact layout-backed local/upvalue cache entries no longer carry `AtomId`, proven top-level global cache hits can use slot-only `ExactGlobal` locations after runtime slot/atom validation, active declaration cache seeding can use compiled slots before falling back to runtime location search, compound/update writes reuse the already resolved binding cell, and existing assignment, compound assignment, delete, and constructor lookup paths try the static binding cell before falling back to lazy builtin materialization. Layout-backed local/upvalue/exact-global cache hits now run before static-name atom lookup and can skip ordinary name and shadowing scans when their frame guards prove they are still valid. Builtin cache hits and non-proven global hits remain guarded because Annex B behavior and dynamic runtime scopes can still expose shadowing cases. | Add true bytecode-ready direct binding operands for the remaining generic bytecode read/write paths, then reduce fallback name lookup only where runtime and compiled frame alignment is proven. Keep compact closure frames observable through resource counters while direct bytecode operands are introduced. |
| Atom interner | Partial. VM-local atoms cover lexical/global binding names and the atom table now uses a sorted vector index instead of a tree map; parser-known static names are typed and deduplicated per parsed script so later property and binding operands can attach atom/slot metadata without changing parser surfaces again. Static object literal creation uses borrowed parser-known property names while storing and resolving ordinary object properties through atom-backed `PropertyKey` values. Computed property evaluation now produces a runtime dynamic property key that carries the string name plus an already known `PropertyKey` when available, so read/delete/`in` paths can avoid rechecking the atom table and read-modify-write paths can reuse the same key after insertion. Side-effect-free computed literal keys such as `obj["name"]`, `obj[0]`, `obj[true]`, `obj[null]`, and `obj[undefined]` are now parsed as static property operands instead of runtime dynamic string keys. Function and native-function static and dynamic property paths can now accept `PropertyLookup`/`PropertyKey` values instead of rebuilding string-only lookups at the final dispatch boundary, function metadata dispatch now classifies `length`, `name`, and `prototype` once into a typed `FunctionPropertyKind`, and default `length`/`name`/`prototype` descriptor values are pre-resolved into `FunctionProperties` when functions are created. Descriptor and accessor names (`value`, `writable`, `enumerable`, `configurable`, `get`, `set`) are now well-known property keys, and descriptor result objects cache their four fixed keys per context before materialization. | Extend atoms to the remaining built-in names, non-literal dynamic keys, diagnostics, bytecode property operands, and broader intrinsic metadata so hot paths do not fall back to owned `String`. |
| Shapes / hidden classes | Partial foundation. Ordinary object and function properties are atom-keyed, slot-backed, vector-indexed, ordinary object named-property lookups resolve slots through VM-owned `ShapeId` metadata, shapes include data-property descriptor attributes, cacheable named-property lookup snapshots exist for object/prototype reads and `in`, compiled static object-property reads, side-effect-free computed literal property reads, static own-property writes, descriptor-aware static/computed own-property deletes, and `in` presence checks can reuse guarded per-occurrence snapshots, and a VM-local prototype lookup version guards structural chain changes. | Extend guarded site caches toward broader descriptor-aware mutations, native dispatch, and dense-array loops while keeping array/prototype/fallback semantics explicit. |
| Inline caches | Partial. Compiled static member reads, side-effect-free computed literal member reads, non-literal computed member reads, method-call references, static and computed own-property writes, computed compound/update writes, lexical `for...in` computed assignment targets, static/computed own-property deletes, and `in` presence checks now have per-occurrence cache slots instead of sharing lookup snapshots by property name. Static bytecode property instructions now carry `BytecodeProperty` operands that bind the static property name to its access-site cache id, computed/dynamic bytecode property instructions now carry `BytecodeDynamicProperty` operands that bind dynamic property evaluation to the same checked cache-site model instead of passing loose `StaticPropertyAccessId` values, and arbitrary `CallValue` bytecode instructions now carry `BytecodeCallSite` operands backed by `StaticCallSiteId`. Cached snapshots are still monomorphic and guarded by receiver identity, receiver shape, prototype lookup version, descriptor attributes, and the current `PropertyKey`; dynamic computed object caches bypass arrays so numeric index semantics stay on the array storage path. Writes only update existing own named data slots and fall back for missing, prototype, array-specific, `__proto__`, function/native, stale, or different-key paths. Delete fast paths handle ordinary own named object properties when the guarded descriptor/configurable state proves the result; arrays, `__proto__`, function/native objects, prototype-owned hits, stale guards, and uncacheable keys still fall back to generic delete. Guarded static and computed member native calls now reuse the property access-site id to cache the validated `NativeFunctionId -> NativeFunctionKind` dispatch target, and static object-member native calls can also cache the cacheable object-property hit plus slot version so own-property built-ins such as `Math.abs` avoid repeated ordinary lookup until the receiver shape, prototype relationship, or property slot changes. VM resource counters expose native-call cache hits, misses, and slow paths. `CallValue` sites cache the last validated JS function id, native function id plus kind, or host function id and expose separate hit/miss/slow-path counters for embedding resource reports. | Extend per-occurrence cache slots to polymorphic function dispatch, descriptor-aware writes beyond current own-slot updates, dense-array loop sites, and method-specific call-loop specialization. Keep shape, receiver, descriptor, property-key, native-function, host-function, and prototype guards explicit with generic fallback for stale or uncacheable paths. |
| Dense arrays | Partial foundation. Arrays now use a dedicated `ArrayStorage` with packed and holey dense elements plus sparse index keys. Guarded packed fast paths exist for scan/copy-style reads, concat, join, slice, default-attribute reverse, and default-attribute shift/unshift through deque-backed front operations. Packed array construction can append complete default-attribute element vectors directly, and packed concat can build the result through one bulk storage path instead of generic per-index mutation. Guarded holey dense fast paths now exist for `includes`, `indexOf`, `lastIndexOf`, `join`, `slice`, and `concat` when the prototype chain has no indexed own properties in the relevant range. Bytecode direct-native array targets now take a guarded array-specific dispatch lane after validating the current VM-local `NativeFunctionId`, static `length` member reads compile to `ArrayLength`, static numeric index reads/writes compile to `ArrayIndexMember`/`ArrayIndexAssign`, and dynamic computed array index reads/writes first try a direct array-storage read or dense write before materializing a generic dynamic property key. Non-array receivers, sparse index writes, strings, functions, and plain objects still fall back to the ordinary static or dynamic property paths. Overridden or shadowed methods still fall back to ordinary calls. Slice-style packed fast paths still require contiguous packed storage and fall back when front operations wrap the deque. | Reduce the remaining array-method latency debt with broader prototype-index guards, descriptor-aware mutation paths, direct bytecode/native loops, compact holey/sparse materialization, fewer cloned `Value`/property transfers, and explicit materialization points. Preserve fallback for holes, sparse indices, descriptors, and prototype index properties. |
| Built-in intrinsics | Partial. Constructors, prototypes, and mutable builtin bindings are lazily materialized per VM, while unresolved immutable numeric globals (`NaN` and `Infinity`) can use guarded direct loads and `typeof` results when no local or user-global shadowing exists, and unresolved callable builtin globals can bypass builtin `BindingCell` reads in `CallBinding` when the same shadow guards prove no user binding is visible. Function and native-function `length`, `name`, and `prototype` paths now use typed property-kind dispatch before descriptor handling, immutable default `length`, `name`, and `prototype` descriptors are stored on `FunctionProperties`, native function ids are tracked through a VM-local checked slot registry keyed by `NativeFunctionKind`, and bytecode static member, computed-literal member, and binding calls can carry a `NativeCallTarget` expectation that is guarded against the current `NativeFunctionId` before direct dispatch. Static and computed member native calls now cache validated native-function kinds per property access site after ordinary property lookup, and static object-member native calls can validate the cached own-property slot version before direct native dispatch, so mutation of `Math.abs`, `Array.prototype.push`, or other exposed JS properties still falls back through the observable call path. Direct native dispatch is isolated in `runtime/native/direct.rs`; guarded direct member calls dispatch through the already validated `NativeCallTarget` instead of re-entering the broad `NativeFunctionKind` match, while generic native-value calls keep the full kind dispatch. Array native targets use a compact array-specific direct dispatch lane, `eval`, `Symbol`, `Function.prototype.bind`, `Function.prototype.call`, `Object.getOwnPropertyNames`, and `RegExp.prototype.test` now have guarded direct targets backed by native-id validation, direct native member calls borrow the receiver on the guarded native path and clone it only on generic fallback, `RuntimeCallArgs` can expose borrowed slices, fixed-arity array helpers, Math helpers, primitive constructors, and Object static helpers read arguments by reference where they only inspect values, Math numeric helpers return `Value::Number` directly without an empty `checked_value` pass, and unary/binary/variadic native argument helpers avoid cloning full argument vectors where the callee only reads values. | Separate the remaining immutable intrinsic metadata from mutable JS objects, pre-resolve atoms/shapes for built-ins, reuse descriptor layouts, and extend direct native-call paths beyond guarded call dispatch into method-specific bytecode/native loops and owned-argument boundaries only where mutation/storage requires them. |
| Bytecode quickening | Partial foundation. Generic bytecode execution now exists behind `CompiledScript`, with direct opcodes for common literals, bindings, declarations, assignment, property access, object/array literals, logical and non-logical binary operations, unary operators, conditionals, structured blocks, loops, `for...in`, `switch`, `try/catch/finally`, calls, constructors, and bytecode-backed function bodies. Binding-related bytecode instructions now carry `BytecodeBinding` operands with the original `StaticBinding` plus the compiler-assigned `BindingOperand`; unresolved `NaN`/`Infinity` bytecode loads and `typeof` checks can bypass builtin binding materialization under local/user-global shadow guards; unresolved callable builtin `CallBinding` paths can dispatch through VM-owned builtin metadata without reading the builtin binding cell when shadow guards prove no user binding is visible; static property instructions carry `BytecodeProperty`; computed/dynamic property instructions carry `BytecodeDynamicProperty`; arbitrary value calls carry `BytecodeCallSite`; static member, computed-literal member, and binding calls can carry guarded `NativeCallTarget` operands; array native targets take an array-specific fast lane; `eval`, `Symbol`, `Function.prototype.bind`, `Function.prototype.call`, `Object.getOwnPropertyNames`, and `RegExp.prototype.test` participate in the guarded direct-native target model; arithmetic `+`, `-`, `*`, `/`, `%`, and `**` compile to a numeric-specialized opcode with a generic bytecode path, preserving string-concatenation semantics for `+`; static `length` member reads compile to `ArrayLength` with a non-array generic path; static numeric index reads/writes compile to array-index-specialized bytecode with generic property semantics when guards fail; dynamic computed array index reads/writes now quicken `String`, heap-string, and numeric keys into guarded array-storage access before falling back to dynamic `PropertyKey` materialization; and bytecode binding calls, value calls, static member calls, computed member calls, print calls, and binding constructors can pass borrowed stack-tail argument slices to runtime call dispatch instead of first splitting arguments into a new `Vec`. Computed member calls clone only the receiver and property-key value from the slots before the argument tail, then drop the borrowed tail and pre-tail values after dispatch. `CompiledScriptUsage` exposes counts for binding operands, property operands, direct native calls, array native calls, numeric instructions, and static call sites, including computed-literal direct-native calls. | Add mutable quickening state where needed, dense-array loop instructions, broader native argument fast paths, and broader safe quickening from generic operations to specialized number, property, binding-slot, array, and native-call instructions with explicit generic bytecode paths for unproven cases. |
| Memory layout | Partial indexed handles for objects, functions, native functions, host functions, and string-wrapper object payloads. A VM-owned `StringHeap` now provides `StringId` and heap-backed `JsString` handles for selected runtime-generated strings, including script-local string literal constants, host-returned strings, string index-property results, context-aware string-wrapper descriptor values, context-aware error diagnostic property reads, context-owned binding/property/function-argument normalization, `typeof`, concatenation results, `String(...)`, native function names, JSON strings, `Object.keys` results, and `for...in` keys. Parser-known string and template literals are represented as `StaticString` constants in shared syntax metadata and only materialize into VM heap strings at evaluation/storage boundaries. Context-free property lookup now returns typed property results for borrowed diagnostic text and virtual string characters, leaving heap materialization to `Context` instead of creating temporary owned strings in object/property fallback code. `Value` still supports owned strings for public host conversion, parser compatibility at API boundaries, and remaining externalized values. | Extend `StringId` into the remaining externalized producers, then compact handles, Vec-backed heaps, free lists, boxed immutable constants, and captured-variable cells only where closures need them. |
| Parallel execution model | Partial at the product/API level. Independent VMs can exist, but no parallel execution contract is documented for compiled scripts, pools, or cleanup. | Parallelize independent VMs and compilation jobs, share immutable compiled scripts, and avoid trying to parallelize one arbitrary JavaScript context. |

### Performance And Memory Guardrails

Performance and memory guardrails keep implemented behavior close to QuickJS
while the engine grows into a complete embeddable library. They are continuous
acceptance criteria for feature work, not the only purpose of the roadmap.

Performance or memory branches should be checkpoint tasks with measured
baselines and a clear target. Ordinary compatibility and embedding branches
should still add benchmarks for hot paths and record measured exceptions when a
new feature exceeds the budget.

### Resource Control

Resource control work turns limits into a first-class API. Current limits cover
source length, statement count, expression depth, runtime steps, strings, and
global bindings. Future limits should cover heap budgets, atom table budgets,
stack budgets, queued jobs, module loading, host callback quotas, and wall-clock
cancellation hooks.

### Observability

Observability work adds profiling, structured runtime events, resource usage
snapshots, teardown reports, and feature gates for constrained devices. This is
not an afterthought: the engine exists partly so embedders can inspect and
control JavaScript execution.

## Delivery Milestones

These milestones describe the intended product sequence. The task board below
is the operational view; each branch should move one board item forward while
keeping these milestones in mind.

### 1. Trustworthy Project Base

Keep the repository, CI, test runner, Test262 mirror, QuickJS reference setup,
benchmark harness, and tracked reports reliable. This milestone is never fully
finished: every later branch must preserve one-command validation and compact
progress reporting.

### 2. Library-First Execution Shell

Make the Rust library the primary interface. The engine needs explicit
`Engine`, isolated `Vm`, execution `Context`, configuration, resource usage,
and teardown reporting before the CLI can be treated as anything more than a
smoke-test surface.

### 3. Host Extension Surface

Add typed Rust host functions, contextual callback errors, VM-local callback
storage, output separation, and eventually async host callbacks. This must
remain compatible with many VMs in one process and embedder-owned executors.

### 4. Core Language Compatibility

Grow syntax, statements, expressions, lexical scopes, functions, objects,
arrays, prototypes, and error semantics in narrow Test262-driven clusters.
Each cluster should have engine tests and QuickJS differential coverage where
the reference behavior exists.

### 5. Practical Built-Ins

Expand `Object`, `Array`, `String`, `Number`, `Math`, `Boolean`, errors,
functions, and other high-value built-ins based on Test262 failure clusters and
embedding use cases. Built-ins should not hide slow storage decisions; add
benchmarks for hot methods as they land.

### 6. Diagnostics And Error Model

Make syntax, runtime, host callback, and resource-limit errors precise and
stable enough for embedders. Good diagnostics are part of the library contract,
not just developer convenience.

### 7. Reusable Compilation API

Keep `CompiledScript` as the parse-once/evaluate-many contract for embedders.
Its runtime representation must remain bytecode-owned; parser AST is only a
front-end compiler input, not an execution fallback.

### 8. Modules, Jobs, And Async

Design module loading around embedder-owned I/O and policy. Add the JavaScript
job queue before promises, async functions, and async Rust host callbacks. The
VM should own JavaScript jobs, while the embedding application owns the outer
executor and job-draining policy.

### 9. Resource Control

Turn limits into a complete library API: heap budgets, atom table budgets,
stack budgets, queued jobs, module loading, host callback quotas, and
wall-clock cancellation hooks. Every new limit must appear in tests and
teardown/resource reports.

### 10. Observability

Add structured execution events, profiling hooks, resource snapshots, teardown
reports, cancellation hooks, and feature gates for constrained devices. This is
part of the product surface, not only debug tooling.

### 11. Runtime Data Model

Add atoms, slot-based locals, shape-based objects, dense array storage, indexed
VM-owned heaps, and explicit resource accounting. These changes support both
compatibility and performance, but they should be introduced as product
architecture work rather than isolated micro-optimizations.

### 12. Bytecode And Dispatch

Add bytecode after enough language coverage exists to benchmark honestly.
Bytecode, inline caches, and compact dispatch should stay behind the
`CompiledScript` API so ordinary embedder code does not need to change.

### 13. Heap Management And Collection

Grow the indexed ownership model into deterministic heap accounting and a safe
collection strategy. Hard limits, teardown, queued jobs, host callbacks, and
many isolated VMs must remain part of the design.

## Status Legend

Use these status values in the task board:

- `Backlog`: the task is known but not started.
- `In progress`: exactly one task should normally have this status in a branch.
- `Done`: the task was implemented, validated, and documented in the branch.
- `Deferred`: the task was examined, but the branch intentionally leaves it for
  later with a written reason.

The `Done` column uses Markdown checkboxes. A completed task must keep a short
note about what changed, what was difficult, and what remains possible later.

## Task Board

The board is both history and backlog. It is not sorted by priority. Completed
rows record what a branch actually delivered; a completed tranche row does not
mean the whole workstream is complete. Use the current delivery queue first,
then choose one unchecked row that fits the latest report evidence.
Because recent history contains many runtime tranches, the top of the board can
look optimization-heavy. Do not infer roadmap priority from row order.

| Done | Status | Task | Workstream | Purpose | Current notes |
| --- | --- | --- | --- | --- | --- |
| [ ] | In progress | AS-05b2c3 transient and asynchronous owner limits | Embedding API / runtime architecture / resource control | Enforce collection, Promise/job, retained/transient root, execution-frame, association, and module policies without heap scans. | Draft PR #436 charges every remaining growth/release transition through the VM-local ledger, reconciles eighteen ledger-backed and all twenty-six policy categories, adds five integration tests, and extends six architecture mutations. Engine integration tests and guard self-tests pass; the full fast gate is pending. |
| [x] | Done | AS-05b2c2 durable synchronous owner limits | Embedding API / runtime architecture / resource control | Enforce the cross-store Binding, callable, ObjectProperty, and CacheEntry categories without heap scans. | PR #435 merged as `5729920`; correctness `29120888639` certified exact tree `f4388da04b798621e346ae300c0efe02661dcf80` at 36,659/36,659 expected Test262 and 95/95 QuickJS. Post-merge run `29121083477` published report `20260710T202058Z` in `e3f47ef` with 5/5 valid sentinels. Function-call latency moved from 145.41 to 155.18 ms and remains a trend check. |
| [x] | Done | AS-05b2c1 storage-limit policy and payload owners | Embedding API / runtime architecture / resource control | Define the public per-owner policy and enforce payload-bearing/top-level VM stores without hidden heap scans. | PR #434 merged as `db704bd`; required CI `29118568725` certified exact tree `61535178e7f5411842938e5cbcb6b1b3579a40bf` at 36,659/36,659 expected Test262 and 95/95 QuickJS. Post-merge run `29118808176` published canonical report `20260710T194046Z` in `213f350` with 5/5 valid sentinels. AS-05b2c2/c3 retain durable synchronous and async/root/frame/association enforcement. |
| [x] | Done | AS-05b2b logical VM retained payload bytes | Embedding API / runtime architecture / resource control | Add a deterministic byte dimension to the complete AS-05b2a owner map without pretending to report allocator RSS. | PR #433 merged as `1c3dee8`; required CI `29111664726` certified exact tree `73a737a6189ba5cdb4f80931c4b6ea40724208f4` at 36,659/36,659 expected Test262 and 95/95 QuickJS. Post-merge run `29116932748` published canonical report `20260710T190739Z` in `7ee0441` with 5/5 valid sentinels. Hard limits remain AS-05b2c1/c2/c3. |
| [x] | Done | AS-05b2a VM storage owner counts | Embedding API / runtime architecture / resource control | Replace the selected teardown counter subset with a checked map covering every current variable-size VM owner. | PR #432 merged as `afcbe6b`; required CI `29110640594` certified exact tree `45a63d62baafe68c5746633f5dffa0078f5cc504` at 36,659/36,659 expected Test262 and 95/95 QuickJS. Post-merge run `29110845591` published canonical report `20260710T172423Z` in `0132afa` with 5/5 valid sentinels. Logical payload bytes and hard growth limits remain explicit AS-05b2b/c follow-ups. |
| [x] | Done | AS-05a2d retained VM handles | Embedding API / runtime architecture / memory | Give embedders a durable object/function/value root without exposing or relabeling raw arena ids. | PR #431 merged as `30dfc0a` after required CI `29109122937` certified exact tree `0f94732ed87357042eb028fd47c6f9e39daa35cf` at 36,659/36,659 expected Test262 and 95/95 QuickJS. The non-cloneable `RetainedValue` carries opaque VM identity, private registry capability, checked slot generation, consuming release, Drop safety-net cleanup, and a retained-root category. Post-merge run `29109349272` published canonical report `20260710T165910Z` in `c7f3ba8` with 5/5 sentinels. |
| [x] | Done | VM-local monotonic `performance.now()` | Embedding API / built-ins / observability | Provide portable in-script phase markers without introducing process-global mutable clock state or making JavaScript timestamps the canonical benchmark timer. | Adds a per-context monotonic origin and last-observation clamp, a deterministic embedder clock seam across the `Engine`, `Vm`, and compatibility `Runtime` APIs, a lazy `performance` object with `now()`, focused independent-VM and regression-clamp coverage, and benchmark documentation that keeps canonical timing in the external Rust harness. Validation passed with `cargo test --test performance_now_smoke -- --nocapture`, `cargo test --test embedding_api_smoke --quiet`, `cargo clippy --all-targets --all-features -- -D warnings`, and `RSQJS_BASE_REF=origin/main ./scripts/check-fast.sh`; the full report and benchmark gate was intentionally not run for this draft task. |
| [x] | Done | Call funnel scope template tranche | Performance | Reduce per-call constant costs in the general function-call funnel, driven by perf profiles of the worst method-call benchmarks. | Builds the sorted atom-to-slot scope index once per function and shares it across call frames through a copy-on-write scope index (`ScopeIndexData` + `BindingScope::from_shared_template`), so per-call scope construction allocates only the exactly sized value-slot vector; the previous per-call three-vector build plus sort left the `method_this` profile entirely (~7% self time plus ~13% allocator traffic). Skips re-remembering parameter slot locations in the per-function binding cache after the first call. Consults the per-site atom cache before the well-known property string match so hot property names (`value`, `length`, `constructor`) skip the ten-arm string comparison after first access. Adds a median-of-5 file-based perf probe (`tests/perf_probe.rs`). Two measured-worse variants documented and excluded: pooled/single-slot bytecode operand state, and a hoisted cache-first property read that regressed compound-assignment and in-operator paths. End-to-end effect on amplified method-call workloads sits within the +-5% noise of the development machine; the canonical runner benchmark suite is the deciding measurement. Known remaining hotspots for follow-up: funnel self time (eight per-call stack push/pop pairs, seven Rc clone pairs), Value drop traffic, and non-loop bytecode dispatch. |
| [x] | Done | Dynamic array-index bytecode quickening tranche | Runtime architecture / performance | Route computed `array[index]` reads and dense writes through a guarded array-index path before dynamic property-key materialization. | Adds `ObjectHeap::dynamic_array_index_if_array` for runtime `String`, heap-string, and numeric computed keys that can be canonicalized as array indices only after the receiver is proven to be an array. `ComputedMember` and `ComputedPropertyAssign` now try this direct array-storage read or dense write path before allocating or resolving a generic dynamic `PropertyKey`; non-array receivers, primitive strings, plain objects, sparse array writes, functions, native functions, and non-index keys fall back to the existing dynamic property/cache path. Array bytecode helpers were moved into `runtime/bytecode/array.rs` so the bytecode dispatcher stays below the project file-size limit. Direct coverage in `bytecode_quickening_smoke.rs` verifies numeric computed reads, computed dense writes, string-key array reads, sparse-write fallback, plain-object numeric fallback, string-index fallback, repeated compiled evaluation, and stable atom counts. The root engine crate version was bumped to `0.2.18`; the runner crate was unchanged. Validation passed after rebasing onto `e7229f8` with `cargo fmt --all -- --check`, `cargo check --all-targets`, targeted bytecode quickening coverage, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --quiet`, file-size inspection, `git diff --check`, version-bump check, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T221054Z.md` keeps engine fixtures at 68/68, active Test262 at 67/67, full Test262 at 11145/102578 with no skipped cases, and QuickJS differential at 64/64. Benchmarks measured 63 rows with aggregate performance `0.81x` and 41 tracked latency exceptions; memory-budget ratios remain unavailable for this report mode. |
| [x] | Done | Static array-index bytecode quickening tranche | Runtime architecture / performance | Route static numeric property operands such as `array[0]` through dedicated array-index bytecode reads and writes with generic fallback semantics. | Adds `BytecodeArrayIndex` plus `ArrayIndexMember` and `ArrayIndexAssign` instructions for canonical static numeric property names produced by dot-equivalent computed literals. Array receivers take a direct `ObjectHeap` array-storage read or dense write path; non-array receivers, primitive strings, functions, native functions, plain objects, and sparse array writes fall back to the existing static property path so observable JavaScript semantics stay unchanged. The new array-facing `ObjectHeap` helpers live in `runtime/object/array/bytecode.rs` to keep the main array module below the project file-size limit. Direct coverage in `bytecode_quickening_smoke.rs` verifies array reads, dense writes, length extension, missing index reads, plain-object numeric property fallback, string index fallback, repeated compiled evaluation, and stable atom counts. The root engine crate version was bumped to `0.2.16`; the runner crate was unchanged. Validation passed with `cargo fmt --all`, `cargo check --all-targets`, targeted bytecode quickening coverage, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --quiet`, file-size inspection, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T214152Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10890/102578 with no skipped cases, and QuickJS differential at 64/64. Benchmarks measured 63 rows with aggregate performance `0.80x` and 42 tracked latency exceptions; memory-budget ratios remain unavailable for this report mode. |
| [x] | Done | Native call-site inline-cache tranche | Runtime architecture / performance / observability | Cache validated static and computed member native call kinds per property access site and expose hit/miss/slow-path counters to embedders. | Adds per-access-site native-call cache slots to `StaticNameAtomCacheHandle`, keyed by the current `NativeFunctionId` and cached `NativeFunctionKind` after ordinary property lookup has produced the callee. Static and computed member bytecode calls now route through `eval_direct_native_property_call`, so a warm call site can dispatch directly to the cached native kind while mutation or shadowing of the exposed JS property still falls back to the generic call path. `VmResourceUsage` now exposes `native_call_cache_hits`, `native_call_cache_misses`, and `native_call_cache_slow_paths`, which gives the embedding API and future reports a concrete signal for native-call quickening quality. Direct coverage in `native_call_site_cache_smoke.rs` verifies cached native calls, mutation fallback for `Math.abs` and `Array.prototype.push`, static and computed call sites, direct-native operand counts, and counter growth over repeated compiled execution. The root engine crate version was bumped to `0.2.15`; the runner crate was unchanged. Validation passed with `cargo fmt --all`, `cargo check --all-targets`, targeted native-call-site cache coverage, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --quiet`, file-size inspection, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T212153Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10890/102578 with no skipped cases, and QuickJS differential at 64/64. Benchmarks measured 63 rows with aggregate performance `0.79x` and 41 tracked latency exceptions; memory-budget ratios remain unavailable for this report mode. |
| [x] | Done | Numeric add and array length quickening tranche | Runtime architecture / performance | Extend bytecode quickening to the common numeric `+` path and static array `length` reads while preserving generic JavaScript fallback semantics. | Adds `BytecodeNumericBinaryOp::Add` so arithmetic `+` uses the existing number-specialized opcode when both operands are `Value::Number`, then falls back to the generic `add` path for string concatenation and other non-number cases. Adds `BytecodeInstruction::ArrayLength` for static `length` member reads; runtime returns direct VM-owned array length for array objects and falls back to `get_static_property_value` for plain objects, strings, functions, native functions, and other receivers. The bytecode member dispatcher was split further to stay under clippy line limits, and `ObjectHeap::array_length_value_if_array` centralizes the checked array-length value path. Direct coverage in `bytecode_quickening_smoke.rs` verifies numeric-add quickening, string-concatenation fallback, direct array length, and non-array `length` fallbacks over repeated compiled evaluation. The root engine crate version was bumped to `0.2.14`; the runner crate was unchanged. Validation passed with `cargo fmt --all`, `cargo check --all-targets`, targeted bytecode quickening/bytecode/dynamic-property smoke tests, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --quiet`, file-size inspection, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T210623Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10890/102578 with no skipped cases, and QuickJS differential at 64/64. Benchmarks measured 63 rows with aggregate performance `0.81x` and 41 tracked latency exceptions; memory-budget ratios remain unavailable for this report mode. |
| [x] | Done | Computed direct native-call operand tranche | Runtime architecture / performance | Let statically known computed member calls use the same guarded direct-native operand model as static member and binding calls. | Adds `native: Option<NativeCallTarget>` to `CallComputedMember`, derives it at compile time for computed property expressions whose key is statically known, counts it in `CompiledScriptUsage::bytecode_direct_native_call_count()` and `bytecode_array_native_call_count()`, and routes runtime computed calls through `eval_direct_native_call` with the existing `NativeFunctionId` guard and generic fallback. The runtime computed-call dispatch was split into `eval_bytecode_computed_member_call` to keep the invocation dispatcher below clippy line limits. Direct coverage in `static_computed_property_operand_smoke.rs` verifies `Math["max"]` and `values["push"]` use direct operands while later `Math["max"]` and `Array.prototype["push"]` mutation falls back to ordinary calls. The root engine crate version was bumped to `0.2.13`; the runner crate was unchanged. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, targeted static-computed/bytecode/native-call smoke tests, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --quiet`, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T204912Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10890/102578 with no skipped cases, and QuickJS differential at 64/64. Benchmarks measured 63 rows with aggregate performance `0.81x` and 42 tracked latency exceptions; memory-budget ratios remain unavailable for this report mode. |
| [x] | Done | Computed member and native borrowed argument tranche | Runtime architecture / performance | Close the remaining bytecode computed-member call argument allocation and remove obvious full-argument clones from read-only native helper paths. | Routes `CallComputedMember` through borrowed stack-tail argument slices, cloning only the property value and receiver from the two slots before the tail before dynamic key conversion and method dispatch. After the call returns, the VM drops the borrowed argument tail and the two pre-tail values explicitly, so no bytecode call instruction now needs `pop_many(*arg_count)`. `RuntimeCallArgs::unary_value` was removed; the shared native unary helper now returns a borrowed first argument, and Boolean, Number, String, and Object static helpers read `RuntimeCallArgs::as_slice()` instead of cloning the full argument vector. Object helpers still clone individual target or descriptor values when they must return or store owned values. Owned `Vec<Value>` remains for user function call scopes, host callbacks, Array construction/concat/push/unshift, and other mutation/storage boundaries. Direct coverage extends `bytecode_call_stack_args_smoke.rs` with computed member calls that verify `this` binding, argument side-effect order, output, direct-native operand usage, and repeated compiled evaluation. The root engine crate version was bumped to `0.2.12`; the runner crate was unchanged. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features -- -D warnings`, targeted bytecode/dynamic-property/primitive/Object smoke tests, `cargo test --quiet`, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T203450Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10890/102578 with no skipped cases, and QuickJS differential at 64/64. Benchmarks measured 63 rows with aggregate performance `0.80x` and 42 tracked latency exceptions; memory-budget ratios remain unavailable for this report mode. |
| [x] | Done | Borrowed bytecode call argument tranche | Runtime architecture / performance | Avoid allocating owned argument vectors on hot bytecode call paths where the callee only needs a borrowed view of already-evaluated stack values. | Adds checked `BytecodeStack::tail`, `value_before_tail`, and `drop_tail` helpers and routes bytecode binding calls, value calls, static member calls, `print`, and binding constructors through borrowed stack-tail argument slices. Generic computed member calls still use the existing owned `pop_many` fallback because the property key sits below the argument tail and avoiding clones there needs a deeper stack-layout change. `RuntimeCallArgs` now exposes `as_slice`; `print`, error construction, JSON parse/stringify, fixed-arity array helpers, and Math unary/binary/variadic helpers read borrowed arguments instead of cloning full vectors when they do not need ownership. Owned `Vec<Value>` remains for function call scopes, array construction, concat, push, unshift, object helpers, and other storage/mutation boundaries. Direct coverage in `bytecode_call_stack_args_smoke.rs` verifies direct binding native calls, cached binding native calls, static member native calls, binding constructors, `print`, extra argument side effects, output, direct-native operand usage, and repeated compiled evaluation. The root engine crate version was bumped to `0.2.11`; the runner crate was unchanged. Targeted validation passed with `cargo check --all-targets`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --test bytecode_call_stack_args_smoke --quiet`, native/array/Math/JSON smoke tests, `cargo test --quiet`, and file-size inspection. Full validation passed with `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T202039Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10890/102578 with no skipped cases, and QuickJS differential at 64/64. Benchmarks measured 63 rows with aggregate performance `0.81x` and 42 tracked latency exceptions; memory-budget ratios remain unavailable for this report mode. |
| [x] | Done | Descriptor-aware delete inline-cache tranche | Runtime architecture / performance | Add guarded bytecode delete fast paths for ordinary own object properties while preserving generic delete semantics for arrays, prototypes, functions, and stale cache sites. | Adds `CacheablePropertyDelete` and `ObjectHeap::delete_cacheable_own_property_for`, reusing the existing `CacheablePropertyLookup` guard of receiver id, receiver shape, prototype lookup version, property key, owner shape, slot, and descriptor attributes. Static bytecode deletes now pass the `BytecodeProperty` access-site id into `delete_static_property_value`; computed bytecode deletes use `BytecodeDynamicProperty` through `delete_cached_dynamic_property_value`. The fast path deletes configurable own named properties, returns cached `true` for guarded missing properties, returns cached `false` for guarded non-configurable properties, and leaves successful delete hits uncached because the operation changes shape. Arrays, `__proto__`, function/native objects, prototype-owned hits, stale guards, and uncacheable lookups fall back to generic delete. Direct coverage in `property_delete_inline_cache_smoke.rs` verifies static missing-cache invalidation after shape changes, non-configurable descriptor preservation, computed delete missing-cache invalidation, operand counts, and repeated compiled evaluation. The root engine crate version was bumped to `0.2.10`; the runner crate was unchanged. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features -- -D warnings`, targeted property/delete/descriptor smoke tests, `cargo test --quiet`, file-size inspection, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T200246Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10890/102578 with no skipped cases, and QuickJS differential at 64/64. Benchmarks measured 63 rows with aggregate performance `0.82x` and 40 tracked latency exceptions; memory-budget ratios remain unavailable for this report mode. |
| [x] | Done | Computed dynamic bytecode property operand tranche | Runtime architecture / performance | Replace raw computed property cache-site ids in bytecode with an explicit dynamic property operand so computed paths match the static property operand model. | Adds `BytecodeDynamicProperty`, a compact operand around `StaticPropertyAccessId`, and routes computed member reads, computed member calls, computed writes, compound writes, update operations, computed `for...in` assignment targets, computed deletes, and `in` property checks through that operand instead of passing bare ids. Runtime semantics and cache guards stay unchanged: existing dynamic object read/write/`in` caches still validate `PropertyKey`, receiver/shape/prototype facts, and array bypasses, while computed delete carries the operand for ABI consistency and still falls back to the generic semantic delete path. `bytecode_property_operand_count()` now includes dynamic operands, so dynamic-only compiled scripts expose operand coverage through `CompiledScriptUsage`. Direct coverage in `dynamic_property_key_cache_smoke.rs` verifies dynamic read/write/compound/update operands, computed calls, same-site different keys, array index semantics, computed delete, and `in` operands over repeated compiled evaluation. The root engine crate version was bumped to `0.2.9`; the runner crate was unchanged. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --quiet`, file-size inspection, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T194510Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10890/102578 with no skipped cases, and QuickJS differential at 64/64. Benchmarks measured 63 rows with aggregate performance `0.82x` and 42 tracked latency exceptions; memory-budget ratios remain unavailable for this report mode. Remaining quickening work now shifts to descriptor-aware ICs, delete caching where profitable, dense-array loop instructions, broader method-specific native argument fast paths, async/jobs, modules, and tighter resource accounting. |
| [x] | Done | Bytecode property/native/numeric/array operand tranche | Runtime architecture / performance | Extend the bytecode operand model beyond bindings so property access, native calls, numeric arithmetic, and guarded array-native dispatch have explicit specialized execution points. | Adds `BytecodeProperty`, which stores a static property name together with its `StaticPropertyAccessId`, and moves bytecode metric calculation into `bytecode/metrics.rs` so the type module stays below the project file-size limit. Static property reads, writes, deletes, compound/update operations, assignment targets, and static member calls now carry this operand instead of loose `StaticName + access` fields. Adds engine-level `NativeCallTarget` plus runtime mapping to `NativeFunctionKind`; binding calls and static member calls carry optional direct-native targets, validate them against the current VM-local native function registry, and fall back to ordinary calls when user code shadows or mutates the property. Array native targets now use an array-specific direct dispatch lane after the same guard, and `CompiledScriptUsage::bytecode_array_native_call_count()` exposes that coverage. Adds `NumberBinary` for arithmetic `-`, `*`, `/`, `%`, and `**`, with a direct `Value::Number` path and generic numeric fallback. Unary/binary native argument helpers avoid cloning the full argument vector, and bytecode call/literal argument popping uses one checked `split_off` instead of a pop/reverse loop. `CompiledScriptUsage` now exposes `bytecode_property_operand_count()`, `bytecode_direct_native_call_count()`, `bytecode_array_native_call_count()`, and `bytecode_numeric_instruction_count()`. Direct coverage in `bytecode_smoke.rs` verifies property operands, direct native calls through `Array`, `Array.prototype.push`, `Math.max`, `Math.abs`, array native operands, fallback through a user-defined `max`, numeric bytecode instructions, repeated compiled evaluation, and stable atom counts. The root engine crate version was bumped to `0.2.8`; the runner crate was unchanged. Validation passed with `cargo fmt --all`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --quiet`, file-size inspection, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T192856Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10890/102578 with no skipped cases, and QuickJS differential at 64/64. Benchmarks measured 63 rows with aggregate performance `0.82x` and 43 tracked latency exceptions; memory-budget ratios remain unavailable for this report mode. This is a real bytecode ABI tranche, but the broader active objective still needs computed/dynamic property operands, broader method-specific native argument quickening, broader descriptor-aware ICs, async/jobs, modules, and tighter resource accounting. |
| [x] | Done | Bytecode binding direct operand tranche | Runtime architecture / performance | Move binding-heavy bytecode instructions from runtime layout lookup toward compiler-assigned direct operands while preserving fallback semantics. | Adds `BytecodeBinding`, which stores the original `StaticBinding` for diagnostics/fallback plus the resolved `BindingOperand` from `BindingLayout`. `BytecodeProgram::compile` now receives the compiled binding layout, binding-related instructions and assignment targets carry `BytecodeBinding`, and runtime bytecode lookup/assignment/call/constructor paths first try the embedded local/upvalue operand before falling back to existing guarded static binding resolution and lazy builtin materialization. `CompiledScriptUsage::bytecode_binding_operand_count()` exposes how many direct binding operands are present in compiled bytecode. The bytecode compiler control-flow helpers moved to `bytecode/control.rs`, and expression compilation was split into smaller helpers so strict line and clippy limits stay clean. The root engine crate version was bumped to `0.2.7`; the runner crate was unchanged. Direct coverage in `bytecode_smoke.rs` and `binding_layout_smoke.rs` verifies global/local/upvalue binding operations, constructors, compound/update writes, shadowed locals, repeated compiled evaluation, and stable atom counts. Validation passed with `cargo fmt --all`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --quiet`, file-size inspection, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T184834Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10890/102578 with no skipped cases, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 41 tracked latency exceptions and aggregate performance `0.82x`; memory-budget ratios remain unavailable for this report mode. This is an architecture tranche, not completed quickening: property operands, native-call operands, numeric quickening, broader inline caches, async/jobs, modules, and tighter resource accounting remain follow-up work. |
| [x] | Done | Source module hierarchy cleanup | Repository / maintainability | Replace the flat `src/` module namespace with domain-owned module directories before the bytecode VM, runtime object model, and parser continue to grow. | Moves parser, lexer support, binding layout, bytecode compiler/types, runtime bytecode VM, runtime functions, native built-ins, and object/array storage files into `mod.rs`-based module trees such as `runtime/object/array/bulk.rs`. Public crate exports stay focused on `Context`, `Runtime`, and `RuntimeLimits`; internal imports now use the new module ownership boundaries instead of root-level `runtime_*`, `bytecode_*`, `binding_layout_*`, and `parser_*` file names. The root engine crate version was bumped to `0.2.6`; the runner crate was unchanged. Validation passed with `cargo fmt --all`, old-module-path search over `src` and `tests`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --quiet`, file-size inspection, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T182226Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10890/102578 with no skipped cases, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 39 tracked latency exceptions, aggregate performance `0.76x`, and no failed benchmark rows in the current in-process mode; memory-budget ratios remain unavailable for this report mode. Remaining cleanup can split the few files close to the 800-line limit, but no single-source file exceeds the project limit after this branch. |
| [x] | Done | Whole-project plan framing | Documentation / project planning | Reframe the project plan as a product and compatibility roadmap instead of a standalone optimization backlog. | Adds explicit roadmap layers, a product capability backlog, and a branch-selection rule that starts from embedding, compatibility, built-ins, diagnostics, async, resource control, observability, memory, or measured-performance needs. Runtime methods from the benchmark review remain documented as architecture tools, with current implementation status tracked separately so future work can choose them deliberately instead of treating them as the whole roadmap. Validation was limited to documentation review and `git diff --check` because this branch changes no code. |
| [x] | Done | Typed context-free property string results tranche | Runtime architecture / memory | Stop context-free property and object fallback code from constructing temporary owned `Value::String` values for diagnostic text and virtual string characters. | Adds `runtime_property::PropertyValue` and `runtime_object::ObjectPropertyValue`, so generic property reads can return ordinary values, borrowed diagnostic text, or virtual string characters without allocating an owned string. `Context::runtime_property_value` is now the single materialization boundary for those typed reads, routing text and characters through the VM `StringHeap`. `ObjectHeap::get` and prototype traversal now carry virtual string characters as typed results; `ObjectHeap::own_property_descriptor` no longer has a context-free string-wrapper descriptor value fallback because the context-aware `Object.getOwnPropertyDescriptor` path already materializes those descriptor values as heap strings. Direct coverage in `string_heap_smoke.rs` verifies inherited string-wrapper index reads through a prototype chain return heap-backed strings and do not grow heap usage on repeated reads. The root engine crate version was bumped to `0.2.2`; the runner crate was unchanged. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, `cargo test --test string_heap_smoke`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --quiet`, `git diff --check`, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T150245Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 34 latency exceptions and no memory-budget exceptions in the current in-process mode; aggregate performance is `0.68x`, memory comparison is unavailable in this benchmark mode. Remaining compact-value work includes public host conversion compatibility, other externalized string producers, handle compaction, Vec-backed heaps/free lists, boxed immutable constants, and eventual bytecode constants behind `CompiledScript`. |
| [x] | Done | Script-local string literal constants tranche | Runtime architecture / memory | Move parser-known string and template literals out of owned `Value::String` AST nodes and into script-local constants before broader compact-value work. | Adds `StaticStringId` and `StaticString`, a parser-owned `StaticStringTable`, and `CompiledScriptUsage::static_string_count()`. `TokenKind::String` now parses to `Expr::StringLiteral`, runtime evaluation materializes it directly through `Context::heap_string_value(&str)`, and side-effect-free computed keys such as `holder["camera"]` still normalize to static property operands without creating a runtime heap string for the property name. Direct coverage in `string_heap_smoke.rs` verifies repeated compiled string literals are counted as two script-local constants for `"front"` and `"camera"`, materialize only `"front"` into the VM string heap, and do not grow heap usage on repeated compiled evaluation. The root engine crate version was bumped to `0.2.1`; the runner crate was unchanged. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, targeted string heap tests, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --quiet`, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T144425Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 35 latency exceptions and no memory-budget exceptions in the current in-process mode; aggregate performance is `0.67x`, memory comparison is unavailable in this benchmark mode. Remaining compact-value work includes context-free diagnostic/property fallbacks, other externalized string producers, handle compaction, Vec-backed heaps/free lists, boxed immutable constants, and eventually replacing AST evaluation with bytecode constants behind `CompiledScript`. |
| [x] | Done | Computed property access-site cache tranche | Runtime architecture / performance | Extend checked per-occurrence property cache slots from static member reads and writes into non-literal computed property reads and writes without changing array index semantics. | `Expr::ComputedMember` and `Expr::ComputedPropertyAssignment` now carry `StaticPropertyAccessId`, so dynamic bracket reads, writes, compound assignments, updates, computed method calls, and computed `for...in` assignment targets can reuse the same guarded cache machinery as static property sites. Cacheable lookup snapshots now remember the requested `PropertyKey`, so one dynamic site such as `holder[key]` can safely see different keys across calls or loop iterations; computed object caches also bypass arrays to keep numeric indices on `ArrayStorage`. The branch removes the old string-only generic dynamic get/set helpers, routes dynamic property paths through `get_cached_dynamic_property_value` and `set_cached_dynamic_property_value`, and adds regression coverage for repeated compiled computed operations, method-call `this` binding, same-site different keys, and array index semantics. The root engine crate version was bumped to `0.1.1`; the runner crate was unchanged. Validation passed with `cargo fmt --all -- --check`, targeted dynamic/static/array/binding cache tests, `cargo check --all-targets`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --quiet`, `git diff --check`, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T141931Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 38 latency exceptions and no memory-budget exceptions in the in-process mode; aggregate performance is `0.69x`, memory comparison is unavailable in the current benchmark mode, and `computed_properties` is within latency budget at `0.24x`. Remaining cache work includes descriptor-sensitive writes, broader function/native dispatch, deletes where profitable, bytecode property operands, and moving new runtime logic out of `runtime.rs` before it exceeds the file-size limit. |
| [x] | Done | Cached binding before atom lookup tranche | Runtime architecture / performance | Move repeated proven binding hits closer to bytecode-ready direct slot operands by avoiding static-name atom lookup when a cached runtime location is already valid. | `Context::get_binding_static` now tries the per-occurrence static binding location cache before consulting the static-name atom cache. Exact compiled local, upvalue, and proven exact-global cache hits can therefore return a checked `BindingCell` through the saved `BindingLocation` first, while stale or missing cache entries still fall back to atom lookup, compiled operand resolution, ordinary scope search, and guarded builtin/global behavior. Validation passed with `cargo fmt --all`, `cargo check --all-targets`, targeted binding/block/static-name smoke tests, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test`, `git diff --check`, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T135425Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 36 latency exceptions; aggregate performance is `0.68x` under the in-process QuickJS methodology and memory comparison is unavailable for the current benchmark mode. Remaining slot work is a true bytecode binding operand representation and narrower fallback lookup for dynamic or Annex B-sensitive cases. |
| [x] | Done | Global var slot order tranche | Runtime architecture / performance | Align compiled global `GlobalSlot` assignment with the runtime `var` hoist phase before broader direct binding operands. | `BindingLayout::collect_scope_declarations` now collects hoisted `var` declarations for the current var-scope before direct declarations, so later block-contained `var` declarations get the same early slots that runtime hoisting inserts before top-level lexical globals. This reduces the source-order/runtime-order mismatch that forced exact global binding caches to fall back for otherwise proven top-level globals. Direct coverage in `binding_layout_smoke.rs` verifies a compiled script with early `let` globals and a later block-hoisted `var` preserves all global values and slot counts. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, targeted binding/block/static-name smoke tests, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test`, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T134253Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 47 latency and 58 memory exceptions; aggregate performance is `1.29x` and memory is `1.09x`. Remaining slot work is true bytecode-ready direct operands for read/write/eval paths, plus tighter handling for dynamic or Annex B shadowing cases where guarded fallback remains correct. |
| [x] | Done | Exact global binding cache tranche | Runtime architecture / performance | Split binding cache location metadata and safely introduce atom-free top-level global cache hits where runtime and compiled slots are proven aligned. | Adds `runtime_binding_location.rs` so binding cache locations are separated from the already large static-binding runtime module. `BindingLocation::ExactGlobal` stores only a checked `BindingSlot` and is selected only when no local scopes are active and the runtime global scope confirms that the compiled slot still contains the expected `AtomId`; otherwise the runtime keeps the guarded global location and generic resolver fallback. This avoided a hoisted-`var` slot-alignment regression where runtime insertion order could differ from compiled source-order global slots. Direct coverage in `binding_layout_smoke.rs` verifies repeated compiled top-level global reads/writes preserve function-parameter shadowing and do not grow atoms, while existing block-scope coverage protects lexical shadowing after hoisted globals. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, targeted `block_scope_smoke` and `binding_layout_smoke`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test`, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T133250Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 46 latency and 58 memory exceptions; aggregate performance is `1.29x` and memory is `1.09x`. Remaining slot work is true bytecode-ready direct operands for read/write/eval paths and a better global frame construction model that avoids source-order/runtime-order mismatch instead of falling back to guarded lookups. |
| [x] | Done | Native function registry tranche | Runtime architecture / performance / embedding API | Replace linear native-function kind rediscovery with a VM-local checked registry before broader intrinsic metadata and native-call bytecode operands. | Adds `runtime_native_registry.rs` with fixed checked slots for every `NativeFunctionKind`, including standard and Test262 error constructors. `Context` now owns a `NativeFunctionRegistry`, registers all constructors and lazily created native methods through `push_native_function_with_id`, and resolves `native_function_id` through the registry instead of scanning `native_functions`. Direct constructor paths no longer push native functions manually, which keeps future intrinsic materialization behind one checked insertion path. `VmResourceUsage` now reports `native_function_count`, and direct coverage in `native_function_registry_smoke.rs` verifies repeated built-in access reuses registered native functions without growing the native function store. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, targeted registry/resource/embedding smoke tests, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test`, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T131317Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 46 latency and 58 memory exceptions; aggregate performance is `1.28x` and memory is `1.07x`. Remaining intrinsic work still includes immutable metadata tables, pre-resolved built-in atoms and shapes, descriptor layout reuse, and direct native-call or bytecode operands for common built-ins. |
| [x] | Done | Direct string descriptor heap materialization tranche | Runtime architecture / memory | Avoid the remaining context-free owned-string fallback when `Object.getOwnPropertyDescriptor` materializes virtual string-wrapper index descriptors. | Adds `Context::heap_string_char_value` as the shared context-aware char-to-heap-string boundary and uses it for primitive string index reads, string-wrapper index reads, and `Object.getOwnPropertyDescriptor(new String(...), index)` descriptor values. `Object.getOwnPropertyDescriptor` now asks the object heap for the virtual string character and builds the `DataPropertyDescriptor` with a heap-backed value before descriptor object creation, instead of receiving a temporary owned `Value::String` from the context-free object fallback and normalizing it afterward. Focused heap-string coverage now retains the descriptor object itself, verifies the virtual character value is already heap-backed at descriptor materialization time, and confirms later `descriptor.value` reads do not grow string heap usage. Validation passed with `cargo fmt --all`, targeted `string_heap_smoke` descriptor coverage, `cargo check --all-targets`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test`, and `scripts/test-all.sh` using the local Test262 cache. Report `rsqjs-test-report-20260706T130038Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 48 latency and 58 memory exceptions; aggregate performance is `1.30x` and memory is `1.08x`. Remaining compact-value work still includes context-free diagnostic/property fallbacks outside this descriptor path, public host conversion compatibility, parser/public compatibility boundaries, other externalized producers, and an eventual handle-only `Value` layout. |
| [x] | Done | String descriptor heap fallback tranche | Runtime architecture / memory | Close a remaining string-wrapper descriptor path where virtual indexed character values could escape as owned `Value::String` instead of VM-owned heap strings. | `Context::create_property_descriptor_object` now normalizes descriptor `value` through `Context::runtime_value` before materializing the descriptor object, so `Object.getOwnPropertyDescriptor(new String("camera"), 0).value` reuses the VM string heap instead of storing an owned string copy. Focused coverage warms the descriptor path, verifies that the string wrapper payload and virtual character descriptor value are heap-backed, and confirms repeated descriptor reads reuse the same heap entries. Validation passed with `cargo fmt --all`, targeted `string_heap_smoke` coverage, `cargo check --all-targets`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test`, and `scripts/test-all.sh` using the local Test262 cache. Report `rsqjs-test-report-20260706T125009Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 48 latency and 59 memory exceptions; aggregate performance is `1.29x` and memory is `1.08x`. Remaining compact-value work still includes context-free diagnostic/property fallbacks, public host conversion compatibility, parser/public compatibility boundaries, other externalized producers, and an eventual handle-only `Value` layout. |
| [x] | Done | Packed array bulk storage tranche | Runtime architecture / performance / memory | Continue the Dense Arrays direction by avoiding generic per-index property mutation when a packed array can be built from a complete value vector. | `ObjectHeap::create_array` now constructs full packed arrays through a storage-level append API that checks the object property limit once and updates enumerable property accounting once. `Array.prototype.concat` now has a guarded packed-only bulk path that precomputes the result length, copies full packed receiver and argument arrays into a `Vec<Value>`, and creates the result array through the same packed bulk construction API; holey, sparse, descriptor-sensitive, prototype-index-sensitive, and non-array arguments keep the existing generic fallback. Focused coverage verifies that packed array literals and packed concat results still count elements against `max_object_properties` instead of bypassing resource limits. Validation passed with `cargo fmt --all`, targeted packed-array and packed-concat limit coverage, `array_prototype_concat_smoke`, `array_prototype_slice_smoke`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test`, file-size checks, and `scripts/test-all.sh` using the local Test262 cache. Report `rsqjs-test-report-20260706T124144Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 47 latency and 60 memory exceptions; aggregate performance is `1.29x` and memory is `1.10x`. `array_prototype_concat` moved from the previous report's `2.05x` latency ratio to `1.88x`, while `array_prototype_slice` moved from `1.87x` to `1.83x`; overall benchmark counts remain noisy and memory row count worsened, so the branch is a safe storage-boundary improvement rather than a completed array-performance win. The next array work should target storage-level method loops, prototype-index guards, fewer `Value`/property clones, and eventual bytecode/native loop dispatch. |
| [x] | Done | Context string normalization tranche | Runtime architecture / memory | Normalize owned string values at `Context` storage and return boundaries without making every expression dispatch pay for heap checks. | Adds `Context::runtime_value` as the central owned-string normalization helper and applies it where VM state is written or returned through mutable context paths: static/builtin binding assignment, declarations, catch bindings, function parameter scopes, function returns, host returns, static and dynamic property writes, and generic/static property fallback reads. A first attempt to wrap every `eval_expr` result was narrowed after the benchmark report showed avoidable dispatch overhead; the final branch keeps normalization on storage and return boundaries instead. Focused heap-string coverage verifies that strings retained through bindings, object properties, and function parameters return as `Value::HeapString` and reuse the same heap entry. Validation passed with `cargo fmt --all`, targeted context-owned string normalization coverage, full `string_heap_smoke`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test`, and `scripts/test-all.sh` using the local Test262 cache. Report `rsqjs-test-report-20260706T122435Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 45 latency and 54 memory exceptions; aggregate performance is `1.30x` and memory is `1.08x`. `string_builtin` remains latency debt at `1.65x` but is within memory budget at `0.98x`; `compiled_script_reuse` is `1.18x` latency and `0.99x` memory; `atomized_bindings` remains tracked debt at `1.19x` latency and `1.13x` memory. Remaining compact-value work includes public host conversion compatibility, parser literal compatibility, context-free property/diagnostic fallbacks, and the eventual handle-only `Value` layout. |
| [x] | Done | String wrapper heap-storage tranche | Runtime architecture / memory | Store `new String(...)` wrapper payloads as VM-owned heap strings and expose character indices as virtual object properties. | String wrapper objects now store their primitive payload as a VM-owned `JsString` instead of materializing every character as an ordinary named property during construction. Character indices are exposed as virtual own properties for reads, `in`, descriptors, deletion, and enumeration, while context-aware static, dynamic, and generic indexed reads still materialize through `Context::heap_string_value`. Focused heap-string coverage verifies construction interns only the wrapped string, indexed reads lazily intern the character once, repeated reads reuse it, deleting a virtual index is rejected, `for...in` still emits character indices, and wrappers with different string lengths reuse the same shape. Validation passed with `cargo fmt --all`, targeted string-wrapper heap coverage, `string_builtin_smoke`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test`, `git diff --check`, and `scripts/test-all.sh` using the local Test262 cache after an initial network fetch failed with `GnuTLS recv error`. Report `rsqjs-test-report-20260706T121052Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 47 latency and 60 memory exceptions; aggregate performance is `1.28x` and memory is `1.09x`. `string_builtin` improved from the previous report's `1.96x` latency to `1.76x`, but remains tracked debt with `1.12x` memory. Remaining heap-string follow-ups include context-free diagnostic fallbacks, other externalized string producers, and an eventual handle-only `Value` layout. |
| [x] | Done | Error property heap-string tranche | Runtime architecture / diagnostics / memory | Move context-aware error `name` and `message` property reads onto the VM-owned string heap. | `error_property_text` now exposes borrowed diagnostic text so context-aware static, dynamic, and generic `error.name`/`error.message` property reads can materialize through `Context::heap_string_value`; the older `error_property` helper remains as the context-free owned-string fallback. Focused heap-string coverage verifies repeated `ReferenceError.name` reads reuse one heap string, `message` reads intern the diagnostic text once, and dynamic `error[key]` reuses the diagnostic string while only adding the dynamic key literal. Validation passed with `cargo fmt --all`, targeted heap-string coverage, `cargo check --all-targets`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test`, `git diff --check`, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T115430Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 46 latency and 58 memory exceptions; aggregate performance is `1.28x` and memory is `1.08x`. `error_object_properties` remains latency-good but memory debt at `0.76x` latency and `1.10x` memory, while `standard_error_constructors` remains tracked debt at `1.03x` latency and `1.01x` memory. Remaining heap-string follow-ups include context-free diagnostic fallbacks, other externalized string producers, and an eventual handle-only `Value` layout. |
| [x] | Done | String index heap-string tranche | Runtime architecture / memory | Move primitive string index-property reads onto the VM-owned string heap. | String property lookup now separates `length`, indexed character, and missing results with a typed internal result. `Context` materializes indexed characters through `heap_string_value`, so static, dynamic, and generic reads such as `"front"[1]`, `"front"[i]`, and internal property reads reuse VM-owned heap strings instead of returning fresh owned `String` values. Focused heap-string coverage verifies repeated ASCII and escaped Unicode character reads keep stable `string_count` and `string_bytes`. Validation passed with `cargo fmt --all -- --check`, targeted heap-string coverage, `cargo check --all-targets`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test`, `git diff --check`, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T114324Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 41 latency and 56 memory exceptions; aggregate performance is `1.21x` and memory is `1.07x`. `string_concat` remains latency-good but memory debt at `0.76x` latency and `1.13x` memory, while `string_builtin` remains tracked debt at `1.95x` latency and `1.01x` memory. Remaining heap-string follow-ups include other externalized string producers and an eventual handle-only `Value` layout. |
| [x] | Done | Host return heap-string tranche | Embedding API / runtime architecture / memory | Move host callback string returns onto the VM-owned string heap while preserving the public host conversion API. | `checked_host_return_value` now has VM context and interns owned host strings before returning them to JavaScript. Typed host callbacks returning `String` or `&str`, plus legacy callbacks returning `Value::String`, keep the existing API surface while their runtime values become heap-backed at the VM boundary. Focused host API coverage verifies that repeated equivalent host string returns reuse the same heap entry. Validation passed with `cargo fmt --all -- --check`, targeted host API coverage, `cargo check --all-targets`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test`, `git diff --check`, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T112845Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 46 latency and 57 memory exceptions; aggregate performance is `1.27x` and memory is `1.07x`. `string_concat` remains within both budgets at `0.79x` latency and `0.99x` memory, while `string_builtin` remains tracked debt at `1.85x` latency and `1.13x` memory. At the time this tranche landed, remaining heap-string follow-ups included context-aware diagnostics, string index-property results, wrapper/object storage, and an eventual handle-only `Value` layout. |
| [x] | Done | Parser literal heap-string tranche | Runtime architecture / memory | Move parser-produced string and template literal execution onto the VM-owned string heap without changing the parser or public AST compatibility boundary. | `eval_expr` now routes `Expr::Literal(Value::String(_))` through `Context::literal_value`, so ordinary string literals and no-substitution template literals intern into `StringHeap` at runtime and repeated evaluations reuse the same VM-owned `JsString` entry. Non-string literals keep the existing checked clone path. Direct coverage in `string_heap_smoke.rs` now verifies that repeated ordinary and template literals do not grow `string_count` or `string_bytes`, and that concatenation reuses already interned literal operands before adding only the missing operand and result strings. Validation passed with `cargo fmt --all -- --check`, targeted heap-string coverage, `cargo check --all-targets`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test`, `git diff --check`, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T111831Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 45 latency and 57 memory exceptions; aggregate performance is `1.26x` and memory is `1.07x`. `string_concat` is now within both budgets at `0.76x` latency and `0.99x` memory, while `string_builtin` remains tracked debt at `1.87x` latency and `1.12x` memory. This is a focused continuation of the heap-string migration; context-free diagnostics, string index properties, host returns, and a handle-only `Value` layout remained later work at the time this tranche landed. |
| [x] | Done | String heap foundation tranche | Runtime architecture / memory | Introduce VM-owned heap strings and resource accounting before the broader compact `Value` and `StringId` migration. | Adds `string_heap.rs` with checked `StringId`, `JsString`, and `StringHeap` storage backed by deduplicated `Rc<str>` entries plus byte accounting. `Value` now has a heap-backed string variant while keeping `Value::String(String)` for parser literals, host returns, and public compatibility; equality, display, truthiness, type names, numeric coercion, property access, JSON, object built-ins, host conversions, and delete/enumeration paths treat both string variants with the same JavaScript semantics. `Context` now owns the heap, exposes `heap_string_value`, and `VmResourceUsage` reports `string_count` and `string_bytes`. The first converted runtime paths are `typeof`, string concatenation, `String(...)`, string wrapper character properties, native function names, JSON string parse/stringify results, `Object.keys` result arrays, and `for...in` key values. Direct coverage in `string_heap_smoke.rs` verifies repeated runtime strings reuse heap entries and keep stable byte counts. Validation passed with `cargo fmt --all -- --check`, targeted heap-string coverage, `cargo check --all-targets`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test`, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T111020Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 44 latency and 59 memory exceptions; aggregate performance is `1.25x` and memory is `1.09x`, with `string_builtin` at `1.71x`, `boolean_builtin` at `1.80x`, `number_builtin` at `1.38x`, `compiled_script_reuse` at `1.22x`, and `atomized_bindings` at `1.17x` latency plus `1.02x` memory. The branch is a memory-model foundation, not a completed string-performance win; parser literals and host returns landed in later tranches, while object/string wrapper storage, diagnostics, and eventual handle-only `Value` layout remain follow-up work. |
| [x] | Done | Holey array no-prototype-index fast paths | Runtime architecture / performance | Continue the Dense Arrays direction by adding safe direct holey-storage reads when prototype-chain indexed properties cannot affect observable results. | Adds `runtime_object_array_fast.rs` as the central owner for guarded holey dense fast paths. `ArrayStorage` now exposes exact-length holey views and range checks for dense or sparse indexed properties, while the fast-path guard walks the receiver prototype chain with the existing traversal budget and falls back whenever an indexed prototype property, sparse key, length mismatch, overflow, or uncacheable shape can affect the result. `includes`, `indexOf`, `lastIndexOf`, `join`, `slice`, and `concat` can now read holey dense storage directly in the no-prototype-index case; inherited indexed values still use the generic property/prototype path. Direct coverage lives in `holey_array_fast_path_smoke.rs` and verifies holes are preserved without prototype indexes while `Array.prototype[0]` and `Array.prototype[2]` are observed and copied through the fallback. Validation passed with `cargo fmt --all -- --check`, targeted holey-array coverage, `cargo check --all-targets`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test`, `git diff --check`, and `scripts/test-all.sh` after rebasing onto the QuickJS benchmark-budget `main` update. Report `rsqjs-test-report-20260706T104244Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 46 latency and 60 memory exceptions; array rows remain tracked debt with `join` at `1.55x`, `concat` at `1.91x`, `includes` at `1.47x`, `indexOf` at `1.36x`, `lastIndexOf` at `1.57x`, and `slice` at `1.68x`. Compared with the previous benchmark-budget baseline, this run improves or roughly preserves several scan/copy rows but is noisy or worse on others, so the main result is the safe architecture boundary for broader holey/sparse array work rather than a completed performance win. |
| [x] | Done | Static binding native-call cache tranche | Runtime architecture / performance | Extend direct native-call metadata from static member call sites to static identifier call sites. | Static binding caches now keep guarded native-call metadata beside binding-location entries, and direct `Identifier(...)` call sites resolve the current binding cell before using any cached native dispatch. The fast path only runs when the current value is still the same `NativeFunctionId`, so local parameters, user functions, shadowing, lazy builtin materialization, and later binding updates keep their observable semantics. Direct coverage lives in `static_binding_native_call_cache_smoke.rs` and verifies one compiled call site moving through native `Boolean`, user JS function, native `Number`, native `String`, and native `Boolean` again across repeated compiled evaluation. Validation passed with `cargo fmt --all -- --check`, targeted static-binding native-call cache coverage, `cargo check --all-targets`, `cargo clippy --all-targets --all-features`, `cargo test`, `git diff --check`, and `scripts/test-all.sh` after rebasing onto the benchmark-rollup `main` update. Report `rsqjs-test-report-20260706T102500Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 42 latency and 17 memory exceptions; `number_builtin` remains at `1.39x`, `string_builtin` is `1.59x`, `boolean_builtin` remains noisy at `1.82x`, `compiled_script_reuse` is `1.23x`, and `atomized_bindings` is `1.12x`. The main result is architectural: global/native constructor calls are now closer to future bytecode `CallNativeBuiltin` operands, while the remaining builtin cost is still dominated by generic call dispatch, object/prototype paths, wrapper allocation, and descriptor/builtin metadata work. |
| [x] | Done | Primitive constructor argument fast path tranche | Runtime architecture / performance | Avoid temporary argument-vector allocation in fixed-arity primitive constructors while preserving JavaScript argument side effects. | `String`, `Number`, and `Boolean` callable/constructable native paths now share a fixed-arity helper that retains only the first argument value and evaluates unused extra arguments in source order without building a temporary `Vec<Value>`. This also fixes `Boolean` extra-argument side effects, which previously stopped after the first argument. Direct coverage lives in `primitive_constructor_argument_fast_path_smoke.rs` and verifies callable and constructable `String`, `Number`, and `Boolean` argument evaluation order. Validation passed with `cargo fmt --all -- --check`, targeted primitive-constructor coverage, `cargo check --all-targets`, `cargo clippy --all-targets --all-features`, `cargo test`, `git diff --check`, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T101053Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 40 latency and 24 memory exceptions; `number_builtin` moved from `1.42x` to `1.39x`, while `string_builtin` and `boolean_builtin` remain noisy tracked latency exceptions at `1.63x` and `1.81x`. The remaining built-in debt is dominated by call dispatch, mutable wrapper/prototype object paths, string allocation, intrinsic metadata, and future bytecode/direct native-call operands rather than argument-vector materialization alone. |
| [x] | Done | Array native argument fast path tranche | Runtime architecture / performance | Remove temporary argument-vector allocation from fixed-arity array native methods while preserving argument side effects. | Fixed-arity and no-argument `Array.prototype` paths now evaluate only the argument values they retain, still evaluate unused extra arguments in source order for side effects, and avoid building temporary `Vec<Value>` collections for `join`, `includes`, `indexOf`, `lastIndexOf`, `slice`, `reverse`, `pop`, and `shift`. Variadic and materializing paths such as `Array`, `push`, `unshift`, and `concat` intentionally stay on the generic path until descriptor handling, dense-array storage, and conversion ordering can be handled together. Direct coverage lives in `array_native_argument_fast_path_smoke.rs` and verifies extra-argument side effects plus receiver-error ordering. Validation passed with `cargo fmt --all -- --check`, targeted array fast-path coverage, `cargo check --all-targets`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T100032Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 42 latency and 23 memory exceptions; `array_prototype_includes` improved from `1.60x` to `1.49x`, `array_prototype_index_of` from `1.60x` to `1.43x`, `array_prototype_last_index_of` from `1.63x` to `1.42x`, and `array_prototype_shift_unshift` from `1.58x` to `1.43x`. `join`, `slice`, and memory ratios remain noisy or still over budget, confirming that the next array gains need packed/holey/sparse storage, descriptor-aware indexed access, and bytecode/native-call operands rather than argument collection cleanup alone. |
| [x] | Done | Native call-site cache tranche | Runtime architecture / performance | Extend per-site static member caches into native-function dispatch without bypassing JavaScript property mutation semantics. | Adds a bytecode-ready native-call cache next to the existing static property access cache. Each entry stores the last `NativeFunctionId` and `NativeFunctionKind` for a static member call site, but the fast dispatch is used only after the current guarded static property read still returns the same native function id. If a builtin property is overwritten, a prototype method is replaced, or the value is no longer native, the call falls back to the generic path. Direct coverage lives in `native_call_site_cache_smoke.rs` and verifies repeated compiled `Math.abs` plus `Array.prototype.push` call sites follow later property/prototype mutations instead of using stale native dispatch. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features`, targeted native-call cache coverage, `cargo test`, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T095002Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 42 latency and 23 memory exceptions; `math_builtin` moved from `1.77x` to `1.66x` and `math_methods` from `2.74x` to `2.73x`, but the overall signal is mixed and confirms that the next native-call gains need direct static builtin or bytecode operands that reduce call-reference/property-lookup work rather than only caching the final native kind. |
| [x] | Done | Math native argument fast path tranche | Runtime architecture / performance | Remove per-call argument-vector allocation from fixed-arity `Math.*` native methods while preserving argument evaluation order. | Fixed-arity `Math.*` native methods now evaluate the first one or two argument expressions directly, evaluate unused extra arguments for side effects in source order, and avoid building a temporary `Vec<Value>` for unary, binary, integer, rounding, and random paths. Variadic `Math.max`, `Math.min`, and `Math.hypot` intentionally keep the generic argument-vector path so later object-to-number conversion ordering can be handled together with broader native-call dispatch. Direct coverage extends `math_methods_smoke.rs` with extra-argument side-effect ordering checks across unary, binary, and random calls. Validation passed with `cargo fmt --all`, `git diff --check`, targeted math smoke tests, `cargo check --all-targets`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260706T093933Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 41 latency and 20 memory exceptions; `math_methods` improved from `2.93x` to `2.74x`, `math_random` from `2.02x` to `1.95x`, and the remaining native-call debt is dominated by AST dispatch, static builtin property lookup/call reference setup, variadic math argument handling, and future bytecode/native-call operands. |
| [x] | Done | Static property store cache tranche | Runtime architecture / performance | Preserve per-site property operands for static assignments and use guarded own-slot writes where shape facts prove it is safe. | `PropertyAssignment` now carries the original `StaticPropertyAccessId` from the parsed member target, so simple static assignments no longer discard access-site metadata. `set_static_property_value` reuses the per-site cache slot for ordinary object writes and calls `ObjectHeap::write_cacheable_own_property_value_for` only when the cached lookup is still valid for the receiver and points to an existing own named data slot. Missing properties, prototype hits, arrays, `__proto__`, functions, native functions, stale guards, and uncacheable paths fall back to generic assignment. Direct coverage lives in `static_property_store_cache_smoke.rs` and verifies access slot counts, repeated compiled static writes, fallback after delete/shape changes, non-writable descriptor behavior, and computed literal assignment through static store operands. Validation passed with `cargo fmt --all`, `cargo test --test static_property_store_cache_smoke`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh` with local QuickJS/Test262 cache directories. Report `rsqjs-test-report-20260706T093031Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 30 latency and 26 memory exceptions; `compiled_script_reuse`, `method_this`, `object_prototypes`, `computed_properties`, and `atomized_bindings` are within latency budget in this run, while remaining debt is concentrated in descriptors, built-ins, arrays, function/property paths, and memory ratios. |
| [x] | Done | Static computed property operand tranche | Runtime architecture / performance | Route side-effect-free computed literal keys such as `obj["name"]` and `obj[0]` through static property operands instead of rebuilding dynamic string keys at runtime. | Parser normalization now turns computed string, number, boolean, null, and undefined literal keys into `Expr::Member` with a checked `StaticPropertyAccessId`, so reads, compound assignments, updates, deletes, method-call references, static atom caches, and guarded object/prototype lookup snapshots reuse the existing static property operand path. Truly dynamic computed expressions remain on the generic `DynamicPropertyKey` path. Direct coverage lives in `static_computed_property_operand_smoke.rs` and verifies static access slot counts, missing static computed keys without atom interning, assignment/update atom reuse, and computed method-call `this` binding. Validation passed with `cargo fmt --all`, `cargo test --test static_computed_property_operand_smoke`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh` with local QuickJS/Test262 cache directories. Report `rsqjs-test-report-20260706T092106Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 41 latency and 22 memory exceptions; `computed_properties` is within budget at `0.96x` latency and `1.09x` memory. Remaining runtime-model debt is concentrated in descriptors, built-ins, arrays, prototypes, `in`, and binding lookup. |
| [x] | Done | Per-site `in` operator cache tranche | Runtime architecture / performance | Extend guarded property lookup snapshots from static member reads to compiled `in` presence checks. | Adds an optional `StaticPropertyAccessId` to binary expressions, assigns a checked occurrence slot to every parsed `BinaryOp::In`, exposes `CacheablePropertyPresence` and receiver-validated presence reads from `ObjectHeap`, and routes compiled `in` evaluation through `has_cached_dynamic_property_value`. The cache is only used when the dynamic property key already has a `PropertyKey`; unknown string keys remain uncacheable and fall back without interning. Direct coverage lives in `in_operator_access_site_cache_smoke.rs` and verifies occurrence counts, repeated compiled `in`, missing-key atom counts, prototype deletion, own-property addition, and fallback-preserving semantics. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, `cargo test --test in_operator_access_site_cache_smoke`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh` with local QuickJS/Test262 cache directories. Report `rsqjs-test-report-20260706T091056Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 40 latency and 29 memory exceptions; `in_operator` remains tracked debt at `1.19x` latency and `1.10x` memory because AST dispatch, dynamic key conversion, and array-index fallback still dominate. Other remaining object/runtime debt includes `object_prototypes` at `1.17x`, `object_prototype_root` at `1.29x`/`1.17x`, `object_builtin` at `1.29x`, `method_this` at `1.19x`, and `compiled_script_reuse` at `1.29x`. Future work should add store/call caches, computed property operands that can retain known keys across eval, bytecode dispatch, and compact cache storage. |
| [x] | Done | Per-site static property read cache tranche | Runtime architecture / performance | Move compiled static member read cache ownership from property names to checked per-occurrence access slots. | Adds `StaticPropertyAccessId`, tracks `static_property_access_count` in parse and compiled-script usage, gives each parsed `obj.foo` member occurrence its own checked cache slot, and sizes the static-name cache handle with separate atom and property-access capacities. Static member reads, read-modify-write operations, and method call references now pass the occurrence id into `get_static_property_value`, so two `.slot` sites on different receivers no longer churn the same static-name lookup cache. Direct coverage lives in `static_property_access_site_cache_smoke.rs` and verifies occurrence counts, repeated compiled reads with the same property name, and guarded method-call reads across prototype replacement plus own-property shadowing. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, `cargo test --test static_property_access_site_cache_smoke`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh` with local QuickJS/Test262 cache directories. Report `rsqjs-test-report-20260706T090141Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 39 latency and 34 memory exceptions; remaining tracked debt includes `object_prototypes` at `1.17x` latency, `object_prototype_root` at `1.27x`/`1.18x`, `object_builtin` at `1.27x` latency, `in_operator` at `1.19x`/`1.12x`, `method_this` at `1.16x`/`1.12x`, and `compiled_script_reuse` at `1.16x`/`1.11x`. Future work should add static/computed `in` operands, per-site store/call caches, bytecode property operands, and more compact cache storage once bytecode exists. |
| [x] | Done | Static property inline cache tranche | Runtime architecture / performance | Reuse cacheable shape/prototype lookup snapshots for compiled static `obj.foo` reads while preserving receiver and prototype correctness. | Adds a property lookup cache next to the compiled static-name atom cache and routes ordinary object static property reads through `ObjectHeap::cacheable_property_lookup` before generic `get_property`. Cached hits are guarded by receiver `ObjectId`, receiver `ShapeId`, and prototype lookup version, so same-name reads on different objects fall back and rewrite the cache instead of returning stale slots. `__proto__`, functions, native functions, array-specific, and uncacheable paths keep the existing generic behavior. Direct coverage lives in `static_property_inline_cache_smoke.rs` and checks receiver identity, prototype value and shape changes, own-property shadowing and deletion, missing reads, and `__proto__` fallback. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features`, `cargo test --test static_property_inline_cache_smoke`, `cargo test`, `git diff --check`, and `scripts/test-all.sh` with local QuickJS/Test262 cache directories. Report `rsqjs-test-report-20260706T084909Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 41 latency and 29 memory exceptions; object/prototype rows remain tracked debt (`object_prototypes` at `1.25x`, `object_prototype_root` at `1.34x`/`1.21x`, `object_builtin` at `1.28x`/`1.11x`, `in_operator` at `1.18x`, and `compiled_script_reuse` at `1.20x`/`1.10x`). Future work should move from static-name cache slots to per-occurrence or bytecode site caches and extend the same guarded model to `in`, stores, calls, function/native dispatch, and built-in property loads. |
| [x] | Done | Descriptor property key cache tranche | Runtime architecture / performance | Pre-resolve descriptor object property keys and remove owned-name cloning from fixed descriptor result object construction. | Adds `DescriptorPropertyKeys` cached on `Context`, extends VM-local well-known property keys to `value`, `writable`, `enumerable`, `configurable`, `get`, and `set`, and switches data-object creation to `ObjectPropertyInit<'_>` so descriptor result objects pass fixed names as borrowed strings while JSON-created objects keep owned names alive for the heap builder call. Direct coverage adds `reuses_descriptor_property_keys_and_shape_layout` to verify repeated ordinary object descriptor materialization preserves descriptor semantics without growing atom or shape counters after warmup. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, targeted object descriptor tests, `cargo clippy --all-targets --all-features`, `cargo test`, `git diff --check`, and `scripts/test-all.sh` with local QuickJS/Test262 cache directories. Report `rsqjs-test-report-20260706T083903Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 40 latency and 34 memory exceptions; descriptor-heavy rows remain tracked debt (`object_descriptors` at `2.26x`/`1.24x`, `function_descriptors` at `1.97x`/`1.29x`, `function_intrinsic_descriptors` at `2.08x`/`1.27x`). Future work should move descriptor-heavy loops toward bytecode property operands, inline-cache-backed property reads, direct native call paths, and a more compact descriptor object allocation model rather than adding more string-special cases. |
| [x] | Done | Function prototype intrinsic defaults tranche | Runtime architecture / performance | Store JS and native function `prototype` descriptor defaults with the existing function intrinsic defaults instead of synthesizing descriptor objects in lookup helpers. | Adds optional `prototype` descriptors to `FunctionIntrinsicDefaults`, pre-resolves constructable JS function prototype descriptors and native function prototype descriptors during function creation, keeps JS prototype descriptor values synchronized when `f.prototype` is reassigned, and removes the separate JS/native prototype descriptor synthesis helpers from `runtime_function.rs`. Direct coverage extends `function_intrinsic_defaults_smoke.rs` to verify JS/native prototype descriptor flags, JS prototype value updates, protected native prototype writes, and stable atom counts across repeated prototype descriptor reads. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, targeted function intrinsic tests, `cargo clippy --all-targets --all-features`, `cargo test`, `git diff --check`, and `scripts/test-all.sh` with local QuickJS/Test262 cache directories. Report `rsqjs-test-report-20260706T082755Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 36 latency and 28 memory exceptions; function descriptor rows remain tracked debt (`function_descriptors` at `2.10x`/`1.17x`, `function_intrinsic_descriptors` at `2.16x`/`1.17x`). Future work should move descriptor object allocation toward reusable shapes and bytecode property operands rather than adding more ad-hoc intrinsic branches. |
| [x] | Done | Function intrinsic defaults tranche | Runtime architecture / performance | Pre-resolve immutable JS and native function `length`/`name` defaults so descriptor and value hot paths do not rebuild the same metadata on every access. | Adds `FunctionIntrinsicDefaults` to `FunctionProperties`, prepares JS function defaults from `FunctionArity` and `FunctionName` at creation, prepares native defaults from `NativeFunctionKind`, removes stored `Function.name`/`Function.arity` fields that were only needed for repeated default reconstruction, and adds `function_intrinsic_defaults_smoke.rs` coverage for JS/native descriptor semantics plus stable atom counts across repeated descriptor reads. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, targeted function intrinsic tests, `cargo clippy --all-targets --all-features`, `cargo test`, `git diff --check`, and `scripts/test-all.sh` rerun with local QuickJS/Test262 cache directories after an initial network fetch failure. Report `rsqjs-test-report-20260706T081932Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 29 latency and 25 memory exceptions; `function_properties` and `function_custom_properties` are within both budgets at `1.03x`/`1.06x` and `1.07x`/`1.02x`, while `function_descriptors` and `function_intrinsic_descriptors` remain descriptor-path debt at `2.02x`/`1.24x` and `1.90x`/`1.28x`. Future work should avoid synthesizing prototype descriptors on hot paths, move descriptor objects toward reusable shapes, and add bytecode property operands for descriptor-heavy loops. |
| [x] | Done | Function intrinsic property kind tranche | Runtime architecture / performance | Replace repeated string dispatch for function `length`, `name`, and `prototype` metadata with a typed property-kind boundary. | Adds `FunctionPropertyKind` and routes JS function plus native-function intrinsic descriptor, value, `has`, set, define, delete, and enumerable-key paths through it. Custom properties still use atom-backed `PropertyKey` storage, while intrinsic metadata keeps string-compatible public behavior. Direct coverage lives in `function_intrinsic_property_kind_smoke.rs` and verifies JS/native intrinsic metadata reads do not create new atoms, while descriptor reconfiguration and protected prototype deletion semantics still hold. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, targeted intrinsic metadata coverage, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T075925Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 29 latency and 27 memory exceptions; `function_properties` is within both budgets at `1.01x`/`1.00x`, `function_custom_properties` is within latency budget at `1.09x` but remains a memory exception at `1.13x`, and `function_descriptors` plus `function_intrinsic_descriptors` remain descriptor-path debt at `1.91x`/`1.27x` and `1.98x`/`1.27x`. Future work should pre-resolve intrinsic metadata and move bytecode property operands beyond string names. |
| [x] | Done | Function property key lookup tranche | Runtime architecture / performance | Carry atom-backed property identity through ordinary function and native-function property paths. | Adds lookup/key-based helpers for function and native-function property read, write, delete, own-descriptor, and `in` checks, then routes static and dynamic property dispatch through those helpers. This keeps intrinsic `name`/`length`/`prototype` semantics string-compatible while allowing custom function properties to use the same `PropertyLookup` and `PropertyKey` values as ordinary objects. Direct coverage lives in `function_property_key_cache_smoke.rs` and verifies repeated dynamic reads/writes on JS and native functions do not create extra atoms after the first property insertion. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, targeted function property coverage, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T074307Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 32 latency and 20 memory exceptions; `function_properties` is within both budgets at `1.06x`/`1.09x`, `function_custom_properties` is within latency budget at `0.96x` but remains a memory exception at `1.13x`, and `function_descriptors` plus `function_intrinsic_descriptors` still need descriptor-path and intrinsic-metadata work. |
| [x] | Done | Dynamic property key cache tranche | Runtime architecture / performance | Carry atom-backed property identity through computed property paths instead of passing only owned strings between runtime stages. | Adds `DynamicPropertyKey` as a runtime key object with the JS property name and optional `PropertyKey`, factors dynamic property operations into `runtime_dynamic_property.rs`, and routes computed member calls, reads, assignments, compound assignments, updates, deletes, `in`, and computed `for...in` assignment targets through that key. Missing computed property reads/deletes still do not intern new names; the first dynamic write interns once and later read-modify-write/delete operations reuse the known key. Direct coverage lives in `dynamic_property_key_cache_smoke.rs`. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, targeted dynamic property tests, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T073350Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 40 latency and 21 memory exceptions; `computed_properties` is within both budgets at `0.90x`/`1.09x`, while `in_operator`, compound/update paths, `compiled_script_reuse`, and `atomized_bindings` remain tracked debt for bytecode-ready property operands and inline caches. |
| [x] | Done | Exact binding cache location tranche | Runtime architecture / performance | Remove atom/name validation from already proven compiled local and upvalue cache hits while keeping globals guarded. | Refactors `BindingLocation` from an atom-carrying struct into guarded global/builtin/local and atom-free exact local/upvalue variants. Cached compiled local hits now read by frame slot after the first checked resolution, cached upvalue hits read by upvalue slot, and global/builtin paths continue carrying atoms and shadow guards. Direct coverage verifies repeated function calls keep local frames call-scoped instead of reusing stale cells. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, targeted static-binding and upvalue tests, `cargo test`, `cargo clippy --all-targets --all-features`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T071816Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 43 latency and 24 memory exceptions; `function_parameters_scope` and `closure_environments` remain within both budgets at `0.92x`/`1.01x` and `0.93x`/`1.00x`, while `atomized_bindings` remains a latency exception at `1.19x` and `compiled_script_reuse` remains a latency/memory exception at `1.20x`/`1.10x`, so bytecode/direct operand dispatch remains the next binding-path step. |
| [x] | Done | Transitive upvalue lifting tranche | Runtime architecture / memory | Remove the conservative nested-function capture fallback when compact upvalue frames can represent the closure chain. | Adds `StaticFunctionId` metadata to parsed function expressions, keeps function parent/upvalue metadata in `BindingLayout`, lifts captured declarations through intermediate function scopes, and resolves closure creation through exact declaration slots or parent upvalue frames. Direct coverage verifies a three-level `outer -> middle -> inner` closure chain evaluates correctly with zero legacy captured scopes and compact upvalue cells. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, targeted binding-layout/language/embedding tests, `cargo test`, `cargo clippy --all-targets --all-features`, `git diff --check`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T065952Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmark signal improves `closure_environments` to within both latency and memory budgets at `0.93x` and `1.03x`; direct bytecode operands, exact-global validation, and compact closure-cell layout remain follow-up work. |
| [x] | Done | Establish persistent development plan | Planning | Create the general project plan, task board, and task protocol. | Replaces the earlier narrow plan with a broader development plan. Future branches should update this board when they start and finish work. |
| [x] | Done | Generalize project roadmap scope | Planning | Rebalance the plan around product development order instead of an optimization-first backlog. | Clarifies that performance and memory are recurring guardrails, while embedding API, compatibility, host extensions, resource control, and observability are first-class roadmap tracks. |
| [x] | Done | Embedding API skeleton | Embedding API | Introduce the public direction for `Engine`, isolated `Vm`, execution `Context`, and embedder-owned configuration. | Adds `Engine`, `EngineConfig`, `Vm`, `VmConfig`, VM resource usage, teardown reports, README coverage, and direct library tests for isolated VMs, output separation, VM-specific limits, and teardown. Validation passed with `cargo fmt -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T144858Z.md` keeps existing benchmark exceptions tracked. |
| [x] | Done | Multi-VM isolation fixtures | Embedding API / testing | Prove that many VMs can run in one Rust process with isolated globals, output, limits, errors, and teardown. | Adds a direct library fixture that runs eight VMs in one process, verifies isolated globals and output buffers, forces a separate VM resource-limit failure, then confirms the surviving VMs continue and produce teardown reports. Validation passed with `cargo fmt -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T145428Z.md` keeps benchmark exceptions tracked. |
| [x] | Done | Host function API skeleton | Embedding API | Add the first typed Rust host function registration path. | Adds synchronous `Context::register_host_function`, `HostCall` checked argument accessors, contextual callback errors, VM-local callback storage, and conservative rejection of VM-owned handle return values. Validation passed with `cargo fmt -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T150355Z.md` keeps benchmark exceptions tracked. Async support remains for the promise/job queue task. |
| [x] | Done | Test262 feature map | Compatibility / testing | Convert full Test262 results into a feature-oriented progress map. | Adds compact full-corpus feature-area tables with pass/fail/skip counts, pass rate, active-manifest counts, top skip reasons, and an aggregated `other feature areas` row. Validation passed with `cargo fmt -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T151636Z.md` records 102578 executed Test262 variants, 9098 passed, 93480 failed, and keeps failed case details capped at the last 30. |
| [x] | Done | Parser and lexer Test262 cluster | Compatibility | Reduce top parser and lexer failure categories in full Test262 reports. | Adds the `numeric-literal-syntax` cluster: binary, octal, hexadecimal, decimal exponent, leading-decimal, and numeric separator support without BigInt semantics. Validation passed with `cargo fmt -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T152806Z.md` raises full Test262 passes from 9098 to 9378, raises `language/literals` passes from 514 to 784, lowers parser failures from 25349 to 23170, and lowers the top `lexer: unexpected character` hint from 17549 to 16369. BigInt literals remain unsupported and are now reported explicitly. |
| [x] | Done | Runtime semantics cluster | Compatibility | Expand coherent statement, expression, scope, function, and error semantics. | Adds omitted catch binding support (`try { ... } catch { ... }`) without creating a catch parameter binding. Validation passed with `cargo fmt -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T153644Z.md` raises full Test262 passes from 9378 to 9388, lowers parser failures from 23170 to 23160, and adds one active Test262 and one QuickJS differential case. |
| [x] | Done | Clarify general development sequence | Planning | Make the working plan clearly describe the whole product roadmap instead of looking like a single-workstream plan. | Adds product delivery milestones, aligns the short roadmap with the operational plan, and frames performance work as acceptance criteria plus checkpoint tasks. This was a documentation-only change validated with `git diff --check`; full CI remains the merge gate. |
| [x] | Done | Basic built-ins expansion | Compatibility | Expand high-value `Object`, `Array`, `String`, `Number`, and `Math` behavior. | Starts the built-ins track with `Number` as a function/constructor, `Number.prototype.constructor`, basic string/boolean/null/undefined conversions, and static constants such as `NaN`, infinities, safe integer bounds, `MAX_VALUE`, `MIN_VALUE`, and `EPSILON`. Adds engine, active Test262, QuickJS differential, and benchmark fixtures. Validation passed with `cargo fmt -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T155826Z.md` raises full Test262 passes from 9388 to 9609, raises `built-ins/Number` passes from 8 to 32, and removes `missing binding: Number` from the top missing bindings. The new `number_builtin` benchmark is tracked as a latency exception at `1.29x`; `Number.prototype` primitive-wrapper internals remain future work. |
| [x] | Done | `CompiledScript` AST wrapper | Embedding API / performance | Add a reusable compiled representation before bytecode, so embedders can parse once and evaluate repeatedly. | Adds AST-backed `CompiledScript` and `CompiledScriptUsage`, plus `compile`/`eval_compiled` on `Context`, `Vm`, and compatibility `Runtime`. The parser now reports compile usage so target VMs reject compiled scripts that exceed their stricter source, statement, or expression-depth limits. Direct library tests cover repeated eval in one VM, reuse across isolated VMs, compile-time parse errors, and stricter target VM limits. Benchmark reports now separate cold eval, compile-only, and compiled-eval columns and add `compiled_script_reuse`. Validation passed with `cargo fmt -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T161440Z.md` keeps full Test262 at 9609 passed, measures 51 benchmarks, and records `compiled_script_reuse` as a rounded `1.10x` latency exception while showing `3 us` compile-only, `54 us` compiled eval, and `67 us` cold eval. Future bytecode, atoms, and slot locals can replace the backing representation behind this API. |
| [x] | Done | Atom interner | Performance and memory | Store identifiers, property keys, function names, and reusable string constants as compact atom ids. | Starts atomization with a VM-owned `AtomTable`, `AtomId`, and lexical/global bindings keyed by atoms instead of owned strings. Public APIs still accept string names, missing-name lookups do not create atoms, and `VmResourceUsage` now reports `atom_count`. Adds direct embedding coverage for atom accounting and a binding-heavy `atomized_bindings` benchmark. Property keys, function metadata, reusable string constants, and atom budgets remain follow-up work so this branch does not mix atomization with shape/object-layout changes. Validation passed with `cargo fmt -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T162515Z.md` keeps full Test262 at 9609 passed, measures 52 benchmarks, brings `compiled_script_reuse` within budget at `1.09x`, and records `atomized_bindings` as a `1.17x` latency exception. |
| [x] | Done | Project plan naming and sequence | Planning | Make the canonical plan name and task order reflect the whole product roadmap. | Renames the canonical plan to `docs/project-plan.md`, updates README and roadmap links, broadens the backlog around API, compatibility, async, resources, observability, and runtime architecture, and moves performance work into recurring checkpoint and runtime-architecture tracks. Validation passed with stale-link checks and `git diff --check`; full CI remains the merge gate. |
| [x] | Done | Binding slot storage foundation | Runtime architecture / performance | Move runtime binding storage from map-held cells toward slot-indexed storage before compiler-assigned slots. | Adds a checked `BindingSlot` newtype and makes each `BindingScope` store `BindingCell` values in a `Vec`, with the atom map acting as an atom-to-slot index. Direct embedding coverage verifies assignment updates and lexical shadowing through the slot-backed scope. Full compiler-assigned local, global, and upvalue slots remain the follow-up `Slot-based local bindings` task. Validation passed with `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T164512Z.md` keeps full Test262 at 9609 passed, active Test262 and QuickJS differential at 100%, `compiled_script_reuse` within budget at `1.08x`, and `atomized_bindings` as a tracked latency exception at `1.15x`. |
| [x] | Done | Embedding API stability pass | Embedding API | Review `Engine`, `Vm`, `Context`, `CompiledScript`, configuration, teardown, and error surfaces before more features depend on them. | Adds VM-level wrappers for common embedder operations so ordinary callers do not need to route through `Context` for eval, globals, output, or host registration. The README and direct embedding test now exercise `Vm::register_host_function_typed`, `Vm::register_host_function`, `Vm::eval`, `Vm::compile`, `Vm::eval_compiled`, `Vm::get_global`, `Vm::output`, and `Vm::take_output`. `Context` remains available for lower-level control. Validation passed with `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T170105Z.md` keeps full Test262 at 9609 passed, active Test262 and QuickJS differential at 100%, and leaves existing benchmark exceptions to checkpoint tasks. |
| [x] | Done | Host value conversion layer | Embedding API | Add typed conversions between Rust values and JavaScript values for host functions. | Adds `IntoJsValue`, `FromJsValue`, generic `HostCall::argument<T>()`, and `Context::register_host_function_typed` while keeping the existing `Value`-returning callback API compatible. Direct embedding tests cover typed argument extraction, typed returns for `String`, `f64`, `bool`, and `()`, contextual host errors, VM-local callbacks, and VM-owned handle rejection. The README embedding example now uses the typed host API. Validation passed with `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T165400Z.md` keeps full Test262 at 9609 passed, active Test262 and QuickJS differential at 100%, and leaves existing benchmark exceptions to checkpoint tasks. |
| [x] | Done | General product roadmap order | Planning | Make the canonical plan read as a whole-project delivery order, not as an optimization backlog. | Reframes the plan around repository reliability, embedding API, compatibility, built-ins, diagnostics, modules/jobs/async, resources, observability, and only then deeper runtime architecture. Performance and memory remain acceptance criteria and recurring checkpoint tasks rather than the main roadmap label. This documentation-only change was validated with `git diff --check`; full CI remains the merge gate. |
| [x] | Done | Roadmap first-screen refresh | Planning | Make the top of the canonical plan show the whole-product order before optimization details. | Renames the document heading to a roadmap and execution plan, adds a near-term product order, and clarifies that atoms, slots, shapes, bytecode, inline caches, and GC are architecture work in service of compatibility, embedding, resources, observability, performance, and memory footprint. Documentation-only validation passed with `git diff --check`, README target file checks, and `cargo fmt --all -- --check`; full CI remains the merge gate. |
| [x] | Done | Whole-project delivery queue | Planning | Make the plan show what the project will build and in what rough order, not only where optimization work may happen. | Adds explicit guidance for reading the plan, separates product delivery order from runtime foundation work, and adds a concrete near-term queue led by report triage, compatibility, built-ins, embedding API, diagnostics, async, resources, and observability. Documentation-only validation passed with `git diff --check` and `cargo fmt --all -- --check`; full CI remains the merge gate. |
| [x] | Done | Project-wide sequence refresh | Planning | Make the current plan read as the whole project order instead of an optimization-oriented queue. | Renames the document heading to `Project Development Plan`, adds an explicit plan-scope section, refreshes the current delivery queue after the JSON tranche, and clarifies that the task board is historical plus backlog rather than the priority order. Documentation-only validation passed with `git diff --check`; full CI remains the merge gate. |
| [x] | Done | Engine case registry split | Testing / maintenance | Keep the engine fixture registry below the project file-size limit before adding more compatibility tranches. | Moves runtime/error/built-in engine fixture registration out of the central `cases.rs` file into `cases_engine_runtime.rs`, reducing `cases.rs` from 789 to 745 lines so future built-in cases can be added without pushing the main registry over 800 lines. Validation passed with `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T184709Z.md` keeps engine fixtures at 57/57, active Test262 at 57/57, full Test262 at 9782/102578, and QuickJS differential at 55/55. Benchmark counts are behavior-equivalent but show normal measurement noise with latency exceptions at 26 and memory exceptions at 2. |
| [x] | Done | Runtime performance architecture review | Planning / runtime architecture | Incorporate the latest runtime-performance review into the general project plan without turning the plan into an optimization-only backlog. | Records that compile time is no longer the main measured debt for many heavy benchmarks, adds a status matrix for slots, atoms, shapes, inline caches, dense arrays, built-in intrinsics, bytecode quickening, memory layout, and parallel execution, and updates the delivery queue so profiling and runtime data-model work can be pulled forward when reports justify it. Documentation-only validation passed with `git diff --check` and `cargo fmt --all -- --check`; full CI remains the merge gate. |
| [x] | Done | Whole-project sequence correction | Planning | Keep the canonical plan organized around the whole project order rather than the latest optimization discussion. | Reorders the current delivery queue around compatibility, built-ins, embedding API, diagnostics, async, resources, observability, runtime foundations, and recurring performance checkpoints. Keeps the architecture review details, but makes profiling a supporting evidence step rather than the apparent next mandatory project goal. Documentation-only validation passed with `git diff --check` and `cargo fmt --all -- --check`; full CI remains the merge gate. |
| [ ] | Backlog | Report triage cadence | Testing / planning | Keep the next work item grounded in the latest Test262, QuickJS differential, benchmark, and memory evidence. | Before selecting each compatibility or architecture tranche, summarize the newest report signals and record why the chosen task is next. |
| [ ] | Backlog | Runtime hot-path profiling pass | Performance and memory / runtime architecture | Profile benchmark groups before broad runtime data-structure rewrites. | Use after the next selected product or compatibility branch needs runtime architecture evidence. The current benchmark table shows debt in descriptors, object/prototype traversal, arrays, built-ins, `in`, and compiled evaluation, but profiling is an evidence step for broader work rather than the whole project direction. |
| [ ] | Backlog | Library API documentation pass | Embedding API / documentation | Keep crate docs, README examples, and direct library tests aligned with the current public API. | Do this whenever API shape changes enough that embedders could be confused by stale examples. |
| [x] | Done | JSON built-in tranche | Compatibility | Add the first useful `JSON` object surface for embedders and Test262 progress. | Adds `JSON.parse` and `JSON.stringify` for primitives, arrays, and plain objects, including non-enumerable `parse`/`stringify`, function metadata, array omission/null handling, object omission handling, non-finite numbers, and negative zero stringification. Adds direct Rust smoke coverage, engine fixture, active Test262 fixture, six upstream Test262 manifest rows, QuickJS differential coverage, and a benchmark. Validation passed with `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T203225Z.md` raises engine fixtures to 65/65, active Test262 to 63/63, QuickJS differential to 61/61, full Test262 passes from 10371 to 10432, and `built-ins/JSON` to 52/330 passed. The new `json_builtin` benchmark is tracked as a latency and memory exception at `1.60x` and `1.74x`; reviver, replacer, spacing, `toJSON`, raw JSON, SyntaxError typing, property descriptors, and global configurable delete semantics remain follow-up work. |
| [x] | Done | Object property and descriptor tranche | Compatibility | Improve object semantics that many built-ins and Test262 cases depend on. | Adds data-property attributes, non-configurable delete behavior, non-writable assignment behavior, `Object.getOwnPropertyDescriptor`, `Object.defineProperty`, `Object.keys`, and `Object.hasOwn`, with non-enumerable built-in static methods that do not consume user property limits. Adds direct Rust smoke coverage, engine fixture, active Test262 fixture, five upstream Test262 manifest rows, QuickJS differential coverage, and a benchmark. Validation passed with `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T205739Z.md` raises engine fixtures to 66/66, active Test262 to 64/64, QuickJS differential to 62/62, full Test262 passes from 10432 to 10854, and `built-ins/Object` to 428/6802 passed. The new `object_descriptors` benchmark is tracked as a latency and memory exception at `2.21x` and `1.42x`; accessor descriptors, symbols, freeze/seal/preventExtensions, full global-object descriptors, function-object descriptors, and shape-based layout remain follow-up work. |
| [ ] | Backlog | Array built-in tranche | Compatibility | Grow practical array behavior without prematurely rewriting storage. | Add high-value prototype methods and semantics in small clusters. Pull dense array storage forward only when the current representation blocks correctness, resource accounting, or measured hot paths. |
| [x] | Done | Function descriptor tranche | Compatibility / embedding API | Allow descriptor APIs to operate on ordinary and native function values. | Extends `Object.defineProperty` and `Object.getOwnPropertyDescriptor` to `Function` and `NativeFunction` values for data descriptors, including custom enumerable/non-writable/non-configurable properties and descriptor queries for `name`, `length`, and `prototype`. Adds direct Rust smoke coverage, engine fixture, active Test262 fixture, QuickJS differential coverage, and a benchmark. Validation passed with `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T211411Z.md` raises engine fixtures to 67/67, active Test262 to 65/65, QuickJS differential to 63/63, and full Test262 passes from 10854 to 10872. The new `function_descriptors` benchmark is tracked as a latency and memory exception at `1.85x` and `1.54x`; full intrinsic reconfiguration and delete semantics for `name`/`length`/`prototype`, upstream `built-ins/Function` manifest coverage, accessor descriptors, and standard error expansion remain follow-up work. |
| [x] | Done | Standard errors and function intrinsic semantics tranche | Compatibility / embedding API | Improve standard error objects and complete configurable function intrinsic behavior. | Narrows the branch to full configurable `name` and `length` redefinition/delete behavior for ordinary and native functions, including standard error constructors such as `TypeError`. Adds intrinsic descriptor override/delete state, custom property fallback after deletion, enumerable key handling for redefined intrinsics, direct Rust smoke coverage, engine fixture, active Test262 fixture, QuickJS differential coverage, and a benchmark. Validation passed with `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T215331Z.md` raises engine fixtures to 68/68, active Test262 to 66/66, QuickJS differential to 64/64, and keeps full Test262 at 10872/102578. The new `function_intrinsic_descriptors` benchmark is tracked as a latency and memory exception at `1.93x` and `1.36x`; `Function.prototype` fallback after deleting own `name`/`length`, upstream `built-ins/Function` manifest coverage, accessor descriptors, and broader standard error prototype/descriptors remain follow-up work. |
| [x] | Done | Parser and syntax compatibility tranches | Compatibility | Continue reducing Test262 parser and lexer failure clusters. | Adds the `no-substitution-template-literal` cluster: backtick string literals without `${...}` interpolation, including common escapes and line terminator normalization. Template substitutions now fail with an explicit unsupported-feature lexer error. Adds direct Rust smoke coverage, engine fixture, active Test262 fixture, upstream Test262 manifest row, and QuickJS differential coverage. Validation passed with `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T172449Z.md` raises full Test262 passes from 9609 to 9616, raises `language/expressions` passes from 4153 to 4157, raises active Test262 to 55/55, raises QuickJS differential to 53/53, and lowers the top `lexer: unexpected character` hint from 16369 to 6531. Some cases now advance from lexer failures into parser/runtime failure classes, which is expected until interpolation, tagged templates, BigInt, and related syntax are implemented. No new benchmark case was added because this is parser-only literal support; existing benchmark exceptions remain tracked in the report. |
| [x] | Done | Template literal substitution tranche | Compatibility | Implement `${expr}` template literal substitutions so template-heavy Test262 clusters and harness includes can execute. | Adds lexer template head/middle/tail tokenization with substitution brace tracking and nested template support, `Expr::TemplateLiteral` parsing, `TemplateConcat` bytecode compilation with empty-quasi elision, and runtime concatenation that keeps existing primitive-to-string behavior while rejecting symbol substitution values with `TypeError`. Plain no-substitution templates keep their existing single-string token path. The smoke CLI now prints the full error context chain so lexer/parser/runtime failures are visible in one line. Direct coverage lives in `template_literal_smoke.rs` (12 cases including nesting, braces inside substitutions, regexp-after-`${`, escaped `\${`, symbol `TypeError`, and unterminated-substitution errors); the active Test262 fixture `template_literals.js` gains substitution, nesting, adjacent-substitution, object-brace, escaped-substitution, and symbol-error checks; `manifest.tsv` enables 19 upstream template-literal rows (3 run, 16 negative-parse). Known deferred gaps: tagged templates, user-defined `toString` invocation during substitution (blocked on engine-wide `ToPrimitive`), comma expressions inside substitutions (blocked on missing comma operator support), and per-substitution `ToString` ordering relative to later substitution evaluation. |
| [x] | Done | For-of statement and iterator protocol tranche | Compatibility | Implement `for...of` so iteration-heavy Test262 clusters and iterable-consuming code can execute. | Adds contextual `of` parsing next to the existing for-in head (identifier bindings and member/identifier assignment targets; destructuring heads stay a later tranche), `Stmt::ForOf` and a `ForOf` bytecode instruction mirroring `ForIn` through binding layout, hoisting, capture analysis, metrics, and compilation, plus a dedicated runtime module with three iteration sources: live index/length array iteration, code-point string iteration (including string wrapper objects), and the user iterator protocol through a VM-cached well-known `Symbol.iterator` id with `GetMethod` -> `next()`/`done`/`value` consumption, `TypeError` for non-iterables and non-object iterator results, throw propagation from `next()`, and `IteratorClose` via `return()` on break/throw/return (close errors intentionally dropped). Direct coverage lives in `for_of_smoke.rs` (12 cases including fresh per-iteration `const` bindings, labeled continue across nested loops, live array growth, close-on-break/throw but not on exhaustion, and contextual `of` as an identifier); the runner gains the active Test262 fixture `language/statements/for_of` and the `for_of` QuickJS differential case (output verified identical against the pinned QuickJS build); `manifest.tsv` enables 29 upstream for-of rows (4 run, 25 negative-parse). Known deferred gaps: destructuring for-of heads, `for await...of`, generators as iterables, spread/`Array.from` consumption, and installing real `%Array.prototype%[Symbol.iterator]`/`%String.prototype%[Symbol.iterator]` methods so user code can retrieve default iterators explicitly. |
| [x] | Done | Destructuring binding patterns tranche | Compatibility | Implement object and array destructuring binding patterns so pattern-heavy Test262 clusters and idiomatic parameter/loop code can execute. | Adds a `...` lexer token, `BindingPattern` parser AST with depth accounting (object patterns with identifier/string/number/keyword/computed keys, shorthand defaults, and identifier rest; array patterns with elisions, defaults, nesting, and pattern rest), `Stmt::PatternDecl` requiring an initializer, pattern heads for `for-in`/`for-of` via `ForInTarget::PatternBinding`, and parameter patterns desugared in the parser to synthesized `%patternN%` parameters plus a body-prologue `var` pattern declaration so the existing parameter machinery (defaults, arity, arguments) stays intact. Compilation lowers patterns to a structured `DestructurePattern` bytecode form with lazily evaluated default/computed-key blocks; the runtime walker declares leaves through the standard declaration path, reads object properties through the generic property path (symbol computed keys included), consumes array sources through the for-of iterator machinery with `IteratorClose` when the pattern finishes early, builds object rest from remaining own enumerable keys and array rest through the shared array constructor, threads abrupt completions from user code, and raises `TypeError` for null/undefined object sources and non-iterable array sources. Lexical loop-head patterns get a fresh per-iteration scope. Direct coverage lives in `destructuring_smoke.rs` (16 cases); the runner gains the `language/statements/destructuring` active fixture and the `destructuring` QuickJS differential case (byte-identical against the pinned reference); `manifest.tsv` enables 149 verified upstream rows across `const/dstr`, `let/dstr`, and `for-of/dstr` (80 run, 69 negative-parse). Known deferred gaps: assignment-expression destructuring outside declarations, catch-parameter patterns, duplicate-name early errors across non-simple parameter lists, spec-ordered cross-parameter references to destructured names, and rest collection from primitive sources. |
| [x] | Done | Spread syntax and rest parameters tranche | Compatibility | Implement `...` spread/rest positions so argument-forwarding and literal-composition code can execute, staying bytecode-native. | Adds rest parameters for functions/arrows/methods (identifier rests bind the packed remaining-arguments array through a single argument-repacking step in the call funnel; pattern rests desugar to a synthesized `%rest%` parameter plus a body-prologue pattern declaration; rest is excluded from `Function.prototype.length`; trailing/defaulted rests and setter rests are early errors), spread call arguments for binding/member/computed/value callees and `new` through a `CollectSpreadArgs` instruction plus spread-aware call/construct instruction forms, spread array literals via `ArrayLiteralSpread`, and spread object literal entries via a `BytecodeObjectProperty::Spread` operand that copies own enumerable properties (null/undefined sources copy nothing). Iterable expansion reuses the for-of iterator machinery (arrays, strings, user iterators, TypeError for non-iterables, abrupt-completion threading from user iterator code). Direct coverage lives in `spread_rest_smoke.rs` (13 cases including left-to-right evaluation order, spread+rest combination, and throw propagation); the runner gains the `language/expressions/spread_rest` active fixture and the `spread_rest` QuickJS differential case (byte-identical against the pinned reference); `manifest.tsv` enables 39 verified upstream rows across rest-parameters, spread calls, spread arrays, and spread `new`. Known deferred gaps: spread `SuperCall` arguments (needs classes), optimized argument-pack transport (currently an engine array round-trip), and object spread from primitive sources with index properties. |
| [x] | Done | Class declarations baseline tranche | Compatibility | Implement baseline classes without inheritance so the dominant `class` parser cluster can execute, staying bytecode-native. | Adds class declarations and expressions through a shared `ClassLiteral` AST: explicit and parser-synthesized default constructors, instance and static methods, instance getters/setters merged on the prototype through the accessor object model, identifier/string/number/keyword/computed member keys, `static` recognized contextually, semicolons in bodies, and class-body strict mode. Compilation lowers classes to a `CreateClass` instruction carrying the constructor and member `BytecodeFunction`s with computed keys evaluated onto the stack in member order; class declarations bind let-like (TDZ, no hoisting). The runtime creates the constructor as a constructor-only function (`TypeError: Class constructor cannot be invoked without 'new'` via a `class_constructor` flag checked in both call funnels), installs non-enumerable writable/configurable methods and accessors on the eagerly created prototype object, installs static methods on the constructor function, wires `Class.prototype.constructor`, and sets `name`/`length` intrinsics plus computed method names. Early errors: duplicate constructor, constructor as accessor, static member named `prototype`, getter/setter arity/rest. Unsupported class features fail with explicit messages: inheritance (`extends`/`super`), public/private fields, generator/async methods, static accessors (functions have no accessor slots yet). Direct coverage lives in `class_smoke.rs` (13 cases); the runner gains the `language/statements/class_baseline` active fixture and the `class_baseline` QuickJS differential case (byte-identical against the pinned reference); `manifest.tsv` enables 34 verified upstream rows across class statements and expressions. Known deferred gaps: extends/super/home objects, class fields, private members, generator/async members, static accessors, and `super` property references. |
| [x] | Done | Class inheritance tranche | Compatibility | Implement `extends` and `super` on top of the class baseline so subclass-heavy Test262 clusters can execute, staying bytecode-native. | Adds heritage expressions (`extends <call-expression>`) with class, constructor-function, and native-constructor superclasses plus `TypeError` for non-constructor values; the class prototype object chains to the parent prototype through a `prototype_parent` on function creation, and static inheritance walks a `static_parent` fallback in function property lookup. `super(...)` compiles to `CallSuper`/`CallSuperSpread` and runs the parent constructor chain against the already-created `this` with `new.target` preserved (parent return-object overrides are ignored; native superclasses run as no-ops keeping the current instance — documented deviations from allocate-in-base semantics). `super.property` and `super.method(...)` compile to `SuperMember`/`CallSuperMember`/`CallSuperMemberSpread` and resolve through per-function home-prototype super bindings threaded on a runtime super-frame stack that mirrors `this`; arrow functions capture the frame lexically like `new.target`; base-class methods resolve through the object prototype root. Derived classes without a constructor synthesize `constructor(...args) { super(...args); }`. Parser contexts allow `super.x` in any class member, `super()` only in derived constructors, and clear both inside ordinary nested functions. Direct coverage lives in `class_inheritance_smoke.rs` (13 cases including three-level chains, static inheritance, spread super calls, heritage expressions, and arrow super capture); the runner gains the `language/statements/class_inheritance` active fixture and the `class_inheritance` QuickJS differential case (byte-identical against the pinned reference). Known deferred gaps: allocate-in-base `this` semantics with pre-`super()` TDZ, `super` property writes and computed `super[x]`, static super property access, exotic native subclassing (Array length magic), and class fields/private members. |
| [x] | Done | Arguments object tranche | Compatibility | Bind the `arguments` object in non-arrow function calls so the dominant `missing binding: arguments` runtime cluster can execute, staying bytecode-native. | Adds lazy per-call binding at the single call funnel gated on a compile-time `uses_arguments` flag collected alongside capture analysis, so functions that never reference `arguments` pay nothing (no atom growth, no allocation). The binding lives in a one-slot wrapper scope pushed below the function scope, so parameters, hoisted vars, and lexical bindings named `arguments` shadow it through normal scope ordering without disturbing compiled slot layouts. The value is built from the original passed arguments before rest-parameter repacking and is modeled as a dense array, giving indexed access, writable `length`, `for-of` iteration, and spread in one step; parameter defaults observe it. Documented deviations: no mapped parameter aliasing, no `callee`, array `[[Class]]` instead of the arguments exotic object, and closures/arrows do not capture the enclosing binding (arrows correctly do not bind their own). Direct coverage lives in `arguments_object_smoke.rs` (9 cases); the runner gains the `language/arguments_object` active fixture and the `arguments_object` QuickJS differential case (byte-identical against the pinned reference, including strict functions, methods, shadowing, spread, and re-spreading `[...arguments]`); `manifest.tsv` enables 10 verified upstream rows. Known deferred gaps: mapped aliasing in sloppy mode, `callee` poisoning, exotic `[[Class]]`, and closure capture of `arguments`. |
| [x] | Done | Class public fields tranche | Compatibility | Implement public instance and static class fields so field-syntax Test262 clusters and modern class code can execute, staying bytecode-native. | Adds field parsing in class bodies (identifier/string/number/keyword/computed keys, optional initializers, optional semicolons, `static`/`get`/`set` still usable as field names via tightened prefix detection) with early errors for fields named `constructor` and static fields named `prototype`. Compiled fields ride on `BytecodeClass` with lazily evaluated initializer blocks; computed field keys evaluate once at class definition. At class creation the runtime resolves keys, stores instance fields on the constructor function, and evaluates static fields immediately with `this` bound to the constructor as enumerable data properties. Instance fields initialize against the new object with `this` bound, in declaration order, per instance: base-class constructors initialize at construction entry, derived-class constructors after `super()` completes (threaded through the super-frame's owning constructor id), so parent fields are visible to derived initializers through arbitrary chains. Direct coverage lives in `class_fields_smoke.rs` (10 cases); the runner gains the `language/statements/class_fields` active fixture and the `class_fields` QuickJS differential case (byte-identical against the pinned reference); `manifest.tsv` enables 108 verified upstream rows across class statement/expression element suites. Known deferred gaps: private `#` fields/methods, `static {}` blocks, `super.x` in field initializers, class-binding visibility inside static initializers, and spec field-after-`super()` TDZ interplay. |
| [x] | Done | Map and Set builtins tranche | Compatibility | Provide baseline `Map` and `Set` so collection-based Test262 clusters and everyday scripts can execute, completing the stale PR #302 scope. | Adds insertion-ordered VM-side collection storage with SameValueZero key comparison and -0 normalization, bound to instances through object-id slots mirroring the promise pattern, with per-VM isolation and existing object/property limits enforced. Lazily installed constructors register in the native slot registry so constructor and prototype identity stay stable; plain calls without `new` throw `TypeError`. Map methods `get/set/has/delete/clear/forEach` plus a `size` accessor; Set methods `add/has/delete/clear/forEach` with `keys` aliased to `values`; both prototypes expose `entries/keys/values` and `Symbol.iterator` (entries for Map, values for Set). Constructors seed from any for-of iterable including strings and generators of pairs; Map seeding requires object entries read via properties `0`/`1`. Iterator objects snapshot current entries, expose `next()` as `{value, done}`, and are themselves iterable, so for-of, spread, and manual protocol consumption all work. Direct coverage lives in `map_set_smoke.rs` (12 cases); the runner gains the `built-ins/Map/map_set_baseline` active fixture and the `map_set_baseline` QuickJS differential case (byte-identical against the pinned reference); `manifest.tsv` enables 5 verified upstream rows. Known deferred gaps: live (non-snapshot) iterators, `WeakMap`/`WeakSet`, `Map.groupBy`/set algebra methods, subclassing hooks (`Symbol.species`), and callback throw catchability across the native boundary (engine-wide). |
| [x] | Done | JSON reviver and stringify options tranche | Compatibility | Complete the stale PR #307 scope so JSON.parse and JSON.stringify accept their full argument surfaces. | Adopts and finishes the work-in-progress found in the abandoned task worktree, verified and covered from scratch. `JSON.parse` gains the reviver argument with the recursive holder/key walk and property deletion when the reviver returns `undefined`. `JSON.stringify` gains function replacers (called with holder `this`, key, and value), property-list array replacers (allow-list filtering at every object level), the space argument with the spec number clamp to 0-10 and string prefix up to 10 characters driving pretty-printed object and array output, `toJSON` method calls before serialization, and cycle detection reporting `TypeError`. Direct coverage extends `json_builtin_smoke.rs` to 7 cases (reviver transforms and deletions, allow-list filtering, all space forms including clamping, cycle TypeError); the existing `built-ins/JSON/basic` active fixture and `json_builtin` QuickJS differential case are extended by the adopted work and re-verified byte-identical against the pinned reference; `manifest.tsv` gains verified upstream JSON rows. Known deferred gaps: full property-key ordering guarantees for exotic keys, `Symbol` handling corners, and bigint rejection semantics. |
| [x] | Done | Date built-in baseline tranche | Compatibility | Complete the stale PR #309 scope so date-based Test262 clusters and everyday scripts can execute. | Adopts the working implementation found in the abandoned task worktree and ports it across the native-module restructuring (kind/registry/call-target/dispatch moved under `native/function/`), then finishes coverage. The Date constructor supports no-argument now, millisecond, ISO-string, date-component, and copy forms (a Date argument copies its time value directly — completed during the port) with a per-object time value on the object heap and spec day-number arithmetic with time clipping; local and UTC getter families, `getTime`/`valueOf`/`getDay`, and `setTime` are installed on the prototype alongside `toISOString`/`toJSON`/`toString` families; `Date.now`, `Date.UTC`, and ISO-format `Date.parse` statics register in the native slot registry so prototype identity and `instanceof` stay stable. Local time is UTC (no timezone database) — documented deviation. Direct coverage grows `date_builtin_smoke.rs` to 7 cases; the runner gains the `built-ins/Date/date_baseline` active fixture and the `date_builtin` QuickJS differential case (byte-identical against the pinned reference). Known deferred gaps: setter families beyond `setTime`, RFC 2822 and legacy parse forms, timezone offsets in parsing and display, locale string methods, and `Symbol.toPrimitive`. |
| [x] | Done | Array flat and flatMap tranche | Compatibility | Add `Array.prototype.flat` and `Array.prototype.flatMap` on the native/direct-call path so the Array built-in Test262 cluster can advance without AST fallback. | Implements native/direct-call wiring, VM-owned result arrays, checked flatten depth conversion, sparse-hole skipping through array-like property checks, generic object receivers, `flatMap` callback invocation with `thisArg`, runtime step accounting, active Test262 coverage, QuickJS differential coverage, Rust smoke tests, and a sequential benchmark. Local validation passed `RSQJS_FAST_RUNNER=1 scripts/check-fast.sh` and full `scripts/test-all.sh`; local report artifact `target/rsqjs-reports/test-runs/rsqjs-test-report-20260708T153000Z.md` records engine fixtures 68/68, active Test262 97/97, Test262 file conformance 15322/53404 (28.69%, +7 versus the tracked 20260708T151129Z main report), full Test262 29410/102578 (28.67%, +14), QuickJS differential 85/85, and benchmarks 65 measured with 0 failed and 0 invalid. The new `array_flat_flatmap` benchmark is valid and tracked as a latency exception against QuickJS; its input size was raised slightly after CI showed the QuickJS median just below the 1 ms quality threshold. Known deferred gaps: `ArraySpeciesCreate`, primitive receiver object-coercion consistency with other generic Array methods, richer `Array.prototype.toString` effects when nested arrays are stringified, and packed-array-specific flatten fast paths. |
| [ ] | Backlog | Runtime semantics tranches | Compatibility | Expand statements, functions, lexical environments, `this`, exceptions, equality, iteration, and prototype behavior. | Each tranche should improve a visible Test262 feature area without mixing unrelated semantics. |
| [x] | Done | Practical built-ins tranches | Compatibility | Expand `Object`, `Array`, `String`, `Number`, `Math`, `Boolean`, `Function`, errors, JSON, Date, RegExp, Map, Set, and other high-value built-ins. | Adds the basic `String` built-in: callable `String(value)` conversion, constructable `new String(value)` wrapper objects with `String.prototype`, non-enumerable `length`, enumerable character indices, primitive string `length`/index lookup, `in`, and `for...in` support. Adds direct Rust smoke coverage, engine fixture, active Test262 fixture, upstream Test262 manifest rows, QuickJS differential coverage, and a benchmark. Validation passed with `cargo fmt --all`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T173929Z.md` raises full Test262 passes from 9616 to 9722, raises `built-ins/String` passes from 4 to 62, raises active Test262 to 56/56, raises QuickJS differential to 54/54, and removes `String` from the top missing bindings table. The new `string_builtin` benchmark is tracked as a latency exception at `1.50x` while staying within memory budget at `1.07x`; full String prototype methods, wrapper internal primitive semantics, abstract equality/object-to-primitive coercion, and string Unicode code-unit accuracy remain follow-up work. |
| [x] | Done | Boolean built-in tranche | Compatibility | Promote `Boolean` from a special call-path into a normal built-in binding and constructor. | Adds `Boolean` as a normal native built-in binding with call and constructor paths, `Boolean.prototype.constructor`, function `name`/`length`/`prototype` metadata, and shadowing-correct call behavior by removing the old direct-call special case. Adds direct Rust smoke coverage, engine fixture, active Test262 fixture, upstream Test262 manifest rows, QuickJS differential coverage, and a benchmark. Validation passed with `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T183517Z.md` raises full Test262 passes from 9722 to 9782, raises active Test262 to 57/57, raises QuickJS differential to 55/55, and removes `Boolean` from the top missing bindings table. The new `boolean_builtin` benchmark is tracked as a latency exception at `1.47x` while staying within memory budget at `0.94x`; Boolean wrapper internal primitive semantics and `Boolean.prototype.valueOf`/`toString` remain follow-up work. |
| [x] | Done | Global numeric constants tranche | Compatibility | Add standard global numeric value properties that remove the remaining `NaN` missing-binding cluster. | Adds immutable lazy global bindings for `NaN` and `Infinity`, materializes lazy built-ins before assignment, compound assignment, and delete operations, and adds Rust smoke coverage plus engine, active Test262, upstream Test262 manifest, QuickJS differential, and benchmark fixtures. Validation passed with `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T190135Z.md` raises full Test262 passes from 9782 to 10003, raises engine fixtures to 60/60, raises active Test262 to 58/58, raises QuickJS differential to 56/56, raises `built-ins/Number` passes from 32 to 116, raises `built-ins/String` passes from 62 to 76, and removes `NaN` from the top missing bindings table. The new `global_numeric_constants` benchmark is tracked as a latency exception at `1.22x` while staying within memory budget at `0.91x`; full global object property descriptors remain follow-up work. |
| [x] | Done | Math built-in tranche | Compatibility | Add the first standard `Math` object surface and reduce the `missing binding: Math` cluster. | Adds the `Math` object, standard numeric constants, and `abs`, `ceil`, `floor`, `max`, `min`, `pow`, `round`, `sqrt`, and `trunc`, with signed-zero handling for `max`, `min`, and `round`. Adds direct Rust smoke coverage, engine fixture, active Test262 fixture, six upstream Test262 manifest rows, QuickJS differential coverage, and a benchmark. Validation passed with `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T192241Z.md` raises engine fixtures to 61/61, active Test262 to 59/59, QuickJS differential to 57/57, full Test262 passes from 10003 to 10193, and `built-ins/Math` to 154/654 passed with `Math` removed from top missing bindings. The new `math_builtin` benchmark is tracked as a latency and memory exception at `1.59x` and `1.24x`; full property descriptor semantics, remaining Math methods, and faster native-call/property paths remain follow-up work. |
| [x] | Done | Math methods tranche | Compatibility | Add the next standard `Math` method cluster after the basic object surface. | Adds safe `f64`-backed numeric methods: `acos`, `acosh`, `asin`, `asinh`, `atan`, `atan2`, `atanh`, `cbrt`, `cos`, `cosh`, `exp`, `expm1`, `hypot`, `log`, `log10`, `log1p`, `log2`, `sign`, `sin`, `sinh`, `tan`, and `tanh`. Adds direct Rust smoke coverage, engine fixture, active Test262 fixture, 23 upstream Test262 manifest rows, QuickJS differential coverage, and a benchmark. Validation passed with `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T194126Z.md` raises engine fixtures to 62/62, active Test262 to 60/60, QuickJS differential to 58/58, full Test262 passes from 10193 to 10343, and `built-ins/Math` to 282/654 passed. The new `math_methods` benchmark is tracked as a latency exception at `2.28x` while staying within memory budget at `1.00x`; integer coercion methods (`clz32`, `imul`), float32 rounding, `random`, descriptor semantics, `Symbol.toStringTag`, and faster native-call/property paths remain follow-up work. |
| [x] | Done | Math integer methods tranche | Compatibility | Add deterministic remaining `Math` numeric methods before `random` and descriptor work. | Adds `Math.clz32`, `Math.imul`, and `Math.fround`, with shared safe ToUint32 conversion and a locally justified binary32 cast for ECMAScript `fround` semantics. Adds direct Rust smoke coverage, engine fixture, active Test262 fixture, 12 upstream Test262 manifest rows, QuickJS differential coverage, and a benchmark. Validation passed with `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T195229Z.md` raises engine fixtures to 63/63, active Test262 to 61/61, QuickJS differential to 59/59, full Test262 passes from 10343 to 10367, and `built-ins/Math` to 306/654 passed. The new `math_integer_methods` benchmark is tracked as a latency exception at `2.79x` while staying within memory budget at `1.01x`; `Math.random`, descriptor semantics, `Symbol.toStringTag`, and faster native-call/property paths remain follow-up work. |
| [x] | Done | Math random tranche | Compatibility | Add `Math.random` without mixing in descriptor semantics. | Adds a deterministic per-VM xorshift PRNG for `Math.random`, returning numbers in `[0, 1)` without unsafe code or global state. Adds direct Rust smoke coverage, engine fixture, active Test262 fixture, one upstream Test262 manifest row, QuickJS differential property coverage, and a benchmark. Validation passed with `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T200145Z.md` raises engine fixtures to 64/64, active Test262 to 62/62, QuickJS differential to 60/60, full Test262 passes from 10367 to 10371, and `built-ins/Math` to 308/654 passed. The new `math_random` benchmark is tracked as a latency exception at `1.69x` while staying within memory budget at `0.98x`; explicit seeding, stronger PRNG selection, descriptor semantics, `Symbol.toStringTag`, and faster native-call/property paths remain follow-up work. |
| [ ] | Backlog | Diagnostics and error model | Compatibility / embedding API | Make syntax, runtime, host callback, and resource-limit errors precise and stable enough for embedders. | Preserve error chains and report enough location/context data for production logging. |
| [ ] | Backlog | Module loading design | Compatibility / embedding API | Design how modules are resolved, loaded, cached, limited, and observed by embedding applications. | Keep the embedder in control of I/O and policy; document what is intentionally unsupported in the first implementation. |
| [ ] | Backlog | Promise job queue | Compatibility / embedding API | Add the JavaScript job model required by promises and async functions. | The VM owns JavaScript jobs; the embedding application controls when jobs are drained. |
| [ ] | Backlog | Async host callbacks | Embedding API | Allow Rust host callbacks to complete asynchronously through embedder-owned executors. | Should build on the promise job queue and preserve VM isolation, cancellation, and resource accounting. |
| [ ] | Backlog | Resource limit expansion | Resource control | Extend limits from source and runtime counters toward heap, stack, atom table, jobs, host callbacks, modules, and cancellation. | Every new limit needs library tests, error reporting, and teardown accounting. |
| [ ] | Backlog | Observability hooks | Observability | Add structured execution events, profiling hooks, resource usage snapshots, and teardown reports. | Useful for production embedding, debugging, and future performance work. |
| [x] | Done | Full atomization hot path | Runtime architecture / performance | Extend atoms beyond binding names so hot runtime paths stop returning to owned strings. | First architecture tranche moves ordinary object property storage and property order from owned `String` keys to VM-owned atom keys, while preserving string names only at API boundaries for array index parsing, `length`, and `__proto__`. Adds `PropertyKey`, `PropertyLookup`, and `ObjectPropertyInit`, keeps missing property reads from interning new atoms, tracks numeric prototype properties for array index lookup through `sparse_array_keys`, and splits object modules to keep `runtime_object.rs` below the file-size limit. Direct embedding coverage verifies property atom accounting and missing-property behavior. Validation passed with `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T222712Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. Benchmark exceptions remain in object/prototype and `in` paths, so shapes, compiler-assigned property operands, inline caches, and function/native property atomization remain follow-up work. |
| [x] | Done | Well-known property atom cache | Runtime architecture / performance | Stop repeated atom-table string lookup for standard property keys used by built-ins, prototypes, and function metadata. | Adds a VM-local cache for well-known `PropertyKey` values such as `constructor`, `length`, `name`, and `prototype`, routes `intern_property_key` plus `property_lookup` through it before falling back to the atom table, and returns function/native intrinsic `length`, `name`, and `prototype` values before building fallback custom-property lookups. Dynamic property names, JSON keys, and string indices remain on the generic interning path so this stays a narrow atomization tranche before shared shapes and immutable intrinsic metadata. Direct embedding coverage verifies repeated well-known builtin/function/string paths do not grow the atom table after first materialization. Validation passed with `cargo fmt --all -- --check`, targeted embedding and function smoke tests, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T002800Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. Benchmark signal remains tracked runtime-model debt: object/prototype, function custom-property, built-in, binding, and array rows still need shared shapes, compiler-assigned property operands, direct native-call paths, and deeper dense-array mutation fast paths. |
| [x] | Done | Atom table vector index tranche | Runtime architecture / performance | Remove tree-map lookup from the VM atom table before deeper property operands and slot compiler work. | Replaces the internal `BTreeMap<String, AtomId>` with a sorted `Vec<AtomEntry>` plus checked binary search while preserving stable `AtomId` allocation order and `name()` lookup through the existing string storage. Direct smoke coverage verifies out-of-order property-name atoms, repeated property reads without atom growth, missing-property reads without interning, and later insertion of a new property name. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T015816Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. This is a data-structure foundation; `atomized_bindings` remains a tracked latency exception at `1.21x`, so compiler-assigned slots and property operands remain the real hot-path follow-up. |
| [x] | Done | Static AST name operands tranche | Runtime architecture / performance | Stop representing parser-known identifiers and static property names as untyped raw strings before compiler-assigned slots and property operands. | Adds a `StaticName` newtype backed by `Rc<str>` and uses it for declaration names, identifier expressions, function names and parameters, object literal keys, static member names, assignment targets, `new` constructor names, catch bindings, and for-in bindings. Runtime string APIs still receive `&str` at the boundary, and object creation still materializes owned names where enumeration/diagnostics require them. Direct embedding coverage compiles and reuses a script that exercises static binding, function parameter, constructor, `this` member assignment, object method, member call, for-in, and catch-name paths while verifying repeated compiled evaluation does not grow the atom table. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T020845Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. Benchmark signal is unchanged runtime-model debt: `compiled_script_reuse` is `1.24x` latency and `1.12x` memory, and `atomized_bindings` is `1.26x` latency, so `LocalSlot`, `GlobalSlot`, `UpvalueSlot`, and shape/property operands remain the next hot-path work. |
| [x] | Done | Static name table tranche | Runtime architecture / memory | Deduplicate parser-known names within each compiled script before adding binding and property operand resolvers. | Adds a parser-local sorted `StaticNameTable` so repeated identifiers, function parameters, object literal keys, static member names, constructor names, catch bindings, and for-in bindings share the same `StaticName` handle inside the AST instead of allocating separate `Rc<str>` values. `CompiledScriptUsage` now exposes `static_name_count()` so embedders and future resolver tests can observe the script-local name set. Direct embedding coverage verifies that a repeated binding/property/method script compiles to four unique static names and still evaluates correctly. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T022158Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. This is still pre-resolver architecture work: `compiled_script_reuse` remains a tracked exception at `1.21x` latency, and `atomized_bindings` remains `1.23x` latency and `1.10x` memory, so `LocalSlot`, `GlobalSlot`, `UpvalueSlot`, and shape/property operands remain next. |
| [x] | Done | Function property atomization tranche | Runtime architecture / performance | Remove the remaining string-keyed custom property storage from ordinary and native functions. | Converts `FunctionProperties` custom property maps and property order from owned `String` keys to VM-owned `PropertyKey` values shared with object storage. Preserves `name`, `length`, and `prototype` intrinsic behavior at string API boundaries, makes custom ordinary/native function reads use `PropertyLookup` so missing property reads do not intern atoms, and changes the property set path to borrow property names instead of passing owned `String` values. Splits `runtime_function_properties.rs` out of `runtime_function.rs` to keep files under the project line limit, and adds embedding coverage for ordinary and native function property atom accounting. Validation passed with `cargo fmt --all`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T224246Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. Benchmark signal: `function_properties` is within budget at `1.08x` latency and `1.02x` memory, while `function_custom_properties` remains a tracked exception at `1.13x` latency and `1.11x` memory; remaining follow-up work belongs to shapes, compiler-assigned property operands, inline caches, and compact heap strings. |
| [x] | Done | Function property vector index tranche | Runtime architecture / performance | Remove the tree-map storage/index from ordinary and native function custom properties before shapes and direct native-call paths. | Replaces `FunctionProperties` custom-property `BTreeMap<PropertyKey, FunctionProperty>` with a sorted `Vec<FunctionPropertyEntry>` and checked binary search while preserving insertion-order enumeration through `property_order`. Direct function custom-property coverage verifies out-of-order lookup, descriptor reads, deletion/reinsertion, `for...in`, and `in`; intrinsic `name`, `length`, and `prototype` behavior stays separate. Validation passed with `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, targeted function property tests, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T234624Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. Benchmark signal remains shape/native-call debt: `function_properties` is `1.12x`, `function_custom_properties` is `1.15x`, `function_descriptors` is `1.86x`, `function_intrinsic_descriptors` is `2.05x`, `constructor_prototypes` is `1.28x`, and `prototype_constructor_property` is `1.24x`, so shared shapes, prototype versioning, direct native calls, and inline caches remain follow-up work. |
| [x] | Done | Object property slot storage tranche | Runtime architecture / performance | Move ordinary object named properties from map-owned records toward slot-array storage before shared shapes. | Replaces `BTreeMap<PropertyKey, ObjectProperty>` plus a separate property-order vector with `BTreeMap<PropertyKey, PropertySlot>` pointing into checked `Vec<NamedProperty>` storage. Enumeration now walks slot order directly, delete/reinsert compacts and reindexes slots, descriptors and sparse-array fallback use the same slot helpers, and a focused object descriptor smoke test verifies descriptor lookup after slot deletion, reindexing, reinsertion, `Object.keys`, `for...in`, and `in`. This is a shape-layout foundation rather than full hidden classes: objects still own their slot layout, and shared `ShapeId` transitions remain a follow-up. Validation passed with `cargo fmt --all`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T225403Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. Benchmark signal is mixed as expected for a storage refactor without shape caches: `object_literals` and shorthand methods remain within budget, while `object_prototypes`, `object_prototype_root`, `object_builtin`, `object_descriptors`, `for_in`, and `in_operator` still need shared shapes, prototype versioning, compiler-assigned property operands, and inline caches. |
| [x] | Done | Object property vector index tranche | Runtime architecture / performance | Remove the remaining tree-map index from ordinary object named-property lookup before shared shapes. | `Object` already stored named properties in insertion-order `Vec<NamedProperty>` storage; this branch replaces its `BTreeMap<PropertyKey, PropertySlot>` index with a sorted `Vec<PropertyIndexEntry>` and checked binary search. Direct object descriptor coverage verifies out-of-order property lookup, descriptor reads, deletion/reinsertion, insertion-order `Object.keys`, `for...in`, and `in` semantics. This remains a foundation for shared `ShapeId` layouts and inline caches rather than a full hidden-class implementation. Validation passed with `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T233854Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. Benchmark signal remains shape-driven debt: `object_prototypes` is `1.16x`, `object_prototype_root` is `1.34x`, `object_builtin` is `1.29x`, `object_descriptors` is `2.08x`, `for_in_statements` is `1.24x`, and `in_operator` is `1.19x`, so shared shapes, prototype versioning, compiler-assigned property operands, and inline caches remain the next object-path work. |
| [x] | Done | Function parameter atom layout tranche | Runtime architecture / performance | Stop re-interning function parameter names on every ordinary function call. | Ordinary functions now store an `Rc<[AtomId]>` parameter layout when the function object is created, and call-scope creation binds arguments through those atoms instead of interning `String` parameter names at each invocation. Direct embedding coverage verifies that repeated calls do not grow the atom table after function creation. This is a narrow prerequisite for compiler-assigned `LocalSlot`, `GlobalSlot`, and `UpvalueSlot` operands rather than the final slot compiler. Validation passed with `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T230407Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. |
| [x] | Done | Function parameter arity layout tranche | Runtime architecture / memory | Stop retaining source parameter strings in runtime function objects after atom layout creation. | Ordinary runtime functions now store a `FunctionArity` newtype plus the existing `Rc<[AtomId]>` parameter layout instead of retaining the parser's `Rc<[String]>` parameter list. Function `length` still comes from checked arity conversion, and direct embedding coverage verifies `length` plus repeated-call atom accounting. This keeps the AST representation unchanged until compiler-assigned slots and bytecode replace it. Validation passed with `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T231207Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. |
| [x] | Done | Function name atom layout tranche | Runtime architecture / memory | Store ordinary runtime function names as atoms instead of owned strings. | Ordinary functions now store `FunctionName(Option<AtomId>)`; named functions intern their name once at creation, anonymous functions do not intern an empty string, and `name` property reads materialize a JS string only at the API boundary. Direct embedding coverage verifies named and anonymous `name` behavior plus repeated name reads without atom-table growth. Validation passed with `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T232023Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. |
| [x] | Done | Binding scope vector index tranche | Runtime architecture / performance | Remove the remaining tree-map index from runtime binding scopes before compiler-assigned slots. | `BindingScope` still stores cells in a `Vec`, but its atom-to-slot index is now a sorted `Vec<BindingEntry>` with checked binary search instead of a `BTreeMap`. Direct embedding coverage verifies out-of-order declaration lookup, updates, lexical shadowing, function call scope use, and global reads. This remains a foundation for the larger compiler-assigned `LocalSlot`, `GlobalSlot`, and `UpvalueSlot` task rather than the final slot compiler. Validation passed with `cargo fmt --all`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260705T232944Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. Benchmark signal is still a tracked debt: `atomized_bindings` remains `1.15x` latency and `1.11x` memory, so the next real binding win still needs compiler-assigned slots and upvalues. |
| [x] | Done | Static name id operands tranche | Runtime architecture / performance | Give every parser-known name a stable script-local id before adding binding and property operand resolvers. | Adds a checked `StaticNameId` newtype, makes `StaticName` carry both the script-local id and shared text, and changes the parser `StaticNameTable` to keep first-seen name storage separate from the sorted lookup index. Runtime evaluation still uses the existing `as_str()` boundary, so this branch prepares future `LocalSlot`, `GlobalSlot`, `UpvalueSlot`, static property operands, and bytecode operands without changing runtime resolution yet. Direct embedding coverage now verifies repeated out-of-order static names through compiled evaluation. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features`, `cargo test`, targeted static-name tests, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T023251Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. Benchmark debt remains runtime-model driven: `compiled_script_reuse` is a tracked latency exception at `1.10x`, and `atomized_bindings` remains `1.22x`, so compiler-assigned slots are still the next binding-path step. |
| [x] | Done | Static name atom cache tranche | Runtime architecture / performance | Stop repeatedly resolving parser-known static names through string lookup before compiler-assigned slots. | Adds a VM-local lazy `StaticNameId -> AtomId` cache for compiled-script evaluation, stores the cache with escaped functions, and uses a compact `Rc<[Cell<Option<AtomId>>]>` slot array so hot static-name access does not take locks. Binding identifiers, assignments, declarations, function names, function parameters, constructor names, and static property operands now resolve through the cache while missing-name lookups still do not intern atoms. Direct coverage verifies missing compiled identifiers, repeated compiled static-name paths, and escaped compiled functions after the original eval frame is gone. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features`, `cargo test`, targeted compiled-static-name tests, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T025217Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. Benchmark exceptions improved from the previous local report to 39 latency and 19 memory rows; `atomized_bindings` is down to `1.12x`, while `compiled_script_reuse` remains a near-threshold tracked latency exception at `1.10x`, so full `LocalSlot`, `GlobalSlot`, and `UpvalueSlot` operands remain next. |
| [x] | Done | Static binding slot cache tranche | Runtime architecture / performance | Route repeated parser-known binding access through checked binding-slot locations before the full scope resolver lands. | Adds a script-local `StaticNameId -> BindingLocation` cache for compiled evaluation, with guarded global/local `BindingSlot` locations that validate the current lexical stack, slot atom, and shadowing before reading or assigning. `BindingScope` keeps direct slot access safe through a compact parallel slot-atom vector, compiled functions capture the binding cache alongside the atom cache, and declaration hoisting seeds locations after bindings are created. This is intentionally not the final compiler-assigned scope layout: validation still falls back to runtime resolution when scopes change, and full `LocalSlot`, `GlobalSlot`, and `UpvalueSlot` operands remain the next binding-path task. Direct coverage verifies block shadowing, captured parameter shadowing, missing compiled names without atom growth, and escaped compiled functions. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features`, `cargo test`, targeted compiled-static-name tests, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T030638Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. Benchmark signal remains binding-runtime debt rather than compile debt: `function_parameters_scope` and `closure_environments` are within budget, `atomized_bindings` is a latency exception at `1.12x` with memory within budget, and `compiled_script_reuse` is a latency exception at `1.15x`. |
| [x] | Done | Static binding occurrence operands tranche | Runtime architecture / performance | Give every parser-known binding use its own checked operand id before assigning compile-time local/global/upvalue slots. | Adds `StaticBindingId` and `StaticBinding` so identifier reads, assignments, declarations, parameters, catch bindings, for-in bindings, shorthand object reads, and `new` constructor lookups no longer share one cache slot only because their text matches. `CompiledScriptUsage` now reports `static_binding_count`, runtime static-binding caches are sized and indexed by binding occurrence rather than `StaticNameId`, and parser speculation for ordinary `for` loops now restores static-name and static-binding tables when the `for-in` parse path does not match. Property keys and function display names remain `StaticName` operands because they are not binding lookups. Direct coverage verifies deduplicated static names with distinct binding occurrences and the `for` parser checkpoint. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features`, `cargo test`, targeted static-name/static-binding tests, targeted compiled-static-name tests, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T032116Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. Benchmark signal remains runtime-model debt: `atomized_bindings` is a latency and memory exception at `1.16x` and `1.10x`, `compiled_script_reuse` is a latency exception at `1.16x`, and the next binding-path step is still assigning `LocalSlot`, `GlobalSlot`, and `UpvalueSlot` from these occurrence operands. |
| [x] | Done | Compiled binding layout tranche | Runtime architecture / performance | Build a scope-aware binding operand table before replacing runtime binding lookup with direct slot access. | Adds a separate compiled binding layout pass behind `CompiledScript`. The pass walks the parsed AST after parsing, builds lexical/function scope metadata, assigns checked `GlobalSlot`, `LocalSlot`, and `UpvalueSlot` operands to `StaticBindingId` occurrences, keeps unresolved external names explicit, and exposes slot/unresolved counts through `CompiledScriptUsage`. `eval_compiled` now sizes the static binding cache from the compiled layout rather than from parser usage counters, while runtime reads and writes intentionally keep the existing guarded lookup semantics until the next direct-slot tranche. Direct coverage verifies global/local/upvalue counts for closures, shadowed lexical slots, and unresolved bindings. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features`, targeted binding-layout tests, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T034122Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. Benchmark signal confirms this is a layout foundation rather than the direct-slot speedup: 40 latency rows and 23 memory rows remain over budget, `function_parameters_scope` and `closure_environments` are within budget, `compiled_script_reuse` is a latency exception at `1.19x`, and `atomized_bindings` remains a latency and memory exception at `1.12x` and `1.10x`. The follow-up is to use this layout to create local/global/upvalue frames directly, then retire ordinary runtime shadowing scans from hot binding access. |
| [x] | Done | Binding layout cache seeding tranche | Runtime architecture / performance | Use compiled binding operands to widen the existing safe runtime binding cache without assuming layout slot order matches runtime insertion order. | `Context` now carries the active `BindingLayout` alongside compiled static-name and binding caches, escaped functions capture the same layout, and runtime binding resolution seeds every `StaticBindingId` occurrence with the same compiled operand once the actual runtime `BindingLocation` is known. Function parameter bindings seed inside the function's own cache/layout context, and lexical `for...in` bindings seed after their per-iteration scope is active. This keeps existing guarded lookup validation and fallback semantics while reducing repeated same-operand cache misses. Direct coverage verifies repeated compiled `for...in` lexical bindings and cross-eval function parameter/name atom accounting. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features`, `cargo test`, targeted binding/static-name/for-in tests, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T035848Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. Benchmark signal remains runtime-model debt: 40 latency rows and 23 memory rows remain over budget, `compiled_script_reuse` is `1.21x`, and `atomized_bindings` is `1.19x` latency plus `1.13x` memory, so direct runtime frames and slot operands remain the next binding-path step. |
| [x] | Done | Function parameter frame slots tranche | Runtime architecture / performance | Start building runtime binding frames from compiled local-slot layout instead of relying only on insertion order. | `BindingLayout` now exposes checked binding operands for runtime frame construction, `BindingScope` can insert or replace a cell at an explicit checked `BindingSlot`, and ordinary function call scopes populate parameters at their compiled `LocalSlot` offsets when layout metadata is available. Static binding cache entries now distinguish guarded locations from layout-backed exact local/upvalue locations, allowing local/upvalue cache hits to skip shadowing scans while keeping global hits guarded. An initial broader exact-global attempt reduced full Test262 by six Annex B language cases, so this tranche deliberately leaves global cache validation conservative until global frame construction models those cases explicitly. Direct coverage verifies compiled function parameter frame slots with shadowing and repeated compiled eval atom accounting. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features`, targeted binding/static-name/embedding tests, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T041631Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. Benchmark signal is mixed but directionally useful for binding-heavy rows: `function_parameters_scope` and `closure_environments` remain within budget at `0.91x`, `atomized_bindings` is a latency exception at `1.14x`, and `compiled_script_reuse` remains a latency exception at `1.21x`. |
| [x] | Done | Lexical declaration frame slots tranche | Runtime architecture / performance | Extend compiled local-slot frame construction from function parameters to runtime lexical declaration paths. | `BindingScope` now has a best-effort optional-slot insertion path for compiled local slots, and `Context` exposes checked `StaticBinding` to `LocalSlot` lookup from the active `BindingLayout`. Ordinary `let` and `const` declarations, lexical `for...in` bindings, and catch parameter scopes try their compiled `LocalSlot` first, then fall back to ordinary runtime insertion when the current scope does not match the compiled frame shape. Strict slot insertion remains reserved for function parameters, where frame construction must match the layout. Direct coverage verifies function body locals after parameter slots, lexical `for...in` reuse, and catch/body lexical slot accounting across repeated compiled evaluation. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features`, targeted binding/block/for-in/control-flow/embedding tests, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T042711Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. Benchmark debt remains runtime-model work: 40 latency and 23 memory rows are tracked exceptions, `compiled_script_reuse` is `1.22x`, and `atomized_bindings` is `1.17x`, so hoisted vars, globals, closure cells, and direct slot operands remain future work. |
| [x] | Done | Hoisted var frame slots tranche | Runtime architecture / performance | Extend compiled local-slot frame construction from lexical declarations to function-local hoisted `var` declarations. | `hoist_var` now tries the active compiled `LocalSlot` when hoisting into a local function frame, using the same optional-slot fallback path as lexical declarations. Global `var` declarations deliberately keep ordinary insertion because the current global `BindingScope` also contains built-ins, while `GlobalSlot` metadata only describes script declarations. Direct coverage verifies a function parameter, two hoisted `var` declarations including one from a nested block, and a block lexical declaration sharing the compiled local-slot layout across repeated compiled evaluation. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features`, targeted binding/block/language/embedding/static-name tests, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T043536Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. Benchmark debt remains tracked: 39 latency and 24 memory rows are exceptions, `compiled_script_reuse` is `1.19x`, and `atomized_bindings` is `1.17x` latency plus `1.11x` memory, so global frames, closure cells, and direct slot operands remain future work. |
| [x] | Done | User global frame slots tranche | Runtime architecture / performance | Separate user global bindings from lazy builtin bindings so compiled `GlobalSlot` frame construction can be used safely. | `Context` now owns separate `globals` and `builtin_globals` scopes. Lazy built-ins materialize into `builtin_globals`, user/local lookup shadows built-ins before fallback, and declaration paths use the active compiled layout so top-level `var`/`let`/`const` can best-effort populate `GlobalSlot` offsets when compatible. This unblocks a stable user global frame without allowing late builtin materialization to shift script-declared global slots, while preserving builtin shadowing and numeric constant behavior. The branch also splits `runtime_globals.rs` out of `runtime.rs` to keep the runtime file below the 800-line limit. Direct coverage verifies `compiled_global_slots_are_separate_from_builtins` after materializing `Number`, `Array`, and `Object` first. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, targeted global/builtin/binding tests, `cargo test`, `cargo clippy --all-targets --all-features`, `git diff --check`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T052501Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 39 latency and 24 memory exceptions; `compiled_script_reuse` is `1.23x`, `atomized_bindings` is `1.13x` latency plus `1.12x` memory, and `function_parameters_scope` remains within budget at `0.91x`. Exact global read/write operands, closure cells, and direct slot operands remain future work. |
| [x] | Done | Direct global binding operand tranche | Runtime architecture / performance | Use compiled binding operands earlier in the runtime binding path without broadening exact global semantics. | `get_binding_static` now tries a guarded `GlobalSlot` location directly from the active `BindingLayout` before falling back to runtime atom-to-location search, and declaration cache seeding can build active global or active local locations from compiled slots before using the generic resolver. Compound assignment and update expressions now keep the resolved `BindingCell` and assign through it after computing the new value, avoiding a second binding lookup while preserving mutability checks. Direct coverage verifies compiled global slot operands across reads, `+=`, `++`, ordinary writes, constructor lookup through `new`, and repeated compiled evaluation without atom growth. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, targeted binding-layout tests, `cargo test`, `cargo clippy --all-targets --all-features`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T053820Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmark signal remains noisy runtime-model debt rather than a closed optimization: 63 rows measured, 41 latency and 27 memory exceptions, `compiled_script_reuse` at `1.22x`, `atomized_bindings` at `1.17x` latency plus `1.14x` memory, and `function_parameters_scope` within budget at `0.92x`. Local scope-id frame metadata, closure/upvalue cells, bytecode operands, and exact global cache validation remain future work. |
| [x] | Done | Runtime local scope frame metadata tranche | Runtime architecture / performance | Map compiled local scope ids onto runtime binding frames so `LocalSlot` operands can be used without confusing same-slot lexical scopes. | `BindingScope` now records an optional compiled `ScopeId`, declaration, function parameter, lexical `for...in`, and catch frame creation mark it only after insertion into the expected compiled slot, and `get_binding_static` can resolve first local cache misses through `ScopeId + LocalSlot` before falling back to atom search. Direct coverage verifies same-slot block scopes and repeated compiled evaluation without atom growth. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, targeted binding-layout tests, `cargo test`, `cargo clippy --all-targets --all-features`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T055443Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 32 latency and 25 memory exceptions; `function_parameters_scope` remains within budget at `0.82x`, while `for_in_statements` remains a tracked latency exception at `1.19x`. Closure/upvalue cells, direct write/update/constructor operands, bytecode operands, and exact global cache validation remain future work. |
| [x] | Done | Closure upvalue frame cells tranche | Runtime architecture / performance | Give escaping functions compact captured-cell frames keyed by compiled `UpvalueSlot` before removing legacy captured scope snapshots. | Function creation now walks the current function body, skips nested function bodies, materializes a dense `UpvalueSlot -> BindingCell` frame for resolvable captured variables, and function calls push that frame as the active upvalue frame. `get_binding_static` now resolves first upvalue cache misses through `UpvalueSlot` before falling back to ordinary runtime location search. Direct coverage verifies two closure instances with independent captured cells and repeated compiled evaluation without atom growth. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, targeted binding-layout tests, `cargo test`, `cargo clippy --all-targets --all-features`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T060808Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 38 latency and 26 memory exceptions; `closure_environments` remains within budget at `0.93x`, while `atomized_bindings` remains a tracked latency exception at `1.20x`. Legacy captured scope snapshots remain as fallback and should be shrunk in a later memory-model tranche. |
| [x] | Done | Static binding materialization guard tranche | Runtime architecture / performance | Avoid lazy builtin string fallback when an existing compiled/static binding cell is already available. | Assignment, compound assignment, `delete identifier`, and `new` constructor lookup now route through static binding-cell lookup before attempting lazy builtin materialization, so existing global, local, and upvalue cells do not pay the builtin name-match path. The old unconditional `materialize_builtin_binding` helper is removed. Direct coverage verifies global, local, and upvalue assignment/compound/update behavior across repeated compiled evaluation without atom growth. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, targeted binding-layout tests, `cargo test`, `cargo clippy --all-targets --all-features`, `git diff --check`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T062142Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 41 latency and 24 memory exceptions; `atomized_bindings` remains a latency exception at `1.17x`, `compiled_script_reuse` remains a latency exception at `1.21x`, and `function_parameters_scope` plus `closure_environments` remain within latency budget at `0.99x` and `0.94x`. |
| [x] | Done | Closure capture snapshot shrink tranche | Runtime architecture / memory | Stop carrying full legacy local-scope snapshots when compact upvalue frames already prove enough for a compiled closure. | Adds a `FunctionCaptures` wrapper so compiled functions with complete direct upvalue cells can store an empty legacy capture snapshot, while functions with nested function bodies keep the fallback snapshot until transitive upvalue lifting is implemented. `VmResourceUsage` now reports captured scope count, captured binding count, and upvalue cell count so closure memory shape is visible to embedders. Direct coverage verifies simple escaping closures use compact upvalue cells without legacy snapshots and keeps the existing three-level closure chain passing through the conservative fallback. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, targeted closure/binding-layout tests, `cargo test`, `cargo clippy --all-targets --all-features`, `git diff --check`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T063700Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 41 latency and 24 memory exceptions; `closure_environments` stays within latency budget at `0.93x` but remains a memory exception at `1.11x`, so transitive upvalue lifting and more compact closure cells remain future work. |
| [x] | Done | Static object literal borrowed keys tranche | Runtime architecture / memory | Remove an owned-string allocation from the static object literal creation path while preserving atom-backed property storage. | `ObjectHeap::create` now accepts `ObjectPropertyInit<'_>` so parser-known object literal property names are passed as borrowed `&str` values while ordinary object storage still uses atom-backed `PropertyKey` and shape metadata. Direct embedding coverage verifies repeated compiled object literal evaluation, reused property atoms, and `__proto__` literal prototype behavior. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features`, targeted embedding smoke coverage, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T044715Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 42 latency and 21 memory exceptions; `object_literals` is within latency budget at `0.90x` but remains a memory exception at `1.11x`, so remaining string work belongs to dynamic string keys, `StringId`, compact values, and bytecode/property operands. |
| [ ] | Backlog | Compiler-assigned slots and upvalues | Runtime architecture / performance | Replace repeated runtime name lookup for locals, globals, and captured variables with checked slot indices. | The compiled `BindingLayout` now exists, seeds matching runtime binding-cache occurrences after a safe runtime location is known, populates function parameter frames at compiled local slots, best-effort places lexical declaration paths at compiled local slots, best-effort hoists function-local `var` declarations into compiled local slots, can best-effort place user top-level declarations into compiled `GlobalSlot` offsets, assigns hoisted global `var` slots before lexical globals to match runtime hoisting, uses guarded `GlobalSlot` locations for first global cache misses, maps compiled local `ScopeId` metadata onto compatible runtime frames, materializes compact upvalue frames for escaping functions, lifts transitive upvalues through intermediate closures, stores atom-free exact cache locations for proven compiled local/upvalue hits, stores atom-free exact global cache locations only after runtime slot/atom validation, reuses already resolved binding cells for compound/update writes, avoids builtin materialization fallback when an existing static binding cell is available, and skips legacy capture snapshots for simple and nested compiled closures whose lifted upvalue cells are complete. The next steps are true direct slot operands for the remaining read/write/eval paths and bytecode-ready binding operands. Keep best-effort fallback for dynamic or mismatched runtime scopes. |
| [x] | Done | Prototype chain traversal metadata tranche | Runtime architecture / performance | Centralize prototype traversal before shared shapes and inline caches. | Moves prototype-chain lookup, `in`, `__proto__`, and cycle checks into `runtime_object_prototype.rs`, replacing per-lookup visited-vector allocation with a checked traversal budget based on VM object count. Adds a deep prototype-chain smoke test while preserving existing cycle rejection behavior. This is a foundation for later prototype-version guards and `ShapeId` caches, not the full shape implementation; the `Shape-based object and prototype layout` backlog remains open. Validation passed with `cargo fmt --all -- --check`, targeted object/prototype/`in`/`for...in` tests, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T003559Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. Benchmark signal remains tracked runtime-model debt: `object_prototypes` is still over latency and memory budget, and `object_prototype_root`, `object_builtin`, `for_in_statements`, and `in_operator` still need shared shapes, prototype versioning, compiler-assigned property operands, and inline caches. |
| [x] | Done | Shape id layout metadata tranche | Runtime architecture / performance | Give ordinary objects stable VM-local shape identity before property offset caches and prototype guards. | Adds `ShapeId` and a VM-owned `ShapeTable`, records shape transitions on ordinary named-property add/remove paths including descriptors, sparse array fallback, data objects, string wrappers, and built-in prototypes, and exposes `shape_count` in `VmResourceUsage` for embedding-side resource snapshots. Direct embedding coverage verifies that matching object property layouts reuse shapes and that adding a property creates a new layout. Validation passed with `cargo fmt --all -- --check`, targeted embedding/object/array tests, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T005608Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. This is still metadata-only: shared slot offsets, prototype-version guards, descriptor-aware shape compatibility, and inline caches remain follow-up work. |
| [x] | Done | Shape property offset tranche | Runtime architecture / performance | Move ordinary object named-property lookup from per-object indexes to shared shape-owned offsets. | Replaces the remaining per-object `PropertyIndexEntry` with `ShapePropertyOffset` metadata owned by `ShapeTable`, so property read, write, descriptor, sparse-array fallback, prototype, `in`, and enumeration paths resolve named-property slots through the object's `ShapeId`. Direct embedding coverage now verifies shared layout reuse after deleting a middle property, which exercises offset compaction for the remaining properties. Validation passed with `cargo fmt --all`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T010724Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. Benchmark signal remains tracked runtime-model debt: object/prototype paths still need prototype-version guards, descriptor-aware shapes, compiler-assigned property operands, and inline caches. |
| [x] | Done | Prototype lookup version tranche | Runtime architecture / performance | Add an explicit VM-local guard for future prototype-chain inline caches. | Adds a checked `PrototypeLookupVersion` to `ObjectHeap`, exposes it in `VmResourceUsage`, and bumps it when object structure or `__proto__` changes through central object, descriptor, and array-index mutation paths. Direct embedding coverage verifies that prototype assignment, property add, and property delete advance the version while value-only writes keep it stable. Validation passed with `cargo fmt --all`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T011917Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. This is a coarse safe invalidation guard; later inline caches can refine it with descriptor-aware shapes and per-site cache records. |
| [x] | Done | Descriptor-aware shape layout tranche | Runtime architecture / performance | Make ordinary object shapes distinguish property descriptor attributes, not only key order. | Shapes now store per-slot data-property attributes together with insertion-order keys, reuse layouts only when writable/enumerable/configurable metadata matches, and transition existing object shapes when `Object.defineProperty` or enumerable updates change attributes. Direct descriptor coverage verifies same-attribute layout reuse, different-attribute layout separation, and descriptor update back to an existing compatible shape. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T013421Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. This prepares cacheable lookup APIs and inline caches while preserving current descriptor semantics. |
| [x] | Done | CallValue inline-cache tranche | Runtime architecture / performance / observability | Cache arbitrary bytecode value-call dispatch after direct binding/property/native operands so returned JS, native, and host functions no longer re-run generic callable classification on warm call sites. | Adds parser-assigned `StaticCallSiteId` operands, carries them through `Expr::Call`, `BytecodeCallSite`, and `CallValue`, and stores a monomorphic per-site cache in the compiled-script cache handle. Warm sites guarded by the current callee id dispatch directly to JS functions, native function kinds, or host functions; stale, changed, or non-callable values fall back through the generic call path. `CompiledScriptUsage` now exposes `static_call_site_count()`, and `VmResourceUsage` exposes `call_value_cache_hits`, `call_value_cache_misses`, and `call_value_cache_slow_paths` separately from native property-call counters. Direct coverage in `call_value_inline_cache_smoke.rs` verifies repeated JS, native, and host function-valued calls, callee mutation fallback, static call-site counts, and resource counter growth. The root engine crate version was bumped to `0.2.23`; the runner crate was unchanged. Validation passed with `cargo fmt --all`, `cargo check --all-targets`, targeted call-value cache coverage, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test`, file-size inspection, and `scripts/test-all.sh` using local Test262 and QuickJS caches after rebasing onto `3ec3a16`. Report `rsqjs-test-report-20260706T230625Z.md` keeps engine fixtures at 68/68, active Test262 at 70/70, full Test262 at 11647/102578 with no skipped cases, and QuickJS differential at 64/64. Benchmarks measured 63 rows with aggregate performance `0.80x` and 40 tracked latency exceptions; memory-budget ratios remain unavailable for this report mode. Remaining inline-cache work is polymorphic call dispatch, descriptor-aware writes, dense-array loop sites, and method-specific native loops. |
| [x] | Done | Cacheable property lookup API tranche | Runtime architecture / performance | Give future inline caches a checked shape/prototype lookup snapshot for named object and prototype properties. | Adds `runtime_object_lookup.rs` with `PropertyLookupGuard`, cacheable hit/miss/uncacheable results, receiver `ShapeId`, owner `ShapeId`, slot offset, prototype depth, and VM-local prototype lookup version validation. Object/prototype `get` and `in` paths now try this cacheable named-property path first and fall back to the generic path for array `length`, dense array indices, non-atomized property lookups, and stale guards. Direct smoke coverage verifies own and prototype named reads, `in`, structure-version changes after shadow/delete, and array-specific fallback semantics. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T014403Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. Benchmark debt remains for object/prototype/descriptor paths because this branch adds the safe cache boundary, not bytecode or per-site inline cache storage. |
| [ ] | Backlog | Shape-based object and prototype layout | Runtime architecture / performance | Move ordinary objects and prototypes toward shared shapes plus slot storage instead of per-object string-keyed maps. | `ShapeId`, `ShapeTable`, add/remove transitions, shared property offsets, prototype lookup versioning, descriptor-aware shape compatibility, and cacheable named-property lookup snapshots now exist. Next add compiler-assigned property operands, object/prototype-heavy built-in migration coverage, and eventual per-site inline caches. |
| [x] | Done | Array storage foundation tranche | Runtime architecture / performance | Move array element state into a dedicated storage layer before adding method-specific fast paths. | Adds `ArrayStorage` with packed and holey dense element variants plus sparse index-key tracking, routes ordinary set/delete, descriptor, lookup, and enumeration paths through it, and adds smoke coverage for packed-to-holey transitions. This is a structural prerequisite, not the final fast path: `concat`, `slice`, `includes`, `indexOf`, `lastIndexOf`, `join`, `reverse`, `shift`, and `unshift` still need guarded specialized implementations. |
| [x] | Done | Packed array read/copy fast paths | Runtime architecture / performance | Use the new array storage boundary for guarded packed-array operations before broader array rewrites. | Adds packed full-length guards for `includes`, `indexOf`, `lastIndexOf`, `join`, `slice`, and `concat`, while leaving holey, sparse, descriptor-sensitive, and prototype-index cases on the generic path. Validation passed with `cargo fmt --all -- --check`, targeted array smoke tests, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T001352Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, QuickJS differential at 64/64, and full Test262 at 10872/102578. Benchmark signal remains tracked debt: packed read/copy paths reduce some compiled-eval work, but array methods still need deeper storage and mutation fast paths to reach the 1.00x budget. |
| [x] | Done | Packed array reverse fast path | Runtime architecture / performance | Use `ArrayStorage` for guarded in-place reverse on simple packed arrays. | Adds a full-length packed reverse path for arrays whose dense elements all have default writable, enumerable, and configurable data attributes. Holey arrays, sparse indices, prototype-index behavior, and descriptor-modified dense elements stay on the generic reverse path. Direct smoke coverage verifies the descriptor fallback by reversing an array with a non-writable dense element. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, targeted reverse smoke coverage, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T045624Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 39 latency and 24 memory exceptions; `array_prototype_reverse` remains a tracked exception at `1.68x` latency and `1.10x` memory, so shift/unshift, deeper array storage, and prototype-index guards remain future work. |
| [x] | Done | Packed array deque front operations | Runtime architecture / performance | Avoid full dense element moves for simple packed `shift` and `unshift` calls while preserving descriptor and sparse fallback behavior. | `ArrayStorage::Packed` now uses `VecDeque<ObjectProperty>`, `Array.prototype.shift` uses a guarded `pop_front`, and `Array.prototype.unshift` uses guarded `push_front` for full-length packed arrays whose dense elements all have default writable, enumerable, and configurable data attributes. Descriptor-modified, holey, sparse, and prototype-index-sensitive arrays remain on the generic path; slice-style fast paths require a contiguous deque slice and otherwise fall back. Direct smoke coverage verifies the descriptor fallback for shift/unshift. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, targeted shift/unshift smoke coverage, `cargo clippy --all-targets --all-features`, `cargo test`, and `scripts/test-all.sh`; report `rsqjs-test-report-20260706T051059Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 39 latency and 19 memory exceptions; `array_prototype_shift_unshift` remains a tracked latency exception at `1.63x` with memory within budget at `1.09x`, so the next array work should focus on interpreter dispatch, prototype-index guards, and broader mutation loops rather than only front storage. |
| [x] | Done | Packed array contiguous storage tranche | Runtime architecture / performance | Restore packed arrays to contiguous storage now that method loops need stable slices more than deque front operations. | Replaces `ArrayStorage::Packed(VecDeque<ObjectProperty>)` with `Vec<ObjectProperty>`, so full-length packed guards always expose a contiguous slice and no longer fall back after front deque fragmentation. Packed append, copy, pop, reverse, shift, unshift, dense insert, and dense delete paths now use `Vec` operations while preserving holey/sparse fallback semantics, descriptor guards, property counts, and safe checked limits. Benchmark-only repeat on the branch measured 63 rows with 41 latency exceptions; array rows were directionally better than the paired main benchmark-only run in absolute rsqjs latency (`array_prototype_concat` 578.30 us versus 598.48 us, `array_prototype_includes` 396.28 us versus 529.02 us, `array_prototype_slice` 427.11 us versus 442.15 us), but full report validation remains the merge gate. |
| [ ] | Backlog | Dense array fast paths | Runtime architecture / performance | Continue guarded packed, holey, and sparse array fast paths now that storage has a separate owner. | Read/copy operations, bulk packed construction, packed concat result construction, contiguous packed storage, default-attribute reverse, default-attribute shift/unshift, static numeric bytecode index reads/writes, and dynamic computed array index reads/dense writes have initial paths. Continue reducing measured array debt in `concat`, `slice`, `includes`, `indexOf`, `lastIndexOf`, `join`, `reverse`, `shift`, and `unshift` with storage-level method loops, broader prototype-index guards, descriptor-aware mutation paths, fewer `Value`/property clones, bytecode/native-loop dispatch, and explicit materialization points. Preserve fallback semantics when holes, sparse indices, descriptors, or prototype index properties can affect reads or writes. |
| [ ] | Backlog | Built-in intrinsic metadata and native-call fast paths | Runtime architecture / performance | Reduce repeated construction, lookup, and call overhead for built-in constructors, prototypes, and native functions. | Constructors and methods now register through a checked VM-local native function registry, native-function ids no longer require a linear kind scan, read-only primitive/Object/Array/Math/JSON helper paths can inspect borrowed `RuntimeCallArgs` without cloning complete argument vectors, static/computed member native-call sites cache validated native-function kinds with hit/miss/slow-path resource counters, unresolved immutable numeric globals can load directly without materializing builtin bindings when shadow guards prove that no user binding is visible, and unresolved callable builtin globals can dispatch through VM-owned metadata in `CallBinding` without reading the builtin binding cell under the same shadow guard. Next share the remaining immutable intrinsic metadata, lazily materialize mutable JS objects per VM, pre-resolve atoms and shapes, reuse descriptor layouts, and add method-specific bytecode/native loops for common built-ins without mutable global JavaScript state. |
| [ ] | Backlog | VM-owned heap accounting and GC | Resource control / runtime architecture | Define mark/sweep or reference-counting plus cycle collection over indexed VM heaps. | Must preserve deterministic teardown, hard heap limits, host callback handles, queued jobs, and VM isolation. |
| [x] | Done | Bytecode VM foundation tranche | Runtime architecture / performance | Put compiled execution on a generic bytecode interpreter behind the stable `CompiledScript` API before specializing hot instructions. | Adds a generic `BytecodeProgram` stored in `CompiledScript`, exposes `CompiledScriptUsage::bytecode_instruction_count()`, and routes `eval_compiled` through `Context::eval_bytecode_program` after the existing `var` hoist phase. The first instruction set covered top-level literals, script-local strings, `this`, identifiers, declarations, assignment, property reads/writes, object and array literals, binary/unary operators, conditionals, and selected loops. Layout-backed local/upvalue cache hits also run before static-name atom lookup when the active compiled frame proves the slot is valid. Direct bytecode smoke coverage verifies instruction counts, repeated compiled binding loops without atom growth, and property/array/object bytecode paths. The root engine crate version was bumped to `0.2.3`; the runner crate was unchanged. Validation passed with `scripts/test-all.sh` after rebasing onto `19411cb`, which also verifies the runner uses this nested worktree through the explicit Cargo `paths` override. Report `rsqjs-test-report-20260706T161206Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578 with no skipped cases, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 37 latency exceptions and no memory-budget ratios in the current in-process mode; aggregate performance is `0.70x`. Later tranches broadened bytecode coverage to structured control flow, calls, constructors, and functions. |
| [x] | Done | Canonical compiled bytecode tranche | Runtime architecture / performance / memory | Stop retaining the complete parsed top-level AST in `CompiledScript` and make bytecode own the compiled-script execution metadata it needs. | Adds `BytecodeHoistPlan`, stores it inside `BytecodeProgram`, and routes compiled-script top-level `var` hoisting through that plan instead of `CompiledScript::program().statements`. `CompiledScript` no longer stores the full `Program`; `bytecode.rs` was split into `bytecode_hoist.rs` and later bytecode type/compiler helper modules to keep files below the project size limit. Direct bytecode smoke coverage verifies bytecode-owned hoisting for a non-executed `var` declaration. The root engine crate version was bumped to `0.2.4`; the runner crate was unchanged. Validation passed with `scripts/test-all.sh`; report `rsqjs-test-report-20260706T164401Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10872/102578 with no skipped cases, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 37 latency exceptions and no memory-budget ratios in the current in-process mode; aggregate performance is `0.70x`. Later cleanup moved function bodies to `BytecodeFunction`, replaced expression-argument call APIs with value-stack call frames, and removed the obsolete runtime AST interpreter modules. |
| [x] | Done | Bytecode-first runtime cleanup tranche | Runtime architecture / performance / memory | Remove the remaining AST runtime fallback paths so execution runs through bytecode-owned top-level and function metadata. | Deletes the obsolete AST interpreter modules, removes the `EvalAst*` bytecode instruction family, moves bytecode types into a dedicated module, and compiles user function bodies into `BytecodeFunction` with bytecode-owned hoist plans and capture metadata instead of retaining `Rc<[Stmt]>` bodies. Runtime calls now use value-stack `RuntimeCallArgs` instead of expression-backed argument evaluation, and user functions execute their bytecode blocks through the VM. Direct bytecode smoke coverage verifies structured control-flow execution, function-local hoisting, and closure upvalue capture without AST bodies. The root engine crate version was bumped to `0.2.5`; the runner crate was unchanged. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --quiet`, a slow-path terminology search over `src`, `tests`, and `docs`, and a post-rebase `scripts/test-all.sh` run. Report `rsqjs-test-report-20260706T175913Z.md` keeps engine fixtures at 68/68, active Test262 at 66/66, full Test262 at 10890/102578 with no skipped cases, and QuickJS differential at 64/64. Benchmarks measured 63 rows with 41 tracked latency exceptions, aggregate performance `0.77x`, and no failed benchmark rows in the current in-process mode; memory-budget ratios remain unavailable for this report mode. Remaining bytecode work is not AST fallback removal: it is direct binding/property/native-call operands, quickening, async/job integration, modules, and tighter resource accounting. |
| [x] | Done | Bytecode architecture boundary hardening tranche | Runtime architecture / tests | Make AST runtime fallback removal enforceable so future work cannot quietly reintroduce parser-AST execution surfaces. | Adds an integration guard that scans runtime, embedding, storage, value, and compiled-script layers for parser AST imports, keeping AST usage restricted to the parser, binding analysis, and bytecode compiler boundary. Renames `RuntimeCallArgs::evaluate()` to `to_owned_values()` so function and host callback boundaries describe the real bytecode-owned argument flow instead of AST-era expression evaluation. Updates the bytecode roadmap language to use bytecode regression tests as the execution oracle rather than preserving an interpreter fallback. The root engine crate version was bumped to `0.2.68`; the runner crate was unchanged. Validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, targeted boundary coverage, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --quiet`, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260707T073034Z.md` keeps engine fixtures at 68/68, active Test262 at 71/71, full Test262 at 14568/102578 passed, and QuickJS differential at 64/64. Benchmarks measured 63 rows with aggregate performance `0.80x` and 40 tracked latency exceptions. |
| [x] | Done | Bytecode/compiler module boundary tranche | Runtime architecture / tests | Make the code layout match the bytecode-first architecture by moving AST traversal out of the `bytecode` module. | Splits AST-dependent bytecode compilation into a dedicated `compiler` module, keeps `bytecode` as AST-free VM executable metadata plus metrics, and routes `CompiledScript` through `compiler::compile_program` after parsing and binding-layout analysis. `BytecodeHoistPlan` is now bytecode-owned data with compiler-owned construction, so runtime hoisting still uses bytecode metadata without importing parser AST. The bytecode architecture guard now also scans `src/bytecode`, preventing future reintroduction of AST imports into the bytecode data layer. The rebase preserved current empty-statement and RegExp-literal compiler coverage from main. This does not remove the parser AST front-end; it removes the misleading mixed module boundary and leaves a later direct parser-to-compiler-IR pipeline as an explicit larger redesign. The root engine crate version was bumped to `0.2.74`; validation passed with `cargo fmt --all -- --check`, `cargo check --all-targets`, targeted boundary coverage, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --all-targets --quiet`, `git diff --check`, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260707T085719Z.md` keeps engine fixtures at 68/68, active Test262 at 71/71, full Test262 at 14337/102578 passed, QuickJS differential at 64/64, and benchmark rollup at 0.87x with 42 tracked latency exceptions. |
| [x] | Done | Bytecode-only architecture finalization guard | Runtime architecture / tests | Make the no-AST-runtime boundary repository-wide and remove stale roadmap language that still permits AST-backed compiled execution. | Extends the architecture guard from selected runtime directories to all Rust source files, with parser AST imports allowed only in `src/ast`, `src/parser`, `src/compiler`, and `src/binding_layout`. It also rejects obsolete AST execution markers such as `EvalAst`, `eval_ast`, `AstInterpreter`, and `RuntimeCallArgs::evaluate` in source, and updates roadmap text so remaining generic fallbacks are described as bytecode/runtime semantic paths rather than parser-AST execution fallback. Validation passed with `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --all-targets --quiet`, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260707T093159Z.md` keeps engine fixtures at 68/68, active Test262 at 71/71, full Test262 at 14462/102578 passed, QuickJS differential at 64/64, and benchmark rollup at 0.87x with 42 tracked latency exceptions. |
| [x] | Done | Bytecode AST boundary hardening | Runtime architecture / tests / docs | Make the answer to AST fallback cleanup enforceable: execution is bytecode-only, while parser AST remains only a compile-time front-end IR until a deliberate parser-to-frontend-IR redesign. | Strengthens `bytecode_architecture_boundary_smoke` so non-frontend layers cannot import the lexer/parser/compiler pipeline, cannot import parser AST, and cannot store obvious parser-AST execution markers such as `Rc<[Stmt]>`, `Expr::`, or `FunctionSpec`. `CompiledScript::compile` is documented as the only source-to-bytecode bridge, `docs/architecture.md` defines allowed guarded runtime slow-path terminology, `docs/roadmap.md` no longer describes bytecode as future work, and the `CompiledScript` status is marked as runtime bytecode foundation complete. The root engine crate version was bumped to `0.2.84`. Validation passed with `cargo fmt --all -- --check`, `cargo test --test bytecode_architecture_boundary_smoke`, `cargo clippy --all-targets --all-features`, `cargo test --all-targets --quiet`, and `scripts/test-all.sh`. Report `rsqjs-test-report-20260707T105539Z.md` keeps engine fixtures at 68/68, active Test262 at 71/71, full Test262 at 15319/102578 passed, QuickJS differential at 64/64, and benchmark latency exceptions at 40/63. |
| [x] | Done | Bytecode-only binding metadata boundary | Runtime architecture / tests / docs | Keep slot and binding metadata available to bytecode/runtime without letting runtime depend on the AST scope analyzer. | Moves `BindingLayout`, `BindingOperand`, scope ids, slot ids, declarations, and upvalue metadata into `binding_metadata`. The `binding_layout` module remains the front-end AST analyzer and only builds `binding_metadata::BindingLayout` from a parsed `Program`; bytecode, runtime, compiler, and `CompiledScript` now import the AST-free metadata layer. The architecture guard treats `binding_layout` as part of the front-end pipeline, so runtime/API/value/storage/object/native/function layers cannot import it. This does not remove generic guarded slow paths, because cache misses, descriptors, prototype changes, coercion, host callbacks, and dynamic shadowing still require correct semantic paths. |
| [x] | Done | Nullish coalescing and logical assignment bytecode tranche | Language compatibility / runtime architecture / Test262 | Compile `??`, `&&=`, `||=`, and `??=` into bytecode-native short-circuit operations instead of any AST-era execution path. | Adds lexer/parser coverage for nullish coalescing and logical assignment operators, including the ECMAScript restriction against mixing unparenthesized `??` with `&&` or `||`. Runtime execution uses bytecode-owned RHS blocks and bytecode assignment references for binding, static-property, static array-index, and computed-property targets so the target is evaluated once and the RHS is evaluated only when the operator semantics require a store. Focused Rust coverage verifies binding/property/computed target order and short-circuit behavior, an active Test262-style fixture keeps the supported subset green, a QuickJS differential fixture compares the observable output against the reference engine, and official Test262 manifest entries cover representative coalesce and logical-assignment files. The root engine crate was bumped to `0.2.106`; the runner crate was bumped to `0.2.14`. Validation passed after rebasing onto the compiler module cleanup and string-concat mainline with `RSQJS_FAST_RUNNER=1 scripts/check-fast.sh` and `scripts/test-all.sh`; local report `target/rsqjs-reports/test-runs/rsqjs-test-report-20260708T084448Z.md` has engine fixtures 68/68, active Test262 75/75, full Test262 18225/102578, QuickJS differential 68/68, and benchmarks failed 0 with 63 measured rows. This does not remove `src/ast`; that remains the compile-time frontend IR until the separate frontend-IR redesign. |
| [x] | Done | Math residual built-ins tranche | Compatibility / built-ins / Test262 | Close the remaining high-value `Math` descriptor and method gaps without introducing reference-engine assumptions for newer methods. | Adds exact descriptors for `Math`, Math constants, and `Math[Symbol.toStringTag]`, implements `Math.f16round`, implements `Math.sumPrecise` through a safe fixed superaccumulator, fixes `Math.round` negative-zero and large-integer behavior, wires the new methods through native metadata and direct dispatch, and adds focused engine plus active Test262 coverage. QuickJS differential coverage was intentionally not added for `f16round` and `sumPrecise` because the pinned reference may not expose those newer methods. The root engine crate was bumped to `0.2.129`; the runner crate was bumped to `0.2.30`. Validation passed after rebasing onto `origin/main` with `cargo test --test math_builtin_smoke -- --nocapture`, a targeted full-Math Test262 run, `cargo clippy --all-targets -- -D warnings`, `RSQJS_FAST_RUNNER=1 scripts/check-fast.sh`, and `scripts/test-all.sh`; local report `target/rsqjs-reports/test-runs/rsqjs-test-report-20260708T162557Z.md` has engine fixtures 68/68, active Test262 99/99, file conformance 15678/53404, full Test262 30113/102578, QuickJS differential 86/86, benchmarks failed 0 invalid 0, JetStream failed 0 invalid 0, and `built-ins/Math` at 319/327 file pass plus 638/654 full-variant pass. Remaining Math failures are broader engine gaps: object `ToPrimitive`/`valueOf` coercion, `Math.pow` edge cases, generator syntax, and BigInt syntax. |
| [ ] | Backlog | Frontend IR and parser AST removal | Runtime architecture / frontend | Remove the compile-time parser AST after runtime bytecode execution is stable, without regressing diagnostics, binding analysis, or Test262 coverage. | First define a compact frontend IR with explicit ids for statements, expressions, functions, bindings, property accesses, call sites, literal pools, and source spans. Then migrate `binding_layout` and `compiler` from `src/ast` to that IR while the parser still builds it through the current AST bridge. Finally make the parser emit the IR directly and delete `src/ast` only after architecture guards, embedding tests, focused compiler tests, and full Test262 reports stay stable. This is separate from AST fallback removal: runtime AST fallback is already forbidden and guarded. |
| [ ] | Backlog | Bytecode quickening and direct operands | Runtime architecture / performance | Specialize validated generic bytecode into compact direct operands for the hottest paths without adding JIT or unsafe code. | Binding-related bytecode instructions now carry checked `BytecodeBinding` operands, unresolved `NaN`/`Infinity` loads and `typeof` checks can use guarded direct numeric constants, unresolved callable builtin `CallBinding` can bypass builtin binding-cell reads under shadow guards, static property bytecode instructions carry checked `BytecodeProperty` operands, computed/dynamic bytecode property instructions carry checked `BytecodeDynamicProperty` operands, binding/static/computed-literal member calls can carry guarded `NativeCallTarget` operands, array native targets have a guarded array-specific dispatch lane, `eval`, `Symbol`, `Function.prototype.bind`, `Function.prototype.call`, `Object.getOwnPropertyNames`, and `RegExp.prototype.test` participate in direct native-call target validation, static/computed member native-call sites cache validated native-function kinds per access site, arithmetic `+`, `-`, `*`, `/`, `%`, and `**` use `NumberBinary` with generic fallback, static `length` member reads use `ArrayLength` with non-array fallback, canonical static numeric property reads/writes use `ArrayIndexMember`/`ArrayIndexAssign` with generic fallback, dynamic computed array index reads/writes now bypass generic dynamic property materialization when the receiver and key prove an array-storage operation is valid, bytecode call/literal argument popping avoids the old pop/reverse loop, and all bytecode call instructions now pass stack-tail arguments by borrowed slice before crossing any real owned boundary. Continue from measured bytecode hot paths with broader dense-array loop instructions where semantics are guarded, broader method-specific native argument fast paths, and mutable quickening state only where the immutable compiled bytecode operands are insufficient. Keep quickened instructions invalidatable through existing shape/prototype/native-function guards and preserve explicit fallback for dynamic scopes, descriptors, prototype index properties, host callbacks, and coercion side effects. |
| [ ] | Backlog | Broader per-site inline caches | Runtime architecture / performance | Extend the static member-read, computed-read/write, static own-write, `in`, and `CallValue` occurrence caches into broader polymorphic call, descriptor-aware write, and bytecode property access paths. | Static member reads, side-effect-free computed literal reads, non-literal computed reads, static and computed own-property writes, computed compound/update writes, lexical `for...in` computed assignment targets, `in` presence checks, guarded static/computed member native-call dispatch, and monomorphic arbitrary `CallValue` dispatch now have checked per-occurrence slots or per-access native-call slots. Static and dynamic bytecode property operands make cache sites explicit in the bytecode ABI, guarded direct-native calls reuse those property/cache paths before validating the current native function id, array-native direct calls then take a smaller dispatch lane when the validated target is an array constructor or method, and call-value sites guard the current JS/native/host function id before direct dispatch. Dynamic computed caches validate the current `PropertyKey` before reuse and bypass arrays to preserve indexed element behavior. Next cache shape id, slot offset, receiver/prototype guards, and descriptor facts for the remaining access kinds, especially descriptor-sensitive writes, polymorphic function call dispatch, and dense-array loop sites. Keep fallback paths correct, avoid cache churn across receiver shapes, dynamic keys, native-function mutation, or host callback replacement, and keep cache storage checked through newtype indices. |
| [ ] | Backlog | Compact values and heap strings | Runtime architecture / memory | Reduce scattered allocations and cloned strings without introducing unsafe allocator work first. | `StringId`, `JsString`, `StringHeap`, script-local `StaticString` literals, the host return boundary, string index-property heap strings, context-aware string-wrapper descriptor values, context-aware error property heap strings, string-wrapper heap storage, context-owned binding/property/function-argument normalization, selected heap-backed runtime string paths, and VM string resource accounting now exist. Next migrate context-free diagnostic/property fallbacks outside the descriptor path and other remaining `Value::String(String)` producers toward heap-backed values; then compact object/function/string handles, add Vec-backed heaps and free lists, box immutable constants, and defer NaN boxing unless safer structural changes are exhausted. |
| [ ] | Backlog | Parallel VM execution and compiled-script sharing | Embedding API / runtime architecture | Document and implement the realistic parallelism model for embedders. | Do not try to parallelize one arbitrary JavaScript context. Support independent VM execution, parallel compilation of different scripts, immutable compiled-script sharing, context pooling, async host callbacks through the job queue, and later background cleanup where safe. |
| [ ] | Backlog | Performance and memory checkpoints | Performance and memory | Bring tracked benchmark exceptions back within the `1.00x` latency and memory budget where measurements are stable. | These are recurring checkpoint tasks driven by reports, not the whole project direction. Preserve semantics with engine tests and QuickJS differential cases. |

## Default Project Sequence

The order below is the default project direction. It can change when the latest
report shows a clearer bottleneck, compatibility gap, or embedding API need,
but branches should document why they changed priority.

1. Keep the repository and reports trustworthy.
   Guardrails, CI, test reports, QuickJS setup, Test262 setup, and benchmark
   reporting are part of the product. Do not let them drift.

2. Stabilize the embedding API.
   Keep direct library tests ahead of CLI-only coverage. The API must support
   many VMs per process, isolated state, resource failures, deterministic
   teardown, host output, host functions, and reusable compiled scripts.

3. Expand compatibility in coherent tranches.
   Use Test262 feature maps to pick parser, runtime, built-in, and error-model
   clusters. Each branch should close a visible area and add engine plus QuickJS
   differential coverage where possible.

4. Grow practical built-ins.
   Prioritize `Object`, `Array`, `String`, `Number`, `Math`, `Boolean`,
   `Function`, standard errors, JSON, Date, RegExp, Map, and Set based on
   reports and embedding needs. Add benchmarks for hot paths as they land.

5. Keep host extension support close to real embedding use.
   Improve value conversion, host callback errors, VM-local callback ownership,
   and examples before adding advanced async behavior.

6. Add modules, promises, and async integration.
   Design module loading around embedder-owned I/O and policy. Add the
   JavaScript job queue before promises, async functions, and async Rust host
   callbacks.

7. Expand resource control.
   Move from source and runtime counters toward heap, stack, atom, job, module,
   host callback, and cancellation limits. Every limit must be visible through
   the library API and teardown reports.

8. Add observability as product surface.
   Structured events, profiling hooks, resource snapshots, and execution reports
   should be designed for production embedders, not only for local debugging.

9. Improve runtime data structures when they unblock features, resource
   accounting, observability, or measured debt.
   Atoms, slot-based locals, shape-based objects, dense arrays, and indexed
   heaps are architecture tasks. They should support compatibility and resource
   control while keeping QuickJS-like speed and footprint.

10. Add bytecode after the source language is broad enough.
    Bytecode should preserve the `CompiledScript` API and use bytecode
    regression tests plus QuickJS differential checks as the execution oracle.
    Parser-AST execution fallback is not allowed. Inline caches belong after
    shapes and bytecode.

11. Add explicit VM heap accounting and GC.
    The indexed heap model should grow into deterministic accounting and a safe
    collection strategy compatible with host callbacks, promises, queued jobs,
    and many isolated VMs.

12. Run performance and memory checkpoint tasks continuously.
    Benchmark exceptions should be handled as recurring checkpoint tasks, but
    they do not define the whole roadmap. When a feature makes a hot path
    slower, either fix it in the same branch or record a measured exception
    with a follow-up task.

## Branch Execution Protocol

Every implementation task follows this order:

1. Refresh repository context.
   Read `AGENTS.md`, `README.md`, `docs/architecture.md`,
   `docs/roadmap.md`, `docs/benchmarking.md`, and this document. Inspect the
   latest test report before choosing work.

2. Select one task.
   Pick one row from the task board, create a fresh worktree and branch from
   `origin/main`, and mark that row `In progress` in the task branch. Leave
   unrelated rows unchanged.

3. Define the outcome.
   State whether the branch is primarily compatibility, embedding API,
   testing, performance, memory, resource control, or observability work. Record
   the expected evidence before implementing.

4. Capture the baseline.
   Run the narrow tests or benchmarks that prove the current problem. For
   compatibility work, record the relevant Test262 area. For performance or
   memory work, record the current QuickJS comparison before optimizing.

5. Implement the smallest coherent step.
   Keep changes scoped to the selected task. Maintain safe Rust rules, explicit
   resource limits, VM isolation, and future compatibility with the embedding
   API.

6. Add coverage.
   Add project-specific engine tests for semantics, Test262 coverage when
   relevant, QuickJS differential coverage when the reference behavior exists,
   direct library API tests for embedding-facing behavior, and benchmark cases
   for hot paths.

7. Validate.
   Run formatting, clippy, targeted tests, and `scripts/check-fast.sh` during
   local iteration. Ready pull requests and merge-queue candidates use
   `scripts/test-all.sh` as the full CI gate; the resulting report, rollup, and
   chart stay under `target/reports/` as CI artifacts. After merge, the report
   publisher copies the already-tested report into `reports/test-runs/` and
   regenerates the tracked rollup and chart in one report-only commit.

8. Decide on performance and memory exceptions.
   If a comparable implemented benchmark exceeds `1.00x`, either optimize it in
   the same task or record a tracked exception with the benchmark name, measured
   ratio, suspected cause, and follow-up task. Use the CI report artifact for
   ordinary PR evidence. If an optimization was made, record the latency or
   memory effect in the task notes.

9. Finish the task board row.
   Before the PR is ready, change the row to `Done` or `Deferred`. Add a concise
   note about what changed, problems found, validation performed, and possible
   future work.

10. Open the PR.
    The PR description must explain what changed, why it changed, validation
    results, benchmark or memory results, known exceptions, future work, and
    the report artifact path when a full report was generated.

11. Merge and clean up.
    After green CI, squash-merge the PR, update the main checkout, remove the
    task worktree, and keep the branch.

## Architecture Notes

### `CompiledScript` And Bytecode

The public API should expose a stable compile/evaluate contract, not parser
implementation details. Expose bytecode-oriented controls only when they make
the embedder API cleaner. External API compatibility, test harness
compatibility, and runner compatibility are not design constraints while the
engine is still being built.
The current implementation makes `CompiledScript` bytecode-owned for runtime
execution metadata: the complete top-level `Program` is gone, function bodies
are compiled into `BytecodeFunction`, function parameter signatures and default
expressions are lowered into bytecode-owned metadata, call arguments use
value-stack frames, and the obsolete runtime AST interpreter modules have been
removed. The AST remains the parser front-end and compiler input; AST traversal
belongs in `parser`, `binding_layout`, and `compiler`, while `bytecode` and
`binding_metadata` remain AST-free executable metadata. Runtime layers consume
`binding_metadata`, not the `binding_layout` AST analyzer. AST must not re-enter
the runtime execution path.

Future work should specialize bytecode instructions, add direct operands, and
add new language features in the bytecode compiler/VM rather than preserving
obsolete AST-era surfaces.

Recent benchmark evidence suggests that parsing and compile are not the main
cost for many heavy implemented cases anymore. Do not treat bytecode as only a
parse-cache feature. The value of bytecode is a more compact dispatch model,
explicit resource accounting, compiler-assigned operands, and a place to attach
inline caches and quickened instructions.

### Slot-Based Bindings

String-keyed binding maps are simple but expensive. A compiler pass should
assign local, global, and upvalue slots before execution. The runtime should
then read and write `Vec<Value>` entries through checked newtype indices. This
also gives the engine a natural place to account for stack and closure memory.

The current runtime already moved scope storage in that direction: bindings are
stored in a `Vec` and found through an atom-to-slot map. Compiled scripts also
carry a `BindingLayout` that assigns stable `LocalSlot`, `GlobalSlot`, and
`UpvalueSlot` operands, can seed matching occurrence caches after runtime
locations are known, and can populate function parameter frames at compiled
local-slot offsets. Runtime lexical declarations, lexical `for...in` bindings,
catch parameter scopes, and function-local hoisted `var` declarations now also
try compiled local-slot offsets with a fallback to ordinary insertion when the
active runtime scope does not match the compiled frame shape. User globals are
separate from lazy builtin globals, so top-level declarations can best-effort
populate compiled `GlobalSlot` offsets without late builtin materialization
changing the user global frame. Compiled global layout also collects hoisted
`var` declarations before lexical globals, matching the runtime hoist pass that
creates `var` bindings before evaluating later top-level `let` and `const`
declarations. First global cache misses can build guarded locations directly
from compiled `GlobalSlot` operands, and proven top-level global cache hits can
use atom-free `ExactGlobal` locations only after checking that the runtime slot
still contains the compiled atom. Active declaration cache seeding can use
compiled slots before falling back to runtime location search, compound/update
writes reuse the already resolved binding cell, and assignment, compound
assignment, `delete identifier`, and constructor lookup paths check for an
existing static binding cell before falling back to lazy builtin materialization.
Repeated cached local, upvalue, and proven exact-global binding hits now resolve
the saved `BindingLocation` before consulting the static-name atom cache, so the
hot repeated eval path is closer to a future direct bytecode binding operand.
That is still not the final design. The next slot work should add true direct
slot operands for the remaining read/write/eval paths, then leave name lookup
for dynamic or fallback paths only. Exact global paths must still remain opt-in
and validated where Annex B or dynamic runtime scopes can change observable
shadowing behavior.

### Atom Interner

Identifiers and property keys should use `AtomId` values instead of repeated
owned strings on hot paths. The atom table belongs to the engine or VM boundary
and must not create mutable global state. Atoms should be usable by parser,
compiled scripts, object shapes, function metadata, and diagnostics.

The current implementation has a VM-owned atom table, stores lexical/global
binding keys as `AtomId` values, stores ordinary object and function custom
properties through atom-backed `PropertyKey` values, and carries dynamic
property keys through computed access paths when a key is already known. Public
APIs still accept string names, missing binding/property lookups do not create
atoms, and intrinsic function metadata still keeps string-compatible behavior.
The function metadata path now narrows `length`, `name`, and `prototype` into a
typed `FunctionPropertyKind` before descriptor handling, so later bytecode
property operands can target a compact kind instead of repeating local string
dispatch. Reusable string constants, compile-time atomization, fuller function
metadata atomization, bytecode property operands, and a hard atom-table budget
remain future work.

Full atomization means runtime object, function, built-in, and prototype paths
should not repeatedly allocate, clone, or compare owned `String` values. String
APIs can still accept `&str`; the hot representation should be `AtomId`.

### Shapes And Inline Caches

Shapes describe object layouts. Object instances store values in slots, and
shape transitions describe property additions or layout changes. Once shapes
exist, property access sites can cache `(ShapeId, offset)` and fall back to the
generic lookup path when the shape does not match.

Shapes must cover descriptor attributes and prototype behavior, not only plain
`obj.foo` reads. Prototype chains need an explicit version or equivalent guard
so cached prototype hits are invalidated when the chain changes. Inline caches
should start as interpreter or bytecode data structures, not as JIT code.
Compiled static `obj.foo` reads now use per-occurrence cache slots. This is
still an interpreter-level bridge: the long-term cache owner should become the
bytecode property operand once bytecode exists.

### Dense Arrays

Array storage should distinguish packed arrays, holey arrays, and sparse
objects. Packed storage is the default fast path. Holey storage preserves
JavaScript holes without forcing every array into dictionary mode. Sparse
storage remains the fallback for large or unusual indices.

Array fast paths should be guarded by layout and prototype facts. Packed arrays
can make `includes`, `indexOf`, `lastIndexOf`, `join`, `reverse`, `slice`, and
`concat` mostly linear storage work. Default-attribute `shift` and `unshift`
can use deque front operations so simple calls do not move every dense element.
Array literals and method results that already have a complete packed value
vector should build storage in bulk and update resource counters once instead
of walking through generic per-index property mutation. Holey arrays need
explicit missing-element semantics: direct storage reads are valid only when the
relevant prototype-chain range cannot provide indexed properties. Sparse
arrays, descriptor-modified arrays, arrays affected by prototype index
properties, and non-contiguous packed storage for slice-style operations must
fall back to the generic object/property path or materialize explicitly.

### Built-In Intrinsics And Native Calls

Built-in constructors, prototypes, and native functions should not require
repeated string lookup and object construction on hot paths. Shared immutable
metadata can describe intrinsic names, property attributes, native function
kinds, default prototypes, and method tables. Each VM still owns its mutable JS
objects and descriptors. Native function ids should be registered once in a
VM-local checked slot table keyed by native kind, so lazy materialization does
not rediscover existing functions by scanning storage. Direct native-call paths
are allowed when they preserve observable semantics and fall back when user code
shadows or redefines the built-in.

### Bytecode Quickening

Quickening is a safe-Rust alternative to JIT specialization. The engine can
start with generic bytecode operations such as `Add`, `LoadProp`, `StoreProp`,
and `Call`. After a successful generic execution, an instruction can be
rewritten or annotated as `AddNumber`, `LoadPropCached`, or `CallNativeBuiltin`
with checked guards. If the guard fails, execution falls back to the generic
operation and updates or clears the cache.

### Promise Jobs And Async Host Functions

The engine must not assume a process-wide async runtime. Async integration
belongs at the embedding boundary. A VM should own JavaScript jobs, while the
embedding application owns the executor that drives Rust futures. Once promises
exist, async host callbacks should return JavaScript promises and complete them
through explicit job-draining APIs.

### VM-Owned Heaps

The engine should prefer indexed, Vec-backed heaps over many small shared
allocations. Handles such as `ObjectId`, `FunctionId`, `ShapeId`, `AtomId`, and
future `StringId` values make ownership explicit, keep VM teardown simple, and
support resource accounting without a custom unsafe allocator.

The first memory-layout goal is structural: reduce scattered allocations and
large clones through compact handles, heap strings, boxed immutable constants,
shape sharing, and captured-variable cells only where closures need them. A
custom global allocator or NaN boxing should not be the first move; both add
complexity and are less likely to beat the gains from slots, atoms, shapes, and
dense arrays.

### Garbage Collection

The current indexed handle direction is compatible with a safe collector. A GC
design should start from explicit VM roots, stack slots, globals, closures,
objects, arrays, promises, and host callback handles. It must report memory
usage and reclaim all VM-owned state during teardown.

### Parallelism

One arbitrary JavaScript context should not be the main parallelism target.
Side effects, prototypes, getters, exceptions, and host callbacks make that
model fragile. The product should instead support parallel compilation of
different scripts, parallel execution of independent VMs, immutable
compiled-script sharing, context pooling, async host callbacks through the job
queue, and eventually background cleanup or collection when the root model is
explicit.
